"use client";

import { WorkspaceHeader } from "@/components/patterns/WorkspaceHeader";

export default function BetaWorkspaceSettingsPage() {
  return <section><WorkspaceHeader size="page" eyebrow="settings / workspace" title="operator settings" summary="Each area follows the edge-projected capability ledger; browser visibility never grants access." accent="information" /></section>;
}
