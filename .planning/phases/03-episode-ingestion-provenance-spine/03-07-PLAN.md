---
phase: 03-episode-ingestion-provenance-spine
plan: "07"
type: execute
wave: 4
depends_on: ["03-00", "02-12"]
files_modified:
  - cloudflare/migrations/0006_chat_history.sql
  - cloudflare/src/auth/access.ts
  - cloudflare/src/auth/operator-context.ts
  - cloudflare/src/auth/policy.ts
  - cloudflare/src/chat/*
  - cloudflare/src/ops-router.ts
  - cloudflare/tests/authenticated-chat.test.mjs
  - cloudflare/tests/chat-history.test.mjs
  - web/app/(operator)/ops/chat/*
  - web/lib/ops/policy.ts
  - web/tests/phase2/authenticated-chat.spec.ts
  - .planning/RELEASE-SAFE-INTEGRATION.md
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-04, AUTH-06, AUTH-07, AUTH-10, KNOW-01, KNOW-02, KNOW-04, QUAL-05, QUAL-12]
decisions: [D-03-07]
must_haves:
  truths:
    - "Cloudflare Access remains the only authentication authority for the operator chat surface."
    - "Every persisted conversation and message is owned by the server-resolved active operator."
    - "An operator can leave and later return to prior conversations after Access reauthentication."
    - "Anonymous /chat and /api/chat behavior remains unchanged and stateless."
    - "Conversation history is not silently converted into saved memory or fine-tuning data."
    - "The feature can be disabled through the server release manifest without a forced client update."
  artifacts:
    - path: "cloudflare/migrations/0006_chat_history.sql"
      provides: "Additive D1 schema with operator-owned conversations, messages, indexes, and lifecycle constraints"
    - path: "cloudflare/src/chat/history.ts"
      provides: "Validated persistence, owner-scoped reads, cursor pagination, and idempotent append"
    - path: "cloudflare/src/ops-router.ts"
      provides: "Access-verified, RBAC-checked authenticated chat route and origin boundary"
    - path: "web/app/(operator)/ops/chat"
      provides: "Operator chat and history projection without changing public chat routes"
    - path: "cloudflare/tests/authenticated-chat.test.mjs"
      provides: "JWT, roster, role, spoofing, expiry, and owner-isolation proof"
  key_links:
    - from: "Cf-Access-Jwt-Assertion"
      to: "operators.id"
      via: "verified Access identity resolves to active D1 operator context"
    - from: "chat_messages.conversation_id"
      to: "chat_conversations.operator_id"
      via: "foreign key and server-side owner predicate"
    - from: "public /api/chat"
      to: "existing edge chat contract"
      via: "no persistence branch and unchanged DTO/header behavior"
    - from: "release manifest"
      to: "/ops/chat"
      via: "server-governed preview, pause, and rollback gate"
<objective>
Implement a separately gated authenticated Ask WTF conversation surface whose
history is durable and operator-scoped, while preserving anonymous public chat
as a backward-compatible stateless projection.
</objective>
<context>
@AGENTS.md
@ISA.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RELEASE-SAFE-INTEGRATION.md
@.planning/phases/03-episode-ingestion-provenance-spine/03-07-CONTEXT.md
@cloudflare/src/auth/access.ts
@cloudflare/src/auth/operator-context.ts
@cloudflare/src/ops-router.ts
@web/app/api/chat/route.ts
@web/lib/auth/session.ts
</context>
<tasks>
<task type="checkpoint:human-verify">
  <name>Task 1: Approve Access path, identity, retention, and owner scope</name>
  <files>.planning/RELEASE-SAFE-INTEGRATION.md</files>
  <action>Record the exact Access application path, allowed identity policy, session-duration owner decision, operator mapping rule, conversation retention, deletion/export policy, and whether admins receive metadata-only or content access. Do not configure the external account in this task.</action>
  <verify><automated>rg -n "03-07|/ops/chat|retention|operator_id|metadata-only|Access" .planning/RELEASE-SAFE-INTEGRATION.md .planning/phases/03-episode-ingestion-provenance-spine/03-07-CONTEXT.md</automated><expected>Every identity, scope, lifecycle, and privacy choice is explicit before schema or route work.</expected></verify>
  <done>Owner decisions are recorded and no guessed Access or retention policy remains.</done>
</task>
<task type="auto">
  <name>Task 2: Add additive D1 conversation and message contracts</name>
  <files>cloudflare/migrations/0006_chat_history.sql; cloudflare/src/chat/history.ts; cloudflare/tests/chat-history.test.mjs</files>
  <action>Create operator-owned conversation and message tables with foreign keys, bounded fields, indexes, lifecycle state, and idempotent append constraints. Keep saved memory separate. Add tests for transactionality, ordering, retry idempotency, cursor pagination, retention, and deletion behavior.</action>
  <verify><automated>npm test --prefix cloudflare</automated><expected>Cloudflare and focused persistence tests pass without applying a remote migration or writing production data.</expected></verify>
  <done>History has a portable local contract and cannot cross operator ownership boundaries.</done>
</task>
<task type="auto">
  <name>Task 3: Wire Access-verified authenticated chat endpoints</name>
  <files>cloudflare/src/auth/access.ts; cloudflare/src/auth/operator-context.ts; cloudflare/src/auth/policy.ts; cloudflare/src/ops-router.ts; cloudflare/src/chat/*; cloudflare/tests/authenticated-chat.test.mjs</files>
  <action>Extend the existing verified Access identity context only as needed, add `/ops/api/chat` routes, recheck active D1 role at every request, derive operator ownership server-side, preserve sourceMode/episodeId/citations, and reject spoofed identity, expired, inactive, unknown, cross-operator, malformed, and replayed requests.</action>
  <verify><automated>npm test --prefix cloudflare</automated><expected>Cloudflare and focused auth/ownership tests pass with no raw token or client owner field trusted.</expected></verify>
  <done>Authenticated chat creates and reads only the current operator's durable history.</done>
</task>
<task type="auto">
  <name>Task 4: Add operator UI behind the server release gate</name>
  <files>web/app/(operator)/ops/chat/*; web/lib/ops/policy.ts; web/tests/phase2/authenticated-chat.spec.ts; .planning/RELEASE-SAFE-INTEGRATION.md</files>
  <action>Add `/ops/chat` history and conversation views, loading/empty/error/expired states, and a server-governed feature flag. Keep public `/chat` untouched. Never use localStorage, URL parameters, or client props as authorization or rollout authority.</action>
  <verify><automated>npm run test:contracts --prefix web; npm run build --prefix web</automated><expected>Existing public chat contract tests and focused operator journeys pass; feature-off state is reversible.</expected></verify>
  <done>The authenticated UI is an operator projection, not an authorization boundary.</done>
</task>
<task type="checkpoint:human-verify">
  <name>Task 5: Approve staging evidence before any live activation</name>
  <files>.project/HANDOFF.md; .planning/STATE.md</files>
  <action>Run local/staging authorization, persistence, logout/reauthentication, privacy, rollback, and public compatibility evidence. Obtain owner approval before any Access policy change, remote D1 migration, deployment, or production smoke.</action>
  <verify><automated>git diff --check; git status --short; rg -n "03-07|authenticated|history|rollback|owner approval" .project/HANDOFF.md .planning/STATE.md</automated><expected>Evidence is recorded, production actions remain absent, and unknown gates block activation.</expected></verify>
  <done>Mini-phase is either explicitly activated by owner evidence or remains paused without affecting public chat.</done>
</task>
</tasks>
<verification>
Run public `/api/chat` contract and source-mode suites before and after the
mini-phase. Run the authenticated matrix for valid/expired/tampered/unknown/
inactive identities, all roles, owner isolation, logout/reauthentication,
retry idempotency, retention/delete, audit privacy, feature-off rollback, and
public-route non-persistence. Do not use a live Access or D1 mutation as a
substitute for local proof; any live action requires a separate owner gate.
</verification>
<success_criteria>
Authenticated Ask WTF history is durable, owner-scoped, privacy-governed, and
reversible; public chat remains backward-compatible and stateless; no
production state changes without a separately approved activation.
</success_criteria>
