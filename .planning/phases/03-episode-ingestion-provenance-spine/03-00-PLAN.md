---
phase: 03-episode-ingestion-provenance-spine
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/RELEASE-SAFE-INTEGRATION.md
  - .planning/dispatch/2026-09-01-release-safe-integration-tasks.json
  - .planning/phases/03-episode-ingestion-provenance-spine/03-00-CONTEXT.md
autonomous: true
requirements: [COMP-01, COMP-02, PROV-07, QUAL-05, QUAL-12]
decisions: [D-03-00]
must_haves:
  truths:
    - "The deployed ee00c28 baseline is frozen as a local compatibility reference."
    - "Preserved WIP branches are treated as selective inputs, not whole-branch merges."
    - "Every proposed change has a server-side rollback or disable path before implementation."
    - "Parallel audit work is isolated, read-only, attributable, and cannot mutate production."
  artifacts:
    - path: ".planning/RELEASE-SAFE-INTEGRATION.md"
      provides: "Executable integration sequence, release/channel model, auth/session/memory/model gates, and rollback rules"
    - path: ".planning/dispatch/2026-09-01-release-safe-integration-tasks.json"
      provides: "Four independent audit tasks for the Temperance Execute fleet"
    - path: ".planning/phases/03-episode-ingestion-provenance-spine/03-00-CONTEXT.md"
      provides: "Phase authority and compatibility invariants"
  key_links:
    - from: ".planning/RELEASE-SAFE-INTEGRATION.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 3 gate and dependency sequence"
    - from: ".planning/dispatch/2026-09-01-release-safe-integration-tasks.json"
      to: "Temperance Execute fleet"
      via: "read-only isolated audit dispatch"

<objective>
Turn the release-safe review into a GSD-controlled entry gate. Reconcile the
preserved branches, define stable/beta/experimental/rollback behavior, and
split auth, sessions, memory, admin model controls, and fine-tuning into
dependency-ordered work without changing production.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-episode-ingestion-provenance-spine/03-00-CONTEXT.md
@.planning/RELEASE-SAFE-INTEGRATION.md
@.project/HANDOFF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze compatibility and branch-selection evidence</name>
  <files>.planning/RELEASE-SAFE-INTEGRATION.md</files>
  <read_first>README.md; PROJECT.md; .planning/ROADMAP.md; .planning/STATE.md; .project/HANDOFF.md</read_first>
  <action>Record origin/main ee00c28 as the local baseline. Build a route/API compatibility matrix and a branch overlap matrix for the preserved commits. Mark whole-branch merges prohibited where files overlap. Record every unknown separately from verified source evidence.</action>
  <acceptance_criteria>
    - The baseline commit and owner-supplied production receipt are clearly separated from local verification.
    - Public route, chat, citation, fallback, and source-mode compatibility rules are explicit.
    - Every preserved branch has a selective integration disposition.
  </acceptance_criteria>
  <verify>
    <automated>git status --short; git diff --check; git show --stat --oneline origin/main</automated>
    <expected>Clean planning diff with no production command or remote mutation.</expected>
  </verify>
  <done>Compatibility and branch-selection evidence is reviewable before code integration.</done>
</task>

<task type="auto">
  <name>Task 2: Define server-governed release channels and rollback</name>
  <files>.planning/RELEASE-SAFE-INTEGRATION.md</files>
  <read_first>docs/superpowers/specs/2026-08-27-wtf-os-whole-app-shell-design.md; docs/wtf-os-agentic-settings.md; .planning/ROADMAP.md</read_first>
  <action>Define stable, beta, experimental, paused, and rolled_back states with a persisted server-side manifest containing release ID, UI variant, flags, compatibility range, model policy, retrieval index, and cohort. Keep stable as default, previews opt-in, and rollback server-controlled without URL, cookie, localStorage, client-prop, or HTML-attribute selectors.</action>
  <acceptance_criteria>
    - No user is forced to update.
    - A beta or experimental cohort can be disabled without a code deploy.
    - A legacy UI rollback and a feature-level rollback are both defined.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "stable|beta|experimental|paused|rolled_back|rollback|localStorage|data-wtf-ui" .planning/RELEASE-SAFE-INTEGRATION.md docs/superpowers/specs/2026-08-27-wtf-os-whole-app-shell-design.md</automated>
    <expected>Channel and rollback rules are present and client-owned selectors are prohibited.</expected>
  </verify>
  <done>Release behavior is explicit and reversible before UI rollout work begins.</done>
</task>

<task type="auto">
  <name>Task 3: Sequence auth, sessions, memory, admin, models, and fine-tuning</name>
  <files>.planning/RELEASE-SAFE-INTEGRATION.md</files>
  <read_first>cloudflare/src/auth/policy.ts; cloudflare/src/auth/operator-context.ts; cloudflare/migrations/0001_ops_foundation.sql; web/lib/auth/session.ts; web/lib/models.ts</read_first>
  <action>Separate Access auth sessions, application conversations, agent runs, and explicitly saved memory. Define scoped RBAC for editor/admin/super_admin, protected admin panels, an allowlisted model registry with fallback and evaluation metadata, and fine-tuning gates for provenance, consent, redaction, evaluation, shadow rollout, and alias rollback.</action>
  <acceptance_criteria>
    - Public access remains stateless and protected surfaces deny by default.
    - Admin visibility does not imply raw prompt, response, or private-memory access.
    - Fine-tuning cannot consume private history by default and cannot become active before evaluation gates pass.
  </acceptance_criteria>
  <verify>
    <automated>git diff --check; rg -n "Access|D1|deny|editor|admin|super_admin|conversation|agent run|memory|model registry|fine-tun" .planning/RELEASE-SAFE-INTEGRATION.md</automated>
    <expected>Each capability has an owner, scope, dependency, and explicit activation gate.</expected>
  </verify>
  <done>Future work is dependency-ordered and does not silently expand production authority.</done>
</task>

</tasks>

<verification>
Run the four independent read-only audit tasks from
`.planning/dispatch/2026-09-01-release-safe-integration-tasks.json` using the
Temperance Execute fleet. Accept results only with a run index, substantive
output, and backend/model attribution. Integrate no code until the audit
reports and the compatibility gate are reviewed.
</verification>

<success_criteria>
The repository contains a reviewable GSD entry gate and a parallel audit
manifest. Production remains unchanged, stable remains the default, and all
future implementation is blocked behind explicit local verification and owner
approval gates.
</success_criteria>

<output>
Create `03-00-SUMMARY.md` after the audit fleet and planning checks are
complete; do not mark Phase 3 complete from this gate.
</output>
