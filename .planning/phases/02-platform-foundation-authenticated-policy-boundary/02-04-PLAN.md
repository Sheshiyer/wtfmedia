---
phase: 02-platform-foundation-authenticated-policy-boundary
plan: "04"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - cloudflare/src/auth/policy.ts
  - cloudflare/src/operators.ts
  - web/lib/auth/capabilities.ts
  - web/lib/auth/policies.ts
  - cloudflare/tests/operator-policy.test.ts
  - web/tests/security/phase2-threat-results/02-04.json
autonomous: true
requirements: [AUTH-03, AUTH-04, AUTH-09, QUAL-07, QUAL-12]
decisions: [D-09, D-10, D-11]
must_haves:
  truths:
    - "One closed deny-by-default policy governs pages, APIs, records, fields, exports, and navigation projection."
    - "The super_admin seat changes only through one atomic audited transfer path."
  artifacts:
    - path: "cloudflare/src/auth/policy.ts"
      provides: "Closed role/resource/action/record/field policy vocabulary"
    - path: "cloudflare/src/operators.ts"
      provides: "Safe roster mutation and atomic ownership transfer"
  key_links:
    - from: "web/lib/auth/policies.ts"
      to: "cloudflare/src/auth/policy.ts"
      via: "shared serializable policy contract and parity tests"
---

<objective>
Make authorization semantics closed, shared, and database-safe across every protected surface.
</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@web/lib/auth/capabilities.ts @web/lib/auth/policies.ts @cloudflare/src/db.ts @02-CONTEXT.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace allow-fallback policy with a closed capability matrix</name>
  <files>cloudflare/src/auth/policy.ts, web/lib/auth/capabilities.ts, web/lib/auth/policies.ts, cloudflare/tests/operator-policy.test.ts</files>
  <read_first>current capability/policy drafts; D-09/D-10; UI-SPEC activated navigation; AUTH-03/04 matrix; public-route classification</read_first>
  <action>Define closed roles, resources, actions, record predicates, and field sets. Explicitly model control_room read for all roles; operator administration and audit view/export for super_admin/admin; approved chat write for editor only where existing public Ask WTF is exited to, not a new operator module. Unknown inputs deny. Export a server policy decision and an allowlisted navigation/field projection; public routes remain a separate explicit classification.</action>
  <acceptance_criteria>
    - Exhaustive matrix tests cover super_admin/admin/editor plus anonymous/unknown and every activated route/action.
    - Missing policy, unknown string, tampered record/field/environment, and later shell roles deny.
    - UI projection exactly matches activated UI-SPEC destinations and cannot grant capability.
  </acceptance_criteria>
  <verify><automated>cd cloudflare &amp;&amp; npm test -- operator-policy --grep matrix</automated><expected>Exit 0; no protected unknown combination allows.</expected></verify>
  <done>The same closed vocabulary controls all server and UI projections.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement safe operator mutations and atomic super_admin transfer</name>
  <files>cloudflare/src/operators.ts, cloudflare/tests/operator-policy.test.ts, web/tests/security/phase2-threat-results/02-04.json</files>
  <read_first>migrated schema; approved roster; shared policy from Task 1; RESEARCH Pattern 3; audit encoder interface contract</read_first>
  <action>Implement invite, activate/deactivate, and admin/editor role changes only after server policy approval. Reject generic mutation of the current super_admin. Implement one D1 batch that demotes current owner, promotes an active approved target, appends a typed handoff event, then confirms exactly one active super_admin; any failure rolls back and returns a safe error. Run the exact threat runner.</action>
  <acceptance_criteria>
    - Admin cannot create, demote, deactivate, or transfer super_admin; only current super_admin can transfer.
    - Concurrent/partial/invalid transfers leave exactly one active super_admin and no orphan audit event.
    - Invite policy does not infer authority from email suffix or job title.
  </acceptance_criteria>
  <verify><automated>node web/scripts/run-phase2-threat.mjs --plan 02-04 --task 2</automated><expected>Exit 0; T-02-10..12 are recorded passed.</expected></verify>
  <done>Role administration is deny-by-default and ownership transfer is atomic plus audited.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Owning task | Automated command | Mitigation |
|---|---|---|---|---|---|---|
| T-02-10 | Elevation of Privilege | capability fallback | critical | Task 1 | cd cloudflare &amp;&amp; npm test -- operator-policy --grep unknown | Closed enums; unknown means deny. |
| T-02-11 | Tampering | operator mutations | high | Task 2 | cd cloudflare &amp;&amp; npm test -- operator-policy --grep mutation | Server policy before every mutation; safe field allowlist. |
| T-02-12 | Elevation of Privilege | ownership transfer | critical | Task 2 | cd cloudflare &amp;&amp; npm test -- operator-policy --grep transfer | D1 batch, unique index, postcondition, typed audit. |
</threat_model>
<verification>Run exhaustive policy and transactional transfer probes.</verification>
<success_criteria>All protected decisions fail closed and the owner invariant survives concurrency and failure.</success_criteria>
<output>Create 02-04-SUMMARY.md when complete.</output>
