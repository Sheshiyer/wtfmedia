---
phase: 02-platform-foundation-authenticated-policy-boundary
plan: "12"
type: execute
wave: 8
depends_on: ["02-11"]
files_modified:
  - .runtime/preflight/phase2-staging.json
  - web/tests/visual/phase2-approval.json
  - .runtime/preflight/phase2-production-smoke.json
  - web/tests/security/phase2-threat-results/02-12.json
  - .planning/phases/02-platform-foundation-authenticated-policy-boundary/02-VERIFICATION.md
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, AUTH-08, AUTH-09, AUTH-10, QUAL-07, QUAL-09, QUAL-10, QUAL-12, QUAL-13]
decisions: [D-03, D-04, D-18, D-19, D-20, D-23, D-24, D-25, D-26]
must_haves:
  truths:
    - "Staging setup and migrations occur only after exact-target preflight and produce sanitized create-only receipts."
    - "Production remains blocked until complete staging evidence and owner approval exist; the only production probe is read-only and exact-host allowlisted."
  artifacts:
    - path: ".runtime/preflight/phase2-staging.json"
      provides: "Ignored sanitized exact-target staging receipt"
    - path: "web/tests/visual/phase2-approval.json"
      provides: "Owner-bound UI and evidence approval"
    - path: ".planning/phases/02-platform-foundation-authenticated-policy-boundary/02-VERIFICATION.md"
      provides: "Final requirement and gate evidence index"
  key_links:
    - from: ".runtime/preflight/phase2-production-smoke.json"
      to: "approved production host"
      via: "read-only exact-host allowlist after owner gate"
---

<objective>
Execute the manifest-gated staging demonstration, collect owner approval, run one read-only production smoke, and close Phase 2 only on complete evidence.
</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@docs/PHASE2-OPERATIONS-RUNBOOK.md @web/scripts/verify-phase2.mjs @02-UI-SPEC.md @02-CONTEXT.md @PROJECT.md</context>
<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision and prove isolated staging through exact-target preflight</name>
  <files>.runtime/preflight/phase2-staging.json, web/tests/security/phase2-threat-results/02-12.json</files>
  <read_first>operations runbook; environment schema; staging migration commands; production gate D-23..D-26; project draft-held and external-mutation boundaries</read_first>
  <action>Before any remote command, require owner authorization for the exact staging hostname, Access app/policy, D1 database, Worker route, cache namespace, secret set, and planned commands. Validate BASE_URL parses to the exact allowlisted staging host, environment equals staging, every protected resource is distinct from production/local, preview has none, and receipts are ignored, create-only, symlink-refusing, and sanitized. Then apply repository migrations with explicit `--env staging --remote`, configure staging Access/operator route/secrets, deploy only the staging target, and run `npm run verify:phase2 -- --staging`. Never touch production or copy its data downward.</action>
  <acceptance_criteria>
    - Human approval names exact staging targets and commands before mutation.
    - Receipt contains only symbolic names, migration versions, status, timestamps, and distinctness hashes; no IDs, values, tokens, account numbers, or payloads.
    - Complete authorization/lifecycle/audit/isolation/UI/rollback/runbook staging matrix passes; failed or unknown exits nonzero.
  </acceptance_criteria>
  <verify><automated>cd web &amp;&amp; npm run verify:phase2 -- --staging --receipt ../.runtime/preflight/phase2-staging.json</automated><expected>Exit 0 with all staging sections proven and no production mutation.</expected></verify>
  <done>Staging is demonstrably isolated and all approved security, lifecycle, audit, UI, rollback, and runbook gates pass.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Obtain owner evidence approval and run exact-host read-only production smoke</name>
  <files>web/tests/visual/phase2-approval.json, .runtime/preflight/phase2-production-smoke.json, web/tests/security/phase2-threat-results/02-12.json, .planning/phases/02-platform-foundation-authenticated-policy-boundary/02-VERIFICATION.md</files>
  <read_first>staging receipt/evidence packet; all Phase 2 summaries and threat aggregate; UI candidate packet; production smoke runbook; UI-SPEC Responsive Evidence Matrix</read_first>
  <action>Present the staging packet and synthetic UI captures to the owner. Record approval bound to candidate/evidence hashes, exact commit, policy hash, and timestamp; rejection or missing review blocks. Only after approval, require a separately authorized exact production host and run the read-only smoke: public continuity, anonymous protected denial, authorized Control Room GET/status GET if the owner supplies a safe session, no-store headers, and version evidence. The smoke must not create operators, mutate roles, transfer ownership, export audit data, purge, change settings/policies/secrets, run migrations, deploy, or cut over traffic. Merge T-02-33..35 and write VERIFICATION.md mapping all requirements/decisions to persistent evidence.</action>
  <acceptance_criteria>
    - Owner approval is hash-bound and covers 320/768/1440, keyboard/focus/accessibility, rollback/runbook, and all staging gates.
    - Production smoke command is create/write/delete-free, exact-host allowlisted, sanitized, and fails on redirect to another host or unknown result.
    - VERIFICATION.md reports every AUTH/QUAL ID, D-01..D-26, T-02-01..35, plan summary, and gate as passed with source—or blocks close.
  </acceptance_criteria>
  <verify><automated>node web/scripts/run-phase2-threat.mjs --plan 02-12 --task 2 &amp;&amp; cd web &amp;&amp; npm run verify:phase2 -- --final</automated><expected>Exit 0 only after owner approval and read-only production smoke receipts are complete.</expected></verify>
  <done>Phase 2 closes with owner-approved staging evidence and a read-only production smoke; no cutover or production write occurred.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Owning task | Automated command | Mitigation |
|---|---|---|---|---|---|---|
| T-02-33 | Tampering | staging/production target | critical | Task 1 | node cloudflare/scripts/phase2-preflight.mjs --receipt .runtime/preflight/phase2-staging.json | Exact parsed host/env/resource distinctness before remote commands. |
| T-02-34 | Elevation of Privilege | release gate | critical | Task 2 | cd web &amp;&amp; npm run verify:phase2 -- --final | Hash-bound owner approval; every failed/unknown gate blocks. |
| T-02-35 | Information Disclosure | production smoke | critical | Task 2 | node web/scripts/phase2-production-smoke.mjs --receipt .runtime/preflight/phase2-production-smoke.json | Exact-host read-only allowlist and sanitized output. |
</threat_model>
<verification>Require human staging authorization, complete staging verification, owner evidence approval, read-only production smoke, and final aggregate.</verification>
<success_criteria>Phase 2 is accepted without silent live writes, target ambiguity, evidence gaps, or production cutover.</success_criteria>
<output>Create 02-12-SUMMARY.md and finalize 02-VERIFICATION.md when complete.</output>
