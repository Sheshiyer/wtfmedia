---
phase: 02-platform-foundation-authenticated-policy-boundary
plan: "07"
type: execute
wave: 5
depends_on: ["02-06"]
files_modified:
  - web/lib/ops/context.ts
  - web/lib/ops/policy.ts
  - web/lib/ops/dto.ts
  - web/lib/ops/cache.ts
  - web/lib/ops/return-to.ts
  - web/lib/auth.ts
  - web/lib/auth/session.ts
  - web/middleware.ts
  - web/app/ops/recover/page.tsx
  - web/tests/phase2/operator-lifecycle.test.ts
  - web/tests/security/phase2-threat-results/02-07.json
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04, AUTH-06, AUTH-07, QUAL-07, QUAL-12]
decisions: [D-06, D-07, D-08]
must_haves:
  truths:
    - "Next.js independently authorizes every protected page, handler, server action, query, export, and field through server-only request context."
    - "Expiry, denial, deactivation, and sign-out clear protected state before a non-leaking recovery surface renders."
  artifacts:
    - path: "web/lib/ops/context.ts"
      provides: "Server-only request-scoped operator authorization"
    - path: "web/app/ops/recover/page.tsx"
      provides: "Public-safe recovery outside protected shell"
  key_links:
    - from: "web/lib/ops/context.ts"
      to: "Cloudflare protected origin context"
      via: "verified server-only proof plus fresh authorization call"
---

<objective>
Replace the unsigned local session draft with a server-only Next.js DAL and complete non-leaking lifecycle recovery.
</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@web/lib/auth.ts @web/lib/auth/session.ts @web/middleware.ts @02-UI-SPEC.md @02-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build request-scoped Next.js authorization, DTO, and cache boundary</name>
  <files>web/lib/ops/context.ts, web/lib/ops/policy.ts, web/lib/ops/dto.ts, web/lib/ops/cache.ts, web/lib/auth.ts, web/lib/auth/session.ts, web/middleware.ts</files>
  <read_first>current auth/session/middleware drafts; edge ops-router contract; shared policy; Next.js data-security guidance; public DTO/cache contracts</read_first>
  <action>Create server-only modules that verify trusted-origin input and reauthorize immediately before every protected read/mutation. Use React cache only to deduplicate within one render request; prohibit persistent authorization cache. Middleware may reject obvious direct access but cannot grant authority. Delete all wtf_session creation/read logic, header-only identity, atob validation, and editor fallback. Produce minimal DTOs and private,no-store behavior for pages, RSC, handlers, server actions, prefetches, queries, exports, and errors.</action>
  <acceptance_criteria>
    - Source scan finds no wtf_session authority, JWT payload decode, header role, or default editor path.
    - Direct invocation of every protected server entry point denies without independently verified context.
    - Public and operator DTO/cache namespaces and client query keys remain distinct.
  </acceptance_criteria>
  <verify><automated>cd web &amp;&amp; npm run test:unit -- operator-boundary &amp;&amp; npm run typecheck</automated><expected>Exit 0; all Next.js entry points use the server-only authorization boundary.</expected></verify>
  <done>Next.js mirrors edge policy but never treats middleware or UI state as authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement validated recovery, state clearing, and Access logout</name>
  <files>web/lib/ops/return-to.ts, web/app/ops/recover/page.tsx, web/tests/phase2/operator-lifecycle.test.ts, web/tests/security/phase2-threat-results/02-07.json</files>
  <read_first>UI-SPEC Recovery and sign-out contract; current client stores and pending chat state; D-07/D-08; Cloudflare logout/revocation-lag guidance</read_first>
  <action>Provide reauthenticate, operator-unavailable, verification-unavailable, and signing-out modes outside the protected shell. Before navigation/render, clear operator context, protected query caches, roster/audit/status rows, and pending responses. Preserve only a decoded/normalized same-origin /ops path; reject external, scheme-relative, encoded, and public/private crossover targets. Navigate through Access logout after clearing and keep fresh edge+D1 checks during the documented lag. Use exact approved copy and focus behavior.</action>
  <acceptance_criteria>
    - Recovery DOM contains no prior identity, role, nav, counts, rows, RSC payload, or pending protected response.
    - Open-redirect corpus accepts only /ops or descendants after canonical normalization.
    - Failed logout never restores protected state; reentry requires fresh Access and D1 checks.
  </acceptance_criteria>
  <verify><automated>node web/scripts/run-phase2-threat.mjs --plan 02-07 --task 2</automated><expected>Exit 0; T-02-19..21 pass.</expected></verify>
  <done>Lifecycle failures and sign-out transition to a public-safe surface before any protected content can persist.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Owning task | Automated command | Mitigation |
|---|---|---|---|---|---|---|
| T-02-19 | Spoofing | recovery return target | high | Task 2 | cd web &amp;&amp; npm run test:unit -- operator-lifecycle --grep redirect | Canonical same-origin /ops allowlist. |
| T-02-20 | Information Disclosure | protected client state | critical | Task 2 | cd web &amp;&amp; npm run test:browser -- tests/phase2/operator-lifecycle.spec.ts | Clear stores before public-safe recovery render. |
| T-02-21 | Elevation of Privilege | logout lag | high | Task 2 | cd web &amp;&amp; npm run test:browser -- tests/phase2/operator-lifecycle.spec.ts --grep logout | Fresh Access plus D1 on every protected retry. |
</threat_model>
<verification>Run server-entry, state-clearing, redirect, focus, and logout-lag probes.</verification>
<success_criteria>No local cookie, middleware shortcut, stale state, or recovery target can grant or reveal operator access.</success_criteria>
<output>Create 02-07-SUMMARY.md when complete.</output>
