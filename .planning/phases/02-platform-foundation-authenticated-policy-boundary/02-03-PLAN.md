---
phase: 02-platform-foundation-authenticated-policy-boundary
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - cloudflare/package.json
  - cloudflare/src/auth/access.ts
  - cloudflare/src/auth/operator-context.ts
  - cloudflare/tests/access-auth.test.ts
  - web/tests/security/phase2-threat-results/02-03.json
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-07, QUAL-07, QUAL-10, QUAL-12]
decisions: [D-01, D-05, D-06]
must_haves:
  truths:
    - "Only a cryptographically verified Cloudflare Access application token can establish identity."
    - "Every valid identity still requires a fresh active recognized D1 operator lookup."
  artifacts:
    - path: "cloudflare/src/auth/access.ts"
      provides: "Remote-JWKS issuer/audience/expiry verification"
    - path: "cloudflare/tests/access-auth.test.ts"
      provides: "Forged, expired, wrong-audience, missing-operator matrix"
  key_links:
    - from: "cloudflare/src/auth/operator-context.ts"
      to: "OPS_DB operators"
      via: "per-request normalized email lookup"
---

<objective>
Establish the sole authentication chain: verified Access JWT followed by fresh D1 authorization.
</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@cloudflare/src/index.ts @cloudflare/src/db.ts @web/lib/auth.ts @02-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Verify Access application tokens with closed claims</name>
  <files>cloudflare/package.json, cloudflare/src/auth/access.ts, cloudflare/tests/access-auth.test.ts</files>
  <read_first>current Worker package/index; current web/lib/auth.ts decode-only seam; RESEARCH Pattern 1; official Cloudflare token validation source</read_first>
  <action>Pin jose@6.2.10 in the Worker runtime and implement createRemoteJWKSet plus jwtVerify for Cf-Access-Jwt-Assertion with required issuer, audience, expiry, and normalized email claim. Reject missing, malformed, forged, wrong-issuer, wrong-audience, expired, or email-less tokens with one safe denial. Never trust Cf-Access-Authenticated-User-Email, role headers, atob, or a fallback role.</action>
  <acceptance_criteria>
    - Only a valid signature+issuer+audience+expiry fixture returns a normalized email.
    - Every invalid fixture returns the same safe failure shape and logs no token/body.
    - Dependency is exact-pinned and no unrelated package upgrade occurs.
  </acceptance_criteria>
  <verify><automated>cd cloudflare &amp;&amp; npm test -- access-auth --grep jwt</automated><expected>Exit 0; complete JWT failure matrix is deny-only.</expected></verify>
  <done>Cloudflare Access remains the only cryptographically accepted authentication authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bind verified identity to fresh D1 operator authority</name>
  <files>cloudflare/src/auth/operator-context.ts, cloudflare/tests/access-auth.test.ts, web/tests/security/phase2-threat-results/02-03.json</files>
  <read_first>Task 1 verifier; migrated operators schema; D-05..D-08; minimum authorization matrix</read_first>
  <action>Resolve each verified normalized email against OPS_DB for the current request and accept only active super_admin/admin/editor records. Deny absent, inactive, unknown-role, and database-unavailable records uniformly. Return a request-scoped server context containing opaque operator ID, normalized email, effective role, environment, and correlation ID only. Run the plan threat runner.</action>
  <acceptance_criteria>
    - Each protected authorization call performs a D1 lookup; no cross-request role cache exists.
    - Missing/inactive/unknown records are indistinguishable to the client.
    - Context contains no token, raw provider claim, account ID, or secret.
  </acceptance_criteria>
  <verify><automated>node web/scripts/run-phase2-threat.mjs --plan 02-03 --task 2</automated><expected>Exit 0; T-02-07..09 pass with persistent bounded evidence.</expected></verify>
  <done>Authentication and application authorization are two distinct fail-closed checks.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Owning task | Automated command | Mitigation |
|---|---|---|---|---|---|---|
| T-02-07 | Spoofing | Access JWT | critical | Task 1 | cd cloudflare &amp;&amp; npm test -- access-auth --grep forged | Signature, issuer, audience, expiry verification. |
| T-02-08 | Spoofing | identity headers | high | Task 1 | cd cloudflare &amp;&amp; npm test -- access-auth --grep header | Ignore header-only identity and role hints. |
| T-02-09 | Elevation of Privilege | operator lookup | critical | Task 2 | cd cloudflare &amp;&amp; npm test -- access-auth --grep operator | Fresh D1 active/recognized role check on every request. |
</threat_model>
<verification>Run JWT and operator matrix through the exact threat runner.</verification>
<success_criteria>No token, header, default role, or stale cache can bypass Access plus D1 authorization.</success_criteria>
<output>Create 02-03-SUMMARY.md when complete.</output>
