---
phase: 02-platform-foundation-authenticated-policy-boundary
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - cloudflare/migrations/0001_ops_foundation.sql
  - cloudflare/migrations/0002_bootstrap_roster.sql
  - cloudflare/src/db.ts
  - cloudflare/config/ops-environments.schema.json
  - cloudflare/tests/d1-migrations.test.ts
  - web/tests/security/phase2-threat-results/02-02.json
autonomous: true
requirements: [AUTH-09, AUTH-10, QUAL-10, QUAL-12]
decisions: [D-03, D-12, D-13, D-18, D-19, D-20]
must_haves:
  truths:
    - "Repository migrations enforce recognized roles, normalized unique email, append-only audit shape, and exactly one active super_admin."
    - "Local, staging, and production bindings remain symbolically distinct without committed provider identifiers."
  artifacts:
    - path: "cloudflare/migrations/0001_ops_foundation.sql"
      provides: "Portable operator and audit schema with database invariants"
    - path: "cloudflare/config/ops-environments.schema.json"
      provides: "Secret-free environment binding contract"
  key_links:
    - from: "cloudflare/src/db.ts"
      to: "cloudflare/migrations"
      via: "Wrangler migration status, never runtime CREATE TABLE"
---

<objective>
Replace the unsafe draft schema with portable, forward-only D1 migrations and database-enforced invariants.
</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@cloudflare/src/schema.sql @cloudflare/src/db.ts @cloudflare/wrangler.jsonc @02-CONTEXT.md @02-RESEARCH.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create operator, audit, and environment migration contracts</name>
  <files>cloudflare/migrations/0001_ops_foundation.sql, cloudflare/config/ops-environments.schema.json, cloudflare/tests/d1-migrations.test.ts</files>
  <read_first>cloudflare/src/schema.sql; cloudflare/src/db.ts; wrangler.jsonc; D-03, D-11, D-14..D-20; RESEARCH Pattern 3-5</read_first>
  <action>Create ordered Wrangler migrations for operators and typed audit events. Enforce lowercase normalized unique email, roles super_admin/admin/editor only, active flag checks, environment/outcome/action envelope checks, foreign keys, indexes, and a partial unique index permitting exactly one active super_admin. Do not add an editable retention setting or generic audit-update path. Define symbolic required bindings/audience/secret/cache names for local, staging, production, and no protected preview default; exclude IDs and values.</action>
  <acceptance_criteria>
    - Fresh local migration succeeds; repeated migration listing is idempotent.
    - Zero or two active super_admin inserts fail; unknown roles and malformed audit envelopes fail.
    - Environment schema proves distinct symbolic resources and contains no numeric IDs, secrets, or account values.
  </acceptance_criteria>
  <verify><automated>cd cloudflare &amp;&amp; npx wrangler d1 migrations apply OPS_DB --local --persist-to .wrangler/phase2-02-02 &amp;&amp; npm test -- d1-migrations</automated><expected>Exit 0; local-only migrations and invariant probes pass.</expected></verify>
  <done>The schema is portable and database-enforced without any remote mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap the approved roster and remove runtime schema authority</name>
  <files>cloudflare/migrations/0002_bootstrap_roster.sql, cloudflare/src/db.ts, web/tests/security/phase2-threat-results/02-02.json</files>
  <read_first>02-CONTEXT.md D-12/D-13; .project/HANDOFF.md roster; current db.ts/schema.sql; migration tests from Task 1</read_first>
  <action>Add an idempotent bootstrap migration for the seven approved identities and exact roles, leaving Yash job title and roster completeness absent/unknown. Refactor db.ts to assume migrated tables and fail on missing migration rather than creating or altering tables at runtime. Run the Phase 2 threat runner for Task 2.</action>
  <acceptance_criteria>
    - Bootstrap produces one super_admin, one admin, five editors, all active, with normalized emails and no inferred job titles.
    - Reapplying or listing migrations never creates duplicate operators.
    - Runtime code has no CREATE TABLE, hard-coded 90-day retention, or arbitrary metadata acceptance.
  </acceptance_criteria>
  <verify><automated>node web/scripts/run-phase2-threat.mjs --plan 02-02 --task 2</automated><expected>Exit 0; T-02-04..06 persist as passed in the 02-02 fragment.</expected></verify>
  <done>Approved roster and schema invariants are migration-owned and proven locally.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Owning task | Automated command | Mitigation |
|---|---|---|---|---|---|---|
| T-02-04 | Elevation of Privilege | super_admin invariant | critical | Task 2 | cd cloudflare &amp;&amp; npm test -- d1-migrations --grep super_admin | Partial unique index and exact bootstrap count. |
| T-02-05 | Tampering | audit schema | high | Task 1 | cd cloudflare &amp;&amp; npm test -- d1-migrations --grep audit | Typed columns/checks; no general update contract. |
| T-02-06 | Information Disclosure | environment config | high | Task 1 | node cloudflare/scripts/verify-ops-environments.mjs --static | Symbolic bindings only; reject IDs/secrets/shared targets. |
</threat_model>
<verification>Apply migrations only to isolated local persistence and run invariant/static scans.</verification>
<success_criteria>D1 schema and bootstrap satisfy locked invariants without touching staging or production.</success_criteria>
<output>Create 02-02-SUMMARY.md when complete.</output>
