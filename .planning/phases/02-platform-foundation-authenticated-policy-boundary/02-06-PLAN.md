---
phase: 02-platform-foundation-authenticated-policy-boundary
plan: "06"
type: execute
wave: 4
depends_on: ["02-03", "02-04", "02-05"]
files_modified:
  - cloudflare/src/ops-router.ts
  - cloudflare/src/index.ts
  - cloudflare/src/dto.ts
  - cloudflare/tests/ops-router.test.ts
  - cloudflare/tests/cache-isolation.test.ts
  - web/tests/security/phase2-threat-results/02-06.json
autonomous: true
requirements: [AUTH-02, AUTH-04, AUTH-06, AUTH-10, QUAL-07, QUAL-09, QUAL-10, QUAL-12]
decisions: [D-02, D-09, D-10, D-18, D-20]
must_haves:
  truths:
    - "The protected Cloudflare route is the only origin path that can create trusted operator context."
    - "Protected DTOs, errors, RSC payloads, exports, and cache behavior never mix with public traffic or another operator."
  artifacts:
    - path: "cloudflare/src/ops-router.ts"
      provides: "Access plus D1 plus policy protected operator gateway"
    - path: "cloudflare/tests/cache-isolation.test.ts"
      provides: "Identity, role, environment, DTO, error, and cache mixing probes"
  key_links:
    - from: "cloudflare/src/ops-router.ts"
      to: "existing Vercel application"
      via: "server-only trusted-origin proof after policy approval"
---

<objective>
Compose Access verification, D1 authorization, shared policy, DTO projection, audit, and trusted-origin routing into one protected edge boundary.
</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@cloudflare/src/index.ts @cloudflare/src/auth/* @cloudflare/src/operators.ts @cloudflare/src/audit.ts @docs/CLOUDFLARE-INFRASTRUCTURE.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create protected operator router and safe origin handoff</name>
  <files>cloudflare/src/ops-router.ts, cloudflare/src/index.ts, cloudflare/tests/ops-router.test.ts</files>
  <read_first>current Worker routing/secret checks; Plans 03-05 modules; D-01/D-02/D-05/D-09; existing public Worker API contract</read_first>
  <action>Route only the dedicated protected hostname/path classification into the Access verifier, per-request D1 lookup, capability decision, and audit encoder. Forward approved operator requests to Vercel with a rotatable server-only origin proof plus minimal signed/verified context; direct Worker/Vercel, missing proof, preview-unbound, wrong environment, unknown route/action, and spoofed headers deny uniformly. Preserve all public chat/catalogue behavior and secrets.</action>
  <acceptance_criteria>
    - No origin request occurs on invalid Access, missing/inactive/unknown operator, or denied capability.
    - Direct Vercel and preview requests cannot manufacture trusted context using headers.
    - Existing public Worker routes and response contracts remain unchanged.
  </acceptance_criteria>
  <verify><automated>cd cloudflare &amp;&amp; npm test -- ops-router</automated><expected>Exit 0; trusted route and all direct/spoofed paths behave as specified.</expected></verify>
  <done>The Cloudflare operator boundary is the only trusted entrance to protected origin code.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Enforce operator DTO, error, export, and cache isolation</name>
  <files>cloudflare/src/dto.ts, cloudflare/tests/cache-isolation.test.ts, web/tests/security/phase2-threat-results/02-06.json</files>
  <read_first>public DTO contracts; policy field projections; ops-router; current Cache-Control behavior; AUTH-06 and research Pitfall 2/3</read_first>
  <action>Define allowlisted edge DTOs for operator context, roster, audit rows, status, and safe errors. Set protected responses private,no-store and an explicit Cloudflare cache bypass; never store authorization or protected DTOs in KV/cache. Add sequential/concurrent probes across users, roles, environments, RSC/prefetch-like requests, record/field tampering, safe errors, and exports. Run the threat runner.</action>
  <acceptance_criteria>
    - Protected responses are never publicly cacheable and do not reuse identity/role/environment data.
    - Public DTOs never gain operator fields; denied errors expose no title/count/entity existence.
    - Concurrent mixed-role/environment requests return only their allowlisted projections.
  </acceptance_criteria>
  <verify><automated>node web/scripts/run-phase2-threat.mjs --plan 02-06 --task 2</automated><expected>Exit 0; T-02-16..18 pass.</expected></verify>
  <done>Origin access, projections, errors, and caches are policy-separated and non-leaking.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Owning task | Automated command | Mitigation |
|---|---|---|---|---|---|---|
| T-02-16 | Spoofing | direct origin | critical | Task 1 | cd cloudflare &amp;&amp; npm test -- ops-router --grep direct | Dedicated route plus verified origin proof and environment binding. |
| T-02-17 | Information Disclosure | DTO/error/RSC | critical | Task 2 | cd cloudflare &amp;&amp; npm test -- cache-isolation --grep dto | Minimal typed projection and uniform denial. |
| T-02-18 | Information Disclosure | protected cache | critical | Task 2 | cd cloudflare &amp;&amp; npm test -- cache-isolation --grep concurrent | no-store, bypass, and mixed-context probes. |
</threat_model>
<verification>Run edge routing, public non-regression, projection, and concurrent cache probes.</verification>
<success_criteria>Only verified authorized requests cross the operator edge, with zero public or cross-context leakage.</success_criteria>
<output>Create 02-06-SUMMARY.md when complete.</output>
