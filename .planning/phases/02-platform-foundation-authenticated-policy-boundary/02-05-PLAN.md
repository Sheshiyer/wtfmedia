---
phase: 02-platform-foundation-authenticated-policy-boundary
plan: "05"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - cloudflare/src/audit.ts
  - cloudflare/src/scheduled.ts
  - cloudflare/tests/audit-ledger.test.ts
  - cloudflare/tests/audit-export.test.ts
  - web/tests/security/phase2-threat-results/02-05.json
autonomous: true
requirements: [AUTH-10, QUAL-09, QUAL-10, QUAL-12]
decisions: [D-14, D-15, D-16, D-17]
must_haves:
  truths:
    - "Audit events are server-encoded from closed schemas and cannot be updated through application code."
    - "Export and retention purge are authorized, audited, privacy-safe, and environment-specific."
  artifacts:
    - path: "cloudflare/src/audit.ts"
      provides: "Typed append-only event, query, and safe export boundary"
    - path: "cloudflare/src/scheduled.ts"
      provides: "Atomic environment-specific retention purge"
  key_links:
    - from: "cloudflare/src/scheduled.ts"
      to: "audit_events"
      via: "audited D1 batch with explicit cutoff"
---

<objective>
Implement the append-only, allowlisted audit ledger plus audited export and retention lifecycle.
</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@cloudflare/src/db.ts @cloudflare/migrations/0001_ops_foundation.sql @02-CONTEXT.md @02-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Encode, append, query, and export bounded audit events</name>
  <files>cloudflare/src/audit.ts, cloudflare/tests/audit-ledger.test.ts, cloudflare/tests/audit-export.test.ts</files>
  <read_first>D-14..D-17; typed audit schema migration; RESEARCH Pattern 4 and Pitfall 5/8; operator policy contract</read_first>
  <action>Implement action-specific server encoders for authentication outcome, expiry/logout, protected search/view/export, operator/role change, settings-policy change, purge, and super_admin handoff. Persist only event ID/time, actor or pseudonymous subject, effective role, action, entity type/allowlisted ID, outcome, environment, correlation ID, schema version, and bounded scalar metadata keys. Provide admin/super_admin query and CSV export with closed filters, formula-injection neutralization, no-store/download headers, and a separate audit event for each export. Do not expose create/update/delete audit CRUD to browsers.</action>
  <acceptance_criteria>
    - Every required action produces the complete envelope and rejects extra/prohibited keys or oversized scalars.
    - Editor/anonymous cannot infer ledger existence, counts, filters, or exports.
    - CSV uses fixed columns, neutralizes = + - @ starts, and the export action is itself appended.
  </acceptance_criteria>
  <verify><automated>cd cloudflare &amp;&amp; npm test -- audit-ledger audit-export</automated><expected>Exit 0; required events, prohibited payloads, visibility, and CSV defenses pass.</expected></verify>
  <done>Audit write/query/export behavior is typed, append-only, and non-leaking.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement audited atomic retention purge</name>
  <files>cloudflare/src/scheduled.ts, cloudflare/tests/audit-ledger.test.ts, web/tests/security/phase2-threat-results/02-05.json</files>
  <read_first>Task 1 audit encoder; environment contract; Worker scheduled handler docs; D-16/D-17; RESEARCH scheduled pattern</read_first>
  <action>Implement a UTC scheduled handler selecting local ephemeral, staging 30-day, or production 365-day cutoff from a closed server environment. In one D1 batch append a bounded purge receipt and delete only expired rows; do not archive, expose a browser trigger, or accept a client cutoff. Add deterministic local scheduled-time tests, rollback-on-failure tests, and run the threat runner.</action>
  <acceptance_criteria>
    - Boundary timestamps retain/delete the correct rows for all three environments.
    - Purge failure leaves both audit receipt and deletions rolled back.
    - No archive table/object, client cutoff, generic delete, or 90-day constant remains.
  </acceptance_criteria>
  <verify><automated>node web/scripts/run-phase2-threat.mjs --plan 02-05 --task 2</automated><expected>Exit 0; T-02-13..15 are persistently passed.</expected></verify>
  <done>Retention is deterministic, environment-bound, audited, and non-archiving.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Owning task | Automated command | Mitigation |
|---|---|---|---|---|---|---|
| T-02-13 | Information Disclosure | audit metadata | critical | Task 1 | cd cloudflare &amp;&amp; npm test -- audit-ledger --grep prohibited | Action-specific allowlists reject sensitive payloads. |
| T-02-14 | Injection | CSV export | high | Task 1 | cd cloudflare &amp;&amp; npm test -- audit-export --grep formula | Fixed schema and formula-prefix neutralization. |
| T-02-15 | Tampering | retention purge | high | Task 2 | cd cloudflare &amp;&amp; npm test -- audit-ledger --grep purge | Server cutoff plus atomic receipt/delete batch. |
</threat_model>
<verification>Run event, visibility, export, purge, and rollback tests.</verification>
<success_criteria>Audit evidence is attributable without becoming a privacy, mutation, or spreadsheet execution channel.</success_criteria>
<output>Create 02-05-SUMMARY.md when complete.</output>
