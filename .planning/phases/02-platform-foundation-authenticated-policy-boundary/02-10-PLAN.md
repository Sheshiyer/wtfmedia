---
phase: 02-platform-foundation-authenticated-policy-boundary
plan: "10"
type: execute
wave: 6
depends_on: ["02-07"]
files_modified:
  - web/app/ops/audit/page.tsx
  - web/app/api/ops/audit/route.ts
  - web/components/domain/ops/AuditLedger.tsx
  - web/components/domain/ops/AuditExportDialog.tsx
  - web/stories/AuditLedger.stories.tsx
  - web/tests/phase2/audit-ui.spec.ts
  - web/tests/security/phase2-threat-results/02-10.json
autonomous: true
requirements: [AUTH-04, AUTH-10, QUAL-07, QUAL-13]
decisions: [D-14, D-15, D-16, D-17]
must_haves:
  truths:
    - "Only super_admin and admin can discover, filter, view, or export the audit ledger."
    - "The UI renders only allowlisted envelope fields and distinguishes measured zero from unavailable."
  artifacts:
    - path: "web/components/domain/ops/AuditLedger.tsx"
      provides: "Responsive bounded audit review surface"
    - path: "web/components/domain/ops/AuditExportDialog.tsx"
      provides: "Audited filtered export confirmation and safe result"
  key_links:
    - from: "web/app/api/ops/audit/route.ts"
      to: "cloudflare/src/audit.ts"
      via: "closed filters and authorized no-store query/export"
---

<objective>
Implement the approved Audit review/export screen without exposing prohibited payloads, formulas, or denied ledger existence.
</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@02-UI-SPEC.md @cloudflare/src/audit.ts @web/lib/ops/context.ts @web/components/domain/ops/OperatorShell.tsx</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build authorized audit query, filters, and responsive ledger</name>
  <files>web/app/ops/audit/page.tsx, web/app/api/ops/audit/route.ts, web/components/domain/ops/AuditLedger.tsx, web/stories/AuditLedger.stories.tsx, web/tests/phase2/audit-ui.spec.ts</files>
  <read_first>UI-SPEC Audit access/fields/filters/states; audit DTO/query contract; role policy; semantic table/mobile record patterns</read_first>
  <action>Authorize page/RSC/API/query independently. Render timestamp, approved subject label, effective role, action, entity type/allowlisted ID, outcome, environment, and correlation ID only. Provide closed time/action/outcome/role/environment controls, not a raw query language. Use semantic table at desktop and labelled records at 320px. Show measured empty only after successful zero query; show unavailable with retry on failure; denied roles receive the shared recovery boundary with no audit-specific chrome.</action>
  <acceptance_criteria>
    - DOM/RSC/API never contains tokens, raw queries, prompts, responses, arbitrary metadata, secrets, provider bodies, or private payloads.
    - Editor/anonymous have no nav, prefetch, aggregate count, filter, export affordance, or audit-specific denial.
    - Filtered zero and unavailable states are behaviorally and textually distinct at all widths.
  </acceptance_criteria>
  <verify><automated>cd web &amp;&amp; npm run test:browser -- tests/phase2/audit-ui.spec.ts --grep ledger</automated><expected>Exit 0; authorization, field allowlist, filters, states, and responsive semantics pass.</expected></verify>
  <done>Audit review is policy-bound and cannot disclose prohibited or denied data.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement filtered export confirmation and safe download feedback</name>
  <files>web/components/domain/ops/AuditExportDialog.tsx, web/app/api/ops/audit/route.ts, web/tests/phase2/audit-ui.spec.ts, web/tests/security/phase2-threat-results/02-10.json</files>
  <read_first>UI-SPEC export contract; audit CSV service/tests; current filters; dialog focus patterns; server export audit event</read_first>
  <action>Add the secondary export audit records action. Confirmation names only active filters and states that export is audited; it never previews rows. Reauthorize server-side, request fixed allowlisted CSV, use no-store attachment response, and confirm only that an allowlisted export was prepared. Focus safe cancel first, restore focus, announce once, and run the threat runner.</action>
  <acceptance_criteria>
    - Export bypass/direct calls deny without admin/super_admin authorization.
    - Confirmation/success DOM contains no row contents; CSV formula defenses remain intact.
    - Exactly one export audit event captures actor, filters summary, outcome, environment, and correlation ID.
  </acceptance_criteria>
  <verify><automated>node web/scripts/run-phase2-threat.mjs --plan 02-10 --task 2</automated><expected>Exit 0; T-02-27..29 pass.</expected></verify>
  <done>Audit export is secondary, explicit, safe, authorized, and itself audited.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Owning task | Automated command | Mitigation |
|---|---|---|---|---|---|---|
| T-02-27 | Information Disclosure | denied audit route | critical | Task 1 | cd web &amp;&amp; npm run test:browser -- tests/phase2/audit-ui.spec.ts --grep denied | No denied ledger chrome, count, filters, or payload. |
| T-02-28 | Information Disclosure | export UI/download | critical | Task 2 | cd web &amp;&amp; npm run test:browser -- tests/phase2/audit-ui.spec.ts --grep export | Fixed DTO/CSV, no preview, server reauthorization. |
| T-02-29 | Injection | filter/query boundary | high | Task 1 | cd web &amp;&amp; npm run test:unit -- audit-filters | Closed parsed filters and bound D1 statements. |
</threat_model>
<verification>Run authorization, closed-filter, field/privacy, CSV, dialog, axe, and responsive probes.</verification>
<success_criteria>Authorized reviewers can inspect/export evidence while prohibited fields and denied ledger existence remain sealed.</success_criteria>
<output>Create 02-10-SUMMARY.md when complete.</output>
