---
phase: 02-platform-foundation-authenticated-policy-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-platform-foundation-authenticated-policy-boundary/02-VALIDATION.md
  - web/scripts/run-phase2-threat.mjs
  - web/scripts/lib/phase2-threat-results.mjs
  - web/tests/security/phase2-threat-results.json
  - web/tests/security/phase2-threat-results/.gitkeep
  - web/package.json
autonomous: true
requirements: [QUAL-07, QUAL-09, QUAL-10, QUAL-12, QUAL-13]
decisions: [D-23, D-24, D-25, D-26]
must_haves:
  truths:
    - "Every Phase 2 security claim resolves to an immutable plan definition and a privacy-safe persistent result."
    - "Unknown, missing, drifted, or failed high-severity evidence blocks aggregation."
  artifacts:
    - path: "web/scripts/run-phase2-threat.mjs"
      provides: "Exact-command per-plan threat executor"
    - path: ".planning/phases/02-platform-foundation-authenticated-policy-boundary/02-VALIDATION.md"
      provides: "Immutable threat ownership and command ledger"
  key_links:
    - from: "web/scripts/run-phase2-threat.mjs"
      to: "02-*-PLAN.md"
      via: "parsed threat definitions and command digests"
---

<objective>
Create the Phase 2 evidence harness before security-sensitive implementation begins.

Purpose: Make proof persistent, privacy-safe, command-bound, and fail-closed from the first executable plan.
Output: Validation ledger, exact-command runner, fragment validator, empty aggregate, and package scripts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-foundation-authenticated-policy-boundary/02-CONTEXT.md
@.planning/phases/02-platform-foundation-authenticated-policy-boundary/02-RESEARCH.md
@web/scripts/run-phase1-threat.mjs
@web/scripts/lib/phase1-threat-results.mjs
@web/tests/security/phase1-threat-results.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the immutable Phase 2 threat and validation ledger</name>
  <files>.planning/phases/02-platform-foundation-authenticated-policy-boundary/02-VALIDATION.md, web/tests/security/phase2-threat-results.json, web/tests/security/phase2-threat-results/.gitkeep</files>
  <read_first>02-CONTEXT.md; 02-RESEARCH.md Validation Architecture and Blocking Threat Classes; all 02-*-PLAN.md threat_model blocks; Phase 1 validation and aggregate schemas</read_first>
  <action>Create a Phase 2 ledger that maps every T-02 threat ID to exactly one plan/task/command, records the 13 aggregate sections from RESEARCH.md, and declares high/critical failure plus unknown/missing/drift as blocking. Initialize an empty aggregate schema with no mutable pass claims. Keep account IDs, hostnames, tokens, environment values, raw output, and machine-local paths outside artifacts.</action>
  <acceptance_criteria>
    - Every threat ID is unique and owned once; every plan with a threat_model is represented.
    - Aggregate sections include source coverage, build, D1, Access/D1 matrix, policy/DTO, lifecycle, audit, environment/cache, UI, rollback/runbook, privacy, staging approval, and production smoke.
    - The initial aggregate contains definitions and empty results only; no fabricated pass status exists.
  </acceptance_criteria>
  <verify><automated>node web/scripts/verify-phase1.mjs --help >/dev/null 2>&amp;1 || true; node -e "const j=require('./web/tests/security/phase2-threat-results.json'); if(!j.schema_version||Object.keys(j.results||{}).length) process.exit(1)"</automated><expected>Exit 0; Phase 2 aggregate is schema-initialized with zero result claims.</expected></verify>
  <done>The immutable ownership ledger and empty aggregate exist without sensitive runtime material.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact-command fragment execution and validation</name>
  <files>web/scripts/run-phase2-threat.mjs, web/scripts/lib/phase2-threat-results.mjs, web/package.json</files>
  <read_first>Phase 1 threat runner and validator; 02-VALIDATION.md from Task 1; package scripts; privacy scanner contract</read_first>
  <action>Adapt the proven Phase 1 runner into a Phase 2 namespace. Parse commands from the owning plan, hash-bind plan/task/threat/command, execute each unique command once, write one same-directory atomic fragment, and reject owner drift, schema drift, future timestamps, raw stdout/stderr, prompts, responses, queries, tokens, secrets, env values, account IDs, hostnames not allowlisted by a later receipt, and absolute local paths. Add test:phase2:threat and merge:phase2:threat package scripts without changing Phase 1 behavior.</action>
  <acceptance_criteria>
    - Synthetic pass and failure fixtures persist bounded digests and exact exit status while returning the original command status.
    - Unknown IDs, altered commands, duplicate ownership, raw output fields, and prohibited strings fail nonzero before aggregate mutation.
    - Phase 1 runner and aggregate remain byte-compatible and passing.
  </acceptance_criteria>
  <verify><automated>cd web &amp;&amp; npm run test:unit -- phase2-threat-results &amp;&amp; npm run test:privacy -- --check</automated><expected>Exit 0; result fragments are atomic, command-bound, and privacy-safe.</expected></verify>
  <done>Every later plan can produce durable evidence without trusting runner exit status alone.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Owning task | Automated command | Mitigation |
|---|---|---|---|---|---|---|
| T-02-01 | Tampering | plan/result binding | high | Task 2 | cd web &amp;&amp; npm run test:unit -- phase2-threat-results | Hash-bind immutable plan/task/command ownership. |
| T-02-02 | Information Disclosure | evidence artifacts | high | Task 2 | cd web &amp;&amp; npm run test:privacy -- --check | Strict allowlist and bounded digests; reject raw sensitive material. |
| T-02-03 | Repudiation | aggregate provenance | high | Task 1 | node web/scripts/lib/phase2-threat-results.mjs --check-definitions | Require unique ownership and persistent timestamps/digests. |
</threat_model>

<verification>Run focused harness tests, privacy scan, and Phase 1 non-regression.</verification>
<success_criteria>Phase 2 cannot claim a security result without persistent, immutable, privacy-safe evidence.</success_criteria>
<output>Create 02-01-SUMMARY.md when complete.</output>
