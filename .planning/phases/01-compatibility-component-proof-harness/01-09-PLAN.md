---
phase: 01-compatibility-component-proof-harness
plan: "09"
type: execute
wave: 10
depends_on: ["01-08"]
files_modified:
  - web/components/ui/Button.tsx
  - web/components/ui/IconButton.tsx
  - web/components/ui/LinkButton.tsx
  - web/components/ui/AvailabilityState.tsx
  - web/components/ui/LiveRegion.tsx
  - web/components/ui/SkipLink.tsx
  - web/components/ui/Drawer.tsx
  - web/stories/Phase1Primitives.stories.tsx
  - web/tests/security/phase1-threat-results/01-09.json
autonomous: true
requirements: [DSYS-04, DSYS-05, DSYS-06, DSYS-07, DSYS-10, QUAL-03]
must_haves:
  truths:
    - "Only primitives consumed by protected Phase 1 routes ship, with native semantics, 44px targets, visible focus, and complete applicable states."
    - "The Drawer uses Radix behavior under WTF styling and provides labelled modal focus containment, contained scroll, Escape/close, and deterministic focus restoration."
    - "Every required availability state has visible non-color text, safe remaining behavior, and a recovery path."
  artifacts:
    - path: "web/components/ui/Drawer.tsx"
      provides: "Repository-owned accessible dialog/drawer wrapper"
    - path: "web/stories/Phase1Primitives.stories.tsx"
      provides: "Deterministic interaction/state evidence for consumed primitives"
  key_links:
    - from: "web/components/ui/Drawer.tsx"
      to: "@radix-ui/react-dialog"
      via: "behavior-only primitive with repository-owned tokens/styles"
---

<objective>
Create the exact accessible primitives consumed by Episodes and later protected public routes.

Purpose: Replace bespoke interaction risks without building a speculative component library or adopting generic kit styling.
Output: Actions, state/live semantics, skip link, Radix-backed Drawer, and deterministic story proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@docs/design/COMPONENT-INVENTORY.md
@DESIGN.md
@web/styles/tokens.css
@web/styles/motion.css
@web/components/EpisodesBrowser.tsx
</context>

<must_haves>
  <truths>Keyboard, focus, motion, naming, and state language are behavior contracts, not visual polish.</truths>
  <artifacts>All primitives use named exports, forward refs where focus restoration requires them, and expose no domain/private data.</artifacts>
  <key_links>Story interaction tests prove the same components later consumed by protected routes; no duplicate test-only implementations.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-09 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-09.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-09, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement semantic action and navigation primitives</name>
  <files>web/components/ui/Button.tsx, web/components/ui/IconButton.tsx, web/components/ui/LinkButton.tsx, web/components/ui/SkipLink.tsx, web/stories/Phase1Primitives.stories.tsx, web/tests/security/phase1-threat-results/01-09.json</files>
  <read_first>web/components/ui/Button.tsx, web/components/ui/IconButton.tsx, web/components/ui/LinkButton.tsx, and web/components/ui/SkipLink.tsx if present (otherwise treat absent paths as create targets); 01-UI-SPEC.md Component Dependency Rule, Shared Public Shell Contract, and Keyboard/Focus; DESIGN.md UI primitives; web/app/chat/page.tsx control analogs; web/app/layout.tsx navigation analog</read_first>
  <behavior>
    - Buttons use native button semantics, support disabled/loading/pressed/focus-visible states, and retain 44px targets.
    - IconButton requires an accessible name; LinkButton remains a semantic Next/link or anchor preserving browser behavior.
    - SkipLink is first-focusable and targets the public main region.
  </behavior>
  <action>Create repository-owned action wrappers with semantic token variants, native behavior, ref forwarding, explicit IconButton naming, and safe external link handling. In the same task create focused Phase1Primitives story interactions for Enter/Space, loading/disabled/pressed, accessible names, external rel/protocol rejection, skip-link first focus, and visible focus; later tasks extend the same story file. Do not add destructive UI or generic kit styling. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-09 --task 1` so T-01-28 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: no clickable generic container or pointer-only handler is used.
    - Behavior: Enter/Space/native link behavior, disabled/loading state, accessible names, and focus-visible styling are testable.
    - Security: external LinkButton enforces safe rel behavior and does not accept unsafe protocol fixtures.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-09 --task 1</automated>
    <expected>Exit 0; primitives compile and lint with native semantic element contracts.</expected>
  </verify>
  <done>Phase 1 actions and navigation have consistent WTF appearance without losing native semantics. Result fragment: T-01-28 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement complete availability and polite live-state primitives</name>
  <files>web/components/ui/AvailabilityState.tsx, web/components/ui/LiveRegion.tsx, web/stories/Phase1Primitives.stories.tsx, web/tests/security/phase1-threat-results/01-09.json</files>
  <read_first>web/components/ui/AvailabilityState.tsx and web/components/ui/LiveRegion.tsx if present (otherwise treat them as create targets); 01-UI-SPEC.md Availability State Language and Async Focus; docs/design/APP-FLOW.md Non-Negotiable Flow States; docs/design/COMPONENT-INVENTORY.md Required Component States</read_first>
  <behavior>
    - unknown, unavailable, stale, partial, empty, permission-denied, error, offline, unmapped, conflicted, and measured-zero render distinct visible text and non-color semantics.
    - Public permission-denied copy never confirms a protected record exists.
    - LiveRegion announces bounded status changes politely and never streams every answer token.
  </behavior>
  <action>Define the closed availability union and presentation contract with label, explanation, remaining-safe behavior, and recovery. Render semantic status/alert markup with non-color distinctions and implement a stable polite LiveRegion. Extend Phase1Primitives stories in this task with every state, CSS-color-disabled meaning, non-enumerating permission denial, bounded polite announcements, and a negative per-token-announcement case. Do not fabricate zero, owner, provider detail, health, or protected existence. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-09 --task 2` so T-01-27 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: all eleven state identifiers exist exactly once in the type and story fixture matrix.
    - Behavior: state meaning remains distinguishable when CSS colors are disabled.
    - Accessibility: live region is stable and does not receive per-token updates.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-09 --task 2</automated>
    <expected>Exit 0; the closed state vocabulary and polite live-region contracts compile without unsafe fallbacks.</expected>
  </verify>
  <done>Every Phase 1 state can communicate truth and recovery without color-only or fabricated meaning. Result fragment: T-01-27 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement and prove the Radix-backed WTF Drawer</name>
  <files>web/components/ui/Drawer.tsx, web/stories/Phase1Primitives.stories.tsx, web/tests/security/phase1-threat-results/01-09.json</files>
  <read_first>web/components/ui/Drawer.tsx and web/stories/Phase1Primitives.stories.tsx if present (otherwise treat them as create targets); 01-UI-SPEC.md URL-Backed EpisodeDrawer, Drawer Responsive Layout, and Required State Matrix; 01-RESEARCH.md Don't Hand-Roll and URL State pattern; current web/components/EpisodesBrowser.tsx bespoke overlay; Radix Dialog installed package types</read_first>
  <behavior>
    - Open Drawer is labelled, modal, focus-contained, background-inert, scroll-contained, Escape/backdrop/visible-close dismissible, and restores supplied fallback focus.
    - 320px uses full viewport; 768px/1440px use bounded right-edge sizes; reduced motion uses a static/short fade.
    - Stories prove buttons, state vocabulary, skip link, live region, and Drawer default/focus/disabled/loading/error/reduced-motion states with serious/critical axe blocking.
  </behavior>
  <action>Wrap `@radix-ui/react-dialog` as a controlled repository-owned `Drawer` with explicit title/description contracts, visible close label, content-scroll containment, outside-scroll prevention, focus hooks, and semantic token styling. Expose a caller-supplied focus-return/fallback strategy needed by direct episode deep links. Build one consolidated story suite for all primitives and interaction states; use real keyboard tab/Escape assertions and axe error mode. Do not copy Radix visual defaults or add another overlay package. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-09 --task 3` so T-01-26 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Behavior: keyboard-only story opens, cycles focus, Escape closes, and returns focus to the trigger/fallback.
    - Accessibility: labelled modal semantics and serious/critical axe checks pass.
    - Motion: reduced-motion story has no long transform animation.
    - Traceability: every primitive story metadata names at least one Phase 1 route consumer.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-09 --task 3</automated>
    <expected>Exit 0; all consumed primitive states and Drawer keyboard/focus/a11y behavior pass in Chromium.</expected>
  </verify>
  <done>The Episodes slice can consume proven accessible behavior without hand-rolled modal or speculative kit surface. Result fragment: T-01-26 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Keyboard/focus access, truthful state semantics, safe external navigation, and non-leaking public errors.

## Trust Boundaries
| Boundary | Description |
|---|---|
| caller props → semantic primitives | Unsafe URLs, missing names, or private state text could reach public DOM. |
| modal layer → document focus/scroll | Incorrect containment can deny page access or strand focus. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-09.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-26 | Denial of Service | Drawer/actions | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 3 | cd web &amp;&amp; npm run test:components -- Phase1Primitives &amp;&amp; npm run typecheck | defined | Keyboard, focus trap/return, visible close, 44px targets, reduced motion, scroll containment, and browser interaction tests. |
| T-01-27 | Information Disclosure | AvailabilityState | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 2 | cd web &amp;&amp; npm run test:components -- Phase1Primitives --grep "availability\|live region" &amp;&amp; npm run typecheck &amp;&amp; npm run lint | defined | Closed public-safe copy contract; permission-denied never reveals entity existence; story/privacy assertions. |
| T-01-28 | Tampering | LinkButton/source opening | mitigate | high | medium | high | v5.0.0-1.2.2 | Task 1 | cd web &amp;&amp; npm run test:components -- Phase1Primitives --grep "actions\|links\|skip\|focus" &amp;&amp; npm run typecheck &amp;&amp; npm run lint | defined | Preserve semantic link behavior, noreferrer, and approved protocol validation. |
</threat_model>

<verification>
Run type/lint plus consolidated Chromium story interactions and serious/critical a11y checks.
</verification>

<success_criteria>
Every primitive required by the Episodes proof slice is consumed, semantic, keyboard/focus safe, state-complete, and visually repository-owned.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-09-SUMMARY.md` when done.
</output>
