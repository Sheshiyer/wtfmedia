---
phase: 01-compatibility-component-proof-harness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/scripts/capture-phase1-baseline.mjs
  - web/scripts/run-phase1-threat.mjs
  - web/tests/contracts/phase1-compatibility-manifest.json
  - web/tests/contracts/phase1-baseline-approval.json
  - web/tests/security/phase1-threat-results/01-01.json
  - .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, QUAL-02]
must_haves:
  truths:
    - "The authoritative compatibility baseline is the owner-approved current dirty worktree, not HEAD and not a silently cleaned tree."
    - "Protected URLs, query meanings, navigation labels, public data semantics, and every current /api/chat branch are enumerated before migration."
    - "The accepted streaming/chunk semantics are explicitly recorded rather than inferred from final response text."
  artifacts:
    - path: "web/tests/contracts/phase1-compatibility-manifest.json"
      provides: "Repository-relative executable inventory of protected public contracts"
    - path: "web/tests/contracts/phase1-baseline-approval.json"
      provides: "Owner approval reference for the canonical dirty-worktree baseline"
    - path: "web/tests/security/phase1-threat-results/01-01.json"
      provides: "Mutable privacy-safe execution evidence keyed by unique threat ID"
  key_links:
    - from: "web/scripts/capture-phase1-baseline.mjs"
      to: "web/tests/contracts/phase1-compatibility-manifest.json"
      via: "bounded source/hash capture with no raw private payloads"
    - from: "web/tests/contracts/phase1-baseline-approval.json"
      to: "all later compatibility tests"
      via: "approved baseline identifier and protected-file hashes"
---

<objective>
Reconcile and owner-lock the compatibility source before packages or application files change.

Purpose: Prevent Phase 1 from freezing stale HEAD behavior or absorbing unrelated user changes while satisfying D-09 and the dirty-worktree gate.
Output: A privacy-safe compatibility manifest, a bounded capture command, an approval receipt, and initial validation mappings.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-compatibility-component-proof-harness/01-CONTEXT.md
@.planning/phases/01-compatibility-component-proof-harness/01-RESEARCH.md
@.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@.project/HANDOFF.md
@web/app/api/chat/route.ts
@web/app/chat/page.tsx
@web/app/layout.tsx
@web/app/page.tsx
@web/app/episodes/page.tsx
@web/app/connections/page.tsx
</context>

<must_haves>
  <truths>D-09 is frozen against an explicitly approved current source; no revert, reset, clean, stage, commit, or package operation occurs.</truths>
  <artifacts>The manifest names the base SHA, relevant dirty paths and content hashes, protected routes, supported URL/query examples, navigation destinations, public field allowlists, and /api/chat request/response branches without embedding raw diffs.</artifacts>
  <key_links>Later contract, browser, performance, and rollback plans must reject a manifest whose approval receipt or protected hashes no longer match.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-01 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-01.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-01, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto">
  <name>Task 1: Capture the bounded compatibility source without disturbing user work</name>
  <files>web/scripts/capture-phase1-baseline.mjs, web/scripts/run-phase1-threat.mjs, web/tests/contracts/phase1-compatibility-manifest.json, web/tests/security/phase1-threat-results/01-01.json</files>
  <read_first>web/scripts/capture-phase1-baseline.mjs, web/scripts/run-phase1-threat.mjs, web/tests/contracts/phase1-compatibility-manifest.json, and web/tests/security/phase1-threat-results/01-01.json if present (otherwise treat them as create targets); AGENTS.md; .project/HANDOFF.md; 01-CONTEXT.md D-09, D-12, D-16; 01-RESEARCH.md sections Current Repository Evidence, Runtime State Inventory, and Pitfall 1; web/app/layout.tsx; web/app/page.tsx; web/app/episodes/page.tsx; web/app/connections/page.tsx; web/app/chat/page.tsx; web/app/api/chat/route.ts; current `git status --short` and path-scoped `git diff --name-status` output</read_first>
  <action>Before creating or changing any Task 1-owned artifact, capture the authoritative pre-task snapshot under the worktree-specific path returned by `git rev-parse --git-path phase1-snapshots/01-01-task1`; this transient Git-metadata location is structurally outside the worktree, never appears in `git status`, is not a plan artifact, and must be removed on every success or failure exit after comparison. Record `git status --porcelain=v1 -z --untracked-files=all` byte-for-byte plus, for every pre-existing dirty entry, its exact XY status code, repository-relative path bytes, file type/mode, worktree content SHA-256 or absence marker, and index blob identity when staged. Fail before mutation if any of the four permitted Task 1 paths already exists, so every pre-existing dirty path remains outside the permitted delta. The only permitted Task 1 paths are `web/scripts/capture-phase1-baseline.mjs`, `web/scripts/run-phase1-threat.mjs`, `web/tests/contracts/phase1-compatibility-manifest.json`, and `web/tests/security/phase1-threat-results/01-01.json`; snapshot files are never part of this set because they live outside the worktree and are deleted after the post-task comparison.

Then create `run-phase1-threat.mjs` and an empty schema-valid `web/tests/security/phase1-threat-results/01-01.json`; do not create the aggregate ledger. The runner must parse immutable threat definitions from the named PLAN, require globally unique threat IDs and matching plan/task ownership, decode the exact HTML-escaped command, derive a stable command identifier from plan/task/threat/command, execute each unique command once, and atomically update only `web/tests/security/phase1-threat-results/{plan}.json` through a same-directory temporary file plus rename. The top-level fragment contains exactly `schema_version`, `plan`, and `results`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. Reject a threat not owned by the requested plan, a fragment path/plan mismatch, duplicate keys, schema/owner/task/command drift, a future timestamp, a prohibited payload field, credential or secret material, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. Record failures before returning the same nonzero exit. No checkout-local inter-process lock or aggregate read-modify-write is permitted because parallel worktrees never share a mutable result file. Then create `capture-phase1-baseline.mjs` as a read-only Node command that resolves the repository-relative base SHA, records only the protected/relevant dirty path inventory and SHA-256 content hashes, and emits the manifest deterministically. Populate route paths, navigation destinations, supported `/chat?q=` behavior, current episode/query selection behavior, public field allowlists, and the complete source-observed `/api/chat` validation/header/status/error/source matrix per D-09. Record the current buffered Worker-response-to-text-body behavior and browser `ReadableStream` consumption as separate facts; do not claim token cadence that the source does not provide. Exclude raw diffs, environment values, source bodies, private links, absolute paths, and unrelated dirty-file contents. The command must fail if it detects a destructive git command or attempts to write outside the declared artifacts.

After all four enumerated Task 1 paths exist, invoke the threat runner for Plan 01-01 Task 1 so T-01-01 executes the capture check and is recorded. As part of that exact T-01-01 command, `capture-phase1-baseline.mjs --check` must load the pre-task snapshot, capture the post-task status with the same NUL-delimited command and byte-evidence schema, and perform the comparison before returning. Require the post-task record set, after removing exactly the four enumerated Task 1 records, to equal the complete pre-task record set byte-for-byte; require every pre-existing dirty entry's XY code, path bytes, mode/type, worktree hash/absence marker, and staged blob identity to remain exact; and reject any added, removed, renamed, restaged, or content-changed path outside the four-item allowlist. Persist only bounded comparison digests in threat evidence, never snapshot content, then delete the transient snapshot directory before the check returns and Task 1 completes.</action>
  <acceptance_criteria>
    - Source: the pre-task snapshot is captured before any Task 1 artifact exists; after all Task 1 artifacts exist, removing exactly `web/scripts/capture-phase1-baseline.mjs`, `web/scripts/run-phase1-threat.mjs`, `web/tests/contracts/phase1-compatibility-manifest.json`, and `web/tests/security/phase1-threat-results/01-01.json` from the post-task record set leaves the byte-identical pre-task `git status --porcelain=v1 -z --untracked-files=all` record set.
    - Preservation: every pre-existing dirty path retains its exact XY status code, repository-relative path bytes, mode/type, worktree content hash or absence marker, and staged blob identity; no path outside the four-item allowlist is added, removed, renamed, restaged, or content-changed.
    - Snapshot rule: before/after snapshot files exist only under `git rev-parse --git-path phase1-snapshots/01-01-task1`, never enter worktree status or plan ownership, contribute only bounded digests to threat evidence, and are deleted after comparison.
    - Behavior: the manifest enumerates `/`, `/episodes`, `/connections`, `/chat`, and `/api/chat`, plus `X-Sources`, `X-Model`, `X-Fallback`, `Content-Type`, `Cache-Control`, safe 400/503 branches, last-eight handling, accepted question limit, and source keys.
    - Privacy: no field contains an absolute checkout path, secret value/name assignment, raw prompt/response body, private source payload, or raw diff hunk.
    - Command: two consecutive captures produce identical JSON after excluding an explicit `captured_at` field, or omit wall-clock fields entirely.
    - Evidence: `phase1-threat-results/01-01.json` has top-level plan `01-01`; `results["T-01-01"]` is `passed`, binds Task 1 and the exact immutable command/command identifier, and contains only allowed schema fields, digests, and bounded non-sensitive evidence.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-01 --task 1</automated>
    <expected>Exit 0; the exact four-path Task 1 delta is the only worktree change since the pre-task snapshot, every pre-existing dirty record and byte hash is preserved, transient snapshot files are removed, all five protected paths and the full named chat header surface are present, and T-01-01 has one passing privacy-safe result entry.</expected>
  </verify>
  <done>A deterministic, bounded manifest describes the current compatibility source; the exact before/after comparison proves that only the four enumerated Task 1 artifacts changed and every pre-existing dirty record remained byte/status exact, with transient snapshots removed and no revert, stage, commit, or sensitive copy. Result fragment: T-01-01 is recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto">
  <name>Task 2: Map baseline, decision, and threat identifiers into the draft validation contract</name>
  <files>.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md, web/tests/security/phase1-threat-results/01-01.json</files>
  <read_first>.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md; web/tests/contracts/phase1-compatibility-manifest.json; 01-CONTEXT.md D-09, D-12, D-16; this plan's threat model</read_first>
  <action>Replace the baseline-related TBD rows with Plan `01-01`, Wave 1, concrete task identifiers, and the threat IDs in this plan. Add the manifest and approval receipt to Wave 0 requirements. Keep frontmatter `status: draft`, `wave_0_complete: false`, and `nyquist_compliant: false`; do not mark tests green before their files and commands exist. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-01 --task 2` so T-01-02 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: the COMP-01, COMP-02, COMP-03, and QUAL-02 rows cite Plan `01-01` and applicable threat IDs.
    - Behavior: Wave 0 still shows all not-yet-created harness files as pending.
    - Command: a parser finds exactly one `wave_0_complete: false` and one `nyquist_compliant: false` frontmatter entry.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-01 --task 2</automated>
    <expected>Exit 0; baseline rows are mapped and both completion flags remain false.</expected>
  </verify>
  <done>The draft validation architecture points to real baseline tasks and threats without overstating readiness. Result fragment: T-01-02 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve the authoritative compatibility baseline</name>
  <files>web/tests/contracts/phase1-baseline-approval.json, web/tests/security/phase1-threat-results/01-01.json</files>
  <read_first>web/tests/contracts/phase1-baseline-approval.json if present (otherwise treat it as a create target); web/tests/contracts/phase1-compatibility-manifest.json; web/app/api/chat/route.ts; web/app/chat/page.tsx; .project/HANDOFF.md release notes; 01-RESEARCH.md Open Questions 1 and 3</read_first>
  <action>Present the repository-relative dirty-path/hash inventory, route/query/navigation table, and exact `/api/chat` behavior matrix to the owner. Explicitly call out that the current Next proxy awaits Worker JSON before returning a text response while the client consumes the response body through a reader. Ask the owner to confirm this current behavior, including its first-byte/chunk meaning, as the Phase 1 baseline or describe corrections. On approval only, write `phase1-baseline-approval.json` with manifest hash, base SHA, approved protected-file hashes, streaming interpretation, approval reference, and `status: approved`. Do not stage, clean, revert, commit, or bless unrelated dirty files. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-01 --task 3` so T-01-03, T-01-04 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Human: the owner explicitly approves the named protected behavior and streaming interpretation.
    - Source: the approval receipt references the manifest hash and only repository-relative paths.
    - Behavior: downstream package/bootstrap work remains blocked when `status` is not `approved` or hashes drift.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-01 --task 3</automated>
    <expected>After owner approval, exit 0; otherwise execution remains paused.</expected>
  </verify>
  <done>The owner has selected the exact compatibility source and streaming semantics that all migration tests must preserve. Result fragment: T-01-03, T-01-04 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Accepted public URL/query/navigation semantics and the complete `/api/chat` request/response contract.
- User-owned dirty worktree changes and server-only edge-secret boundary.

## Trust Boundaries
| Boundary | Description |
|---|---|
| dirty worktree → baseline manifest | Unreviewed local changes must not be silently promoted or discarded. |
| server route → public contract artifact | Secret/config values and provider details must not cross into committed evidence. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-01.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-01 | Tampering | baseline capture | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 1 | node web/scripts/capture-phase1-baseline.mjs --check &amp;&amp; node -e 'const m=require("./web/tests/contracts/phase1-compatibility-manifest.json"); const p=["/","/episodes","/connections","/chat","/api/chat"]; if(p.some(x=&gt;!m.protected_routes.includes(x))) process.exit(1); if(!m.chat_contract?.headers?.includes("X-Sources")) process.exit(1)' | defined | ASVS V14 configuration integrity: hash named protected files, record base SHA, require owner approval, and compare pre/post git status. |
| T-01-02 | Information Disclosure | manifest/approval artifacts | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 2 | node -e 'const s=require("fs").readFileSync(".planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md","utf8"); if(!s.includes("01-01")\|\|!s.includes("T-01-01")) process.exit(1); if((s.match(/wave_0_complete: false/g)\|\|[]).length!==1\|\|(s.match(/nyquist_compliant: false/g)\|\|[]).length!==1) process.exit(1)' | defined | ASVS V8 data protection: repository-relative paths only; deny secrets, raw bodies, private links, absolute paths, and diff hunks with scanner assertions. |
| T-01-03 | Repudiation | baseline authority | mitigate | high | low | high | v5.0.0-2.1.1 | Task 3 | node -e 'const a=require("./web/tests/contracts/phase1-baseline-approval.json"); const m=require("./web/tests/contracts/phase1-compatibility-manifest.json"); if(a.status!=="approved"\|\|!a.manifest_sha256\|\|!a.streaming_contract\|\|a.base_sha!==m.base_sha) process.exit(1)' | defined | Approval receipt binds owner reference, manifest hash, protected hashes, and streaming interpretation. |
| T-01-04 | Denial of Service | compatibility freeze | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 3 | node -e 'const a=require("./web/tests/contracts/phase1-baseline-approval.json"); const m=require("./web/tests/contracts/phase1-compatibility-manifest.json"); if(a.status!=="approved"\|\|!a.manifest_sha256\|\|!a.streaming_contract\|\|a.base_sha!==m.base_sha) process.exit(1)' | defined | Enumerate request-size/last-eight/timeouts and safe failures so later tests preserve abuse bounds rather than removing them. |
</threat_model>

<verification>
Run the capture twice, validate JSON shape/privacy, compare git status, and require the owner checkpoint before Plan 01-02 may begin.
</verification>

<success_criteria>
The current accepted behavior is explicit, privacy-safe, owner-approved, and mechanically addressable without any destructive repository action.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-01-SUMMARY.md` when done.
</output>
