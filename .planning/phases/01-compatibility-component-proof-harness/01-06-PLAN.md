---
phase: 01-compatibility-component-proof-harness
plan: "06"
type: execute
wave: 6
depends_on: ["01-05"]
files_modified:
  - web/scripts/capture-phase1-performance.mjs
  - web/tests/performance/phase1-baseline.json
  - web/tests/performance/phase1-budget-proposal.json
  - web/tests/contracts/rag-latency.test.ts
  - .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
  - web/tests/security/phase1-threat-results/01-06.json
autonomous: true
requirements: [QUAL-06, COMP-03]
must_haves:
  truths:
    - "Current route, bundle, interaction, and controlled RAG behavior are measured from the owner-approved baseline before visual migration."
    - "Every proposed numeric budget is derived from recorded route-specific evidence and remains unapproved until the next owner checkpoint."
  artifacts:
    - path: "web/tests/performance/phase1-baseline.json"
      provides: "Versioned five-run legacy route/bundle/interaction/RAG measurements with environment identity"
    - path: "web/tests/performance/phase1-budget-proposal.json"
      provides: "Evidence-linked route-specific candidate budgets"
  key_links:
    - from: "web/tests/contracts/phase1-baseline-approval.json"
      to: "web/tests/performance/phase1-baseline.json"
      via: "manifest hash and protected-file hash preflight"
---

<objective>
Measure the accepted current application and derive evidence-backed candidate budgets before any visual migration.

Purpose: Fulfill D-15 and prevent generic performance numbers from becoming false gates.
Output: Reproducible measurement command, versioned baseline, candidate budget proposal, controlled RAG timing contract, and validation mapping.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/tests/contracts/phase1-baseline-approval.json
@web/playwright.config.ts
@web/lighthouserc.cjs
@web/tests/support/rag-stub.mjs
@web/package.json
@.planning/phases/01-compatibility-component-proof-harness/01-RESEARCH.md
@.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
</context>

<must_haves>
  <truths>No route/component migration starts until the baseline is fresh and its numeric budget proposal receives owner approval.</truths>
  <artifacts>The baseline records base SHA, compatibility-manifest hash, Node/npm/Chromium/OS identity, five measurements per route, Next build/resource summary, key interactions, and controlled RAG first-byte/total timing.</artifacts>
  <key_links>The capture command rejects stale `.next`, hash drift, live-service URLs, or a missing approved baseline receipt.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-06 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-06.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-06, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto">
  <name>Task 1: Capture five-run legacy route, bundle, and interaction evidence</name>
  <files>web/scripts/capture-phase1-performance.mjs, web/tests/performance/phase1-baseline.json, web/tests/security/phase1-threat-results/01-06.json</files>
  <read_first>web/scripts/capture-phase1-performance.mjs and web/tests/performance/phase1-baseline.json if present (otherwise treat them as create targets); web/tests/contracts/phase1-baseline-approval.json; web/tests/contracts/phase1-compatibility-manifest.json; web/lighthouserc.cjs; web/playwright.config.ts; 01-RESEARCH.md Performance Baseline and Budget Gate plus Pitfall 8; 01-UI-SPEC.md Capture Contract</read_first>
  <action>Per D-15, implement a bounded Node orchestrator that verifies the approved protected hashes, removes only generated `web/.next` output through the build command's normal lifecycle, produces a fresh production build, runs Lighthouse five times for `/`, `/episodes`, `/connections`, and `/chat`, records Next build/resource summaries, and measures the current route-ready plus key interaction timings available before migration. Write normalized statistics and environment identity to `phase1-baseline.json`; keep raw Lighthouse/browser reports in ignored output. Use the pinned browser and local production server only. Do not contact Vercel, Cloudflare, public fonts/thumbnails, analytics, or a hosted report service; intercept/freeze external assets. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-06 --task 1` so T-01-18 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: baseline binds the owner-approved manifest hash and protected-file hashes.
    - Data: each protected UI route has exactly five raw samples plus median, p75, p95/max fields; bundle/resource summaries name units.
    - Behavior: the command rejects stale-build reuse and exits nonzero on missing/partial route data.
    - Privacy: artifact contains no absolute paths, secret/env values, raw page bodies, source payloads, or external report URLs.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-06 --task 1</automated>
    <expected>Exit 0; fresh, complete five-run metrics and environment/bundle identity exist for all four UI routes.</expected>
  </verify>
  <done>The accepted current public UI has a reproducible route/bundle/interaction baseline. Result fragment: T-01-18 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Measure controlled RAG timing and derive route-specific budget candidates</name>
  <files>web/tests/contracts/rag-latency.test.ts, web/tests/performance/phase1-budget-proposal.json, web/tests/performance/phase1-baseline.json, web/tests/security/phase1-threat-results/01-06.json</files>
  <read_first>web/tests/contracts/rag-latency.test.ts and web/tests/performance/phase1-budget-proposal.json if present (otherwise treat them as create targets); web/tests/support/rag-stub.mjs; web/tests/contracts/api-chat.contract.test.ts; web/app/api/chat/route.ts; web/tests/performance/phase1-baseline.json; 01-RESEARCH.md Performance Baseline and Budget Gate; 01-CONTEXT.md D-15</read_first>
  <behavior>
    - Controlled grounded, abstention, delayed, and timeout stub cases record first response byte, chunk sequence, and total completion without a live model.
    - Candidate budgets are route/metric specific and numerically traceable to observed legacy samples and variance.
    - No candidate is represented as owner-approved.
  </behavior>
  <action>Per D-15, add deterministic RAG timing cases around the local Worker-shaped stub, including bounded delay and timeout, while preserving the approved body/chunk interpretation from Plan 01-01. Append normalized RAG samples to the baseline. Produce `phase1-budget-proposal.json` whose route, bundle, drawer/rail/graph/chat interaction, and RAG candidates each cite source sample IDs, observed maximum, measured run-to-run range, chosen headroom, unit, and rationale. Use the observed maximum plus measured variability/tool resolution as the candidate ceiling; do not copy generic web-vitals thresholds or invent values without source samples. Set proposal status to `pending-owner-approval`. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-06 --task 2` so T-01-19 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Data: every numeric proposal has `source_samples`, `observed_max`, `observed_range`, `headroom`, `budget`, `unit`, and `rationale`.
    - Behavior: RAG tests remain local, deterministic, and preserve the accepted timeout/stream semantics.
    - Source: no approved budget file exists yet and proposal status is pending.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-06 --task 2</automated>
    <expected>Exit 0; deterministic RAG timing passes and every candidate budget is numerically evidence-linked but unapproved.</expected>
  </verify>
  <done>All Phase 1 performance dimensions have measured, reviewable candidate ceilings rather than invented thresholds. Result fragment: T-01-19 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measured budget gate in validation</name>
  <files>.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md, web/tests/security/phase1-threat-results/01-06.json</files>
  <read_first>web/tests/performance/phase1-baseline.json; web/tests/performance/phase1-budget-proposal.json; web/tests/contracts/rag-latency.test.ts; .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md; this plan's threat model</read_first>
  <action>Map QUAL-06 and the RAG-latency portion of COMP-03 to Plan `01-06`, Wave 6, exact commands, and T-01-18 through T-01-20. Record baseline/proposal artifact paths and measured environment. Leave the budget approval and all visual migration rows pending; keep both completion flags false. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-06 --task 3` so T-01-20 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: QUAL-06 points to the real baseline and proposal artifacts and names the pending owner approval.
    - Behavior: no numeric proposal is labelled accepted.
    - Command: completion flags remain false.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-06 --task 3</automated>
    <expected>Exit 0; measured evidence is mapped and approval remains explicitly pending.</expected>
  </verify>
  <done>The validation architecture distinguishes measured evidence from owner-approved budgets. Result fragment: T-01-20 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Performance baseline integrity, user privacy, availability bounds, and deterministic RAG behavior.

## Trust Boundaries
| Boundary | Description |
|---|---|
| local browser/build → versioned metrics | Stale builds or uncontrolled external resources can falsify measurements. |
| synthetic RAG stub → public timing contract | Test timing must not leak secrets or accidentally call live services. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-06.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-18 | Tampering | baseline metrics | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 1 | cd web &amp;&amp; node scripts/capture-phase1-performance.mjs --check &amp;&amp; node -e 'const b=require("./tests/performance/phase1-baseline.json"); for(const r of ["/","/episodes","/connections","/chat"]) if(b.routes[r].runs.length!==5) process.exit(1); if(!b.environment?.chromium\|\|!b.bundle) process.exit(1)' | defined | ASVS V14: bind manifest/file hashes, fresh build, pinned browser, five raw samples, source IDs, and reproducible collector. |
| T-01-19 | Information Disclosure | reports/artifacts | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 2 | cd web &amp;&amp; npm run test:contracts -- tests/contracts/rag-latency.test.ts &amp;&amp; node -e 'const p=require("./tests/performance/phase1-budget-proposal.json"); if(p.status!=="pending-owner-approval") process.exit(1); for(const x of Object.values(p.budgets)) for(const k of ["source_samples","observed_max","observed_range","headroom","budget","unit","rationale"]) if(x[k]===undefined) process.exit(1)' | defined | ASVS V8: local synthetic fixtures, filesystem-only reports, normalized metadata, privacy scan, no raw bodies/absolute paths. |
| T-01-20 | Denial of Service | route/RAG regressions | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 3 | node -e 'const s=require("fs").readFileSync(".planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md","utf8"); if(!s.includes("01-06")\|\|!s.includes("phase1-budget-proposal.json")\|\|!s.includes("pending owner approval")\|\|!s.includes("wave_0_complete: false")) process.exit(1)' | defined | Evidence-derived ceilings and preserved timeout/size bounds become blocking after owner approval. |
</threat_model>

<verification>
Run fresh capture, validate five samples per route, run controlled RAG timing, and schema-check every proposed budget's evidence links.
</verification>

<success_criteria>
The current accepted application is measured reproducibly, and every candidate budget is explicit, evidence-derived, and awaiting owner authority.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-06-SUMMARY.md` when done.
</output>
