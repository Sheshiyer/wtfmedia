---
phase: 01-compatibility-component-proof-harness
plan: "12"
type: execute
wave: 12
depends_on: ["01-11"]
files_modified:
  - web/components/legacy/public/LegacyPublicShell.tsx
  - web/components/patterns/PublicNav.tsx
  - web/components/patterns/PublicShell.tsx
  - web/app/layout.tsx
  - web/components/patterns/PublicShell.stories.tsx
  - web/tests/journeys/public-shell.spec.ts
  - web/tests/security/phase1-threat-results/01-12.json
autonomous: true
requirements: [COMP-01, DSYS-01, DSYS-02, DSYS-04, DSYS-06, DSYS-07, DSYS-08, DSYS-10, QUAL-02, QUAL-03, QUAL-04, QUAL-05]
must_haves:
  truths:
    - "One shared WTF shell/token/type/control/state language supports distinct route compositions per D-07."
    - "The exact prior shell remains selectable for rollback, while the migrated shell preserves all four navigation destinations and corrects Home meaning."
    - "The migrated shell provides the scoped presentation root consumed by Plan 01-22, while all original shared brand/effect implementations remain untouched for legacy mode."
  artifacts:
    - path: "web/components/patterns/PublicShell.tsx"
      provides: "Migrated public header/main/footer shell"
    - path: "web/components/legacy/public/LegacyPublicShell.tsx"
      provides: "Runnable rollback shell"
  key_links:
    - from: "web/app/layout.tsx"
      to: "legacy or migrated shell"
      via: "server-only public UI selector"
---

<objective>
Introduce the shared public shell boundary after the Episodes proof slice is green.

Purpose: Apply D-05/D-07 consistently without flattening route compositions or coupling rollback-sensitive brand motion to shell extraction.
Output: Dual shell variants, semantic navigation/footer, migrated presentation scope, deterministic stories, and shell browser proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/app/layout.tsx
@web/lib/public/public-ui-variant.ts
@web/tests/contracts/legacy-presentation-dependencies.json
@web/tests/visual/legacy/phase1-legacy-baseline.manifest.json
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@DESIGN.md
@PRODUCT.md
</context>

<must_haves>
  <truths>Home, Episodes, Connections, and Ask WTF keep `/`, `/episodes`, `/connections`, `/chat`; navigation copy is `home`, `episodes`, `connections`, `ask wtf` and active meaning is non-color.</truths>
  <artifacts>Legacy shell preserves accepted output and original shared dependencies; migrated shell owns the scoped marker, skip link, responsive nav, public-only footer, and focus target.</artifacts>
  <key_links>Root metadata stays server-owned and layout selection never exposes a public switch or operator state.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-12 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-12.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-12, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto">
  <name>Task 1: Retain the accepted shell and add a server-selected migrated shell boundary</name>
  <files>web/components/legacy/public/LegacyPublicShell.tsx, web/app/layout.tsx, web/components/patterns/PublicShell.tsx, web/components/patterns/PublicNav.tsx, web/tests/security/phase1-threat-results/01-12.json</files>
  <read_first>web/components/legacy/public/LegacyPublicShell.tsx, web/components/patterns/PublicShell.tsx, and web/components/patterns/PublicNav.tsx if present (otherwise treat them as create targets); web/app/layout.tsx; web/lib/public/public-ui-variant.ts; web/tests/contracts/phase1-baseline-approval.json; 01-CONTEXT.md D-07, D-09, D-12; 01-UI-SPEC.md Shared Public Shell Contract</read_first>
  <action>Per D-06, D-07, and D-12, extract the current accepted header/main/footer/custom-cursor presentation into `LegacyPublicShell` without behavioral cleanup, preserving imports of the original Wordmark, Sparkle, Marquee, and CustomCursor and verifying their approved hashes. This is byte-preserving rollback extraction only: add no effect implementation, effect acceptance, or effect behavior test. Create `PublicShell` as the repository-owned migrated shell contract and `PublicNav` as its bounded client island for pathname, `aria-current`, and route-focus behavior; render the migrated scope marker required by Plan 01-08 and implement the four labels/destinations plus responsive 2x2/one-row structure. Make RootLayout preserve metadata/html/body while selecting server-side through `publicUiVariant()`. Keep default legacy and preserve route/data/query/API behavior. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-12 --task 1` so T-01-36 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: normalized legacy shell content matches the accepted layout behavior except documented extraction wiring.
    - Behavior: both variants serve all four UI routes and the same navigation destinations.
    - Security: selector/env is absent from browser bundle/DOM/URL; footer in migrated shell has no model/engine/private path/health/version claims.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-12 --task 1</automated>
    <expected>Exit 0; both shell modules build and protected route destinations remain unchanged.</expected>
  </verify>
  <done>The shell can migrate and roll back independently without changing public contracts. Result fragment: T-01-36 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and behavior-test responsive public navigation and shell semantics</name>
  <files>web/components/patterns/PublicShell.tsx, web/components/patterns/PublicNav.tsx, web/components/patterns/PublicShell.stories.tsx, web/tests/security/phase1-threat-results/01-12.json</files>
  <read_first>PublicShell and PublicNav from Task 1; UI-SPEC Shared Navigation/Route Copy and Responsive Shell; PRODUCT identity; SkipLink; scoped tokens; approved legacy graph/baseline</read_first>
  <behavior>
    - Skip link is first, wordmark links home, active route uses `aria-current=page`, route focus targets destination h1 without stealing user focus, and footer is public-only.
    - 320 uses visible 2x2 navigation, 768 one row, 1440 bounded 1400px container; no hidden destination/page overflow.
    - Migrated shell exposes an inert optional brand slot for Plan 01-22 without importing, rendering, behavior-testing, or mutating any brand-effect implementation.
  </behavior>
  <action>Complete `PublicShell` around bounded `PublicNav` with the exact skip-link, nav, main-focus, footer, public-only copy, scoped marker, and inert optional brand-slot contract. Create focused PublicShell story interactions only for keyboard tab order, skip target, `aria-current`, guarded route focus, 320/768/1440 navigation visibility, banned internal copy, and the empty slot boundary. Explicitly assert that migrated PublicShell has no imports from legacy Wordmark/Sparkle/Marquee/CustomCursor and no imports from the migrated brand modules owned by Plan 01-22. Do not render or behavior-test marquee, sparkle, custom-cursor, or brand reduced-motion behavior here; Plan 01-22 Task 3 exclusively owns effect wiring and acceptance. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-12 --task 2` so T-01-37 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Behavior: every destination remains visible/operable at all three widths with active non-color meaning.
    - Accessibility: skip link, focus-visible, guarded route h1 focus policy, semantic navigation, and responsive reflow pass without any brand-effect behavior assertion.
    - Boundary: the inert brand slot exists, original shared brand/effect hashes remain unchanged, and no legacy or migrated brand-effect import exists before Plan 01-22.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-12 --task 2</automated>
    <expected>Exit 0; shell navigation, focus, responsive, public-copy, and inert-slot tests pass, and migrated PublicShell imports no legacy or Plan 01-22 brand-effect module.</expected>
  </verify>
  <done>The shared migrated shell is public-only, keyboard/focus safe, responsive, scoped, and exposes only an inert no-import brand slot for Plan 01-22. Result fragment: T-01-37 is recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove shell states across routes, viewports, and keyboard input</name>
  <files>web/components/patterns/PublicShell.stories.tsx, web/tests/journeys/public-shell.spec.ts, web/tests/security/phase1-threat-results/01-12.json</files>
  <read_first>web/components/patterns/PublicShell.stories.tsx and web/tests/journeys/public-shell.spec.ts if present (otherwise treat them as create targets); web/components/patterns/PublicShell.tsx; web/components/patterns/PublicNav.tsx; 01-UI-SPEC.md Shared Public Shell Contract and Responsive Shell; web/tests/performance/phase1-budgets.json</read_first>
  <behavior>
    - Stories capture active navigation, skip link, focus, responsive structure, public copy, migrated scope, and the inert brand slot without importing or exercising brand effects.
    - Browser journey runs each protected UI route under migrated shell at 320/768/1440 and legacy shell smoke.
    - No public shell DOM/artifact contains internal/operator/infrastructure fields or selector values.
  </behavior>
  <action>Complete deterministic shell stories and route-matrix journey with keyboard tab order, skip target, `aria-current`, focus-visible, page-overflow, serious/critical axe, privacy, and approved shell performance comparisons. Assert no legacy shared-component hash changes. Capture unapproved shell candidates only and rerun Episodes plus complete legacy presentation comparison. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-12 --task 3` so T-01-38 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Command: story and browser route matrix pass at all widths/preferences.
    - Accessibility: zero serious/critical axe findings plus explicit keyboard/focus assertions.
    - Privacy: footer/DOM/bundles contain none of the forbidden internal/infrastructure vocabulary or selector key.
    - Rollback: legacy shell and Episodes remain runnable.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-12 --task 3</automated>
    <expected>Exit 0; shared shell proof and Episodes regression/rollback pass while visual approval remains pending.</expected>
  </verify>
  <done>The shared public shell is proven without flattening route-specific composition or exposing internal state. Result fragment: T-01-38 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Public navigation meaning, brand-slot boundary, focus access, client privacy, and shell rollback.

## Trust Boundaries
| Boundary | Description |
|---|---|
| server shell selector → client render | Variant or private config must not leak or become client-controlled. |
| pathname/async route → focus | Automated focus can strand or disrupt users. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-12.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-36 | Information Disclosure | shell/footer/selector | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 1 | cd web &amp;&amp; npm run build &amp;&amp; npm run test:contracts -- tests/contracts/public-routes.contract.test.ts | defined | Public-only copy allowlist, bundle/DOM scan, server-only selector, no operator/infrastructure fields. |
| T-01-37 | Tampering | navigation meaning | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 2 | cd web &amp;&amp; npm run test:components -- PublicShell --grep "navigation\|focus\|responsive" &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; node scripts/capture-legacy-presentation.mjs --check | defined | Exact href/label/aria-current matrix and route contract tests across both variants. |
| T-01-38 | Denial of Service | focus/navigation/reflow | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 3 | cd web &amp;&amp; npm run test:components -- PublicShell &amp;&amp; npm run test:browser -- tests/journeys/public-shell.spec.ts tests/rollback/legacy-presentation.spec.ts &amp;&amp; npm run test:privacy -- --check &amp;&amp; npm run test:rollback -- --route=/episodes | defined | Skip link, visible focus, guarded route focus, semantic navigation, inert brand slot, all-width browser/axe tests, and explicit no-import assertions. |
</threat_model>

<verification>
Build both shell variants, run shell stories/journeys at all viewports with keyboard/focus assertions, enforce the no-import brand-slot boundary, privacy scan, and Episodes rollback regression.
</verification>

<success_criteria>
One accessible WTF public shell unifies tokens and controls while preserving distinct route jobs, navigation contracts, privacy, budgets, and rollback.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-12-SUMMARY.md` when done.
</output>
