---
phase: 01-compatibility-component-proof-harness
plan: "05"
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - web/lib/public/contracts.ts
  - web/tests/support/rag-stub.mjs
  - web/tests/contracts/api-chat.contract.test.ts
  - web/tests/contracts/public-projection.contract.test.ts
  - web/tests/contracts/public-routes.contract.test.ts
  - web/tests/privacy/scan.mjs
  - .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
  - web/tests/security/phase1-threat-results/01-05.json
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, QUAL-02, QUAL-05]
must_haves:
  truths:
    - "Executable contracts freeze every accepted /api/chat validation, status, header, source, no-store, safe-error, and response-consumption branch without changing the route."
    - "Public episode, connection, and chat projections expose only explicit allowlisted fields and artifact scans reject named leak classes."
    - "All protected routes and supported query examples are machine-addressable before visual migration."
  artifacts:
    - path: "web/tests/contracts/api-chat.contract.test.ts"
      provides: "Complete deterministic /api/chat contract matrix"
    - path: "web/tests/privacy/scan.mjs"
      provides: "Bounded scanner for source, build, DOM, logs, traces, snapshots, and planning artifacts"
  key_links:
    - from: "web/tests/support/rag-stub.mjs"
      to: "web/app/api/chat/route.ts"
      via: "local Worker-shaped authenticated upstream fixtures"
    - from: "web/lib/public/contracts.ts"
      to: "public projection tests"
      via: "explicit allowlists and forbidden-field vocabulary"
---

<objective>
Turn the owner-approved compatibility baseline into executable public/API/security contracts.

Purpose: Make behavior and projection regressions fail before any protected route is visually migrated.
Output: Typed public contracts, deterministic RAG stub, complete route/API matrices, privacy scanner, and populated validation mappings.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/tests/contracts/phase1-compatibility-manifest.json
@web/tests/contracts/phase1-baseline-approval.json
@web/app/api/chat/route.ts
@web/app/chat/page.tsx
@web/lib/episodes.ts
@web/lib/connections.ts
@.planning/phases/01-compatibility-component-proof-harness/01-RESEARCH.md
@.planning/phases/01-compatibility-component-proof-harness/01-PATTERNS.md
@.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
</context>

<must_haves>
  <truths>D-09 is executable; no test changes production behavior to make it easier to assert.</truths>
  <artifacts>The RAG stub covers grounded, ungrounded, malformed, non-OK, timeout, and delayed responses using dummy secrets and public-shaped sources.</artifacts>
  <key_links>The Next route must send the server-only secret/IP/request-ID to the stub while client-visible bodies, headers, DOM, and artifacts never contain those values.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-05 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-05.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-05, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the complete /api/chat seam against a Worker-shaped stub</name>
  <files>web/lib/public/contracts.ts, web/tests/support/rag-stub.mjs, web/tests/contracts/api-chat.contract.test.ts, web/tests/security/phase1-threat-results/01-05.json</files>
  <read_first>web/lib/public/contracts.ts, web/tests/support/rag-stub.mjs, and web/tests/contracts/api-chat.contract.test.ts if present (otherwise treat absent paths as create targets); web/tests/contracts/phase1-compatibility-manifest.json; web/tests/contracts/phase1-baseline-approval.json; web/app/api/chat/route.ts; web/app/chat/page.tsx; 01-RESEARCH.md Required `/api/chat` Contract Matrix and Known Threat Patterns; 01-PATTERNS.md Ask WTF client and route analogs</read_first>
  <behavior>
    - Invalid JSON, missing/non-array/empty user message, and over-limit question preserve exact accepted 400 behavior without upstream calls.
    - Only the last eight messages are considered and the last user message becomes the upstream question.
    - Missing secret, network timeout, non-OK/malformed/missing-answer upstream cases preserve generic safe 503 JSON with no provider detail.
    - Grounded and abstention success preserve upstream status, exact text body, URI-encoded X-Sources keys/types, X-Model, X-Fallback, Content-Type, Cache-Control no-store, and verified/untimed URL mapping.
    - Outbound secret, client IP, and request ID exist only on the server-to-server stub request and never in public output/artifacts.
  </behavior>
  <action>Define public source/header/request contract types and constants without importing server secrets. Implement a local HTTP stub that records only bounded metadata, accepts a dummy shared secret, provides deterministic grounded/ungrounded/error/delay fixtures, and never calls a live service. Write Vitest contract cases for every accepted branch and source transform. Test the existing Route Handler directly or through a controlled local Next boundary without modifying `web/app/api/chat/route.ts`; any mismatch must be reported as a baseline conflict, not normalized silently. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-05 --task 1` so T-01-13, T-01-15, T-01-16 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Behavior: every matrix row in research has at least one named test and exact status/header/body assertions.
    - Security: source URLs accept only the approved public safe shape; dummy secret and internal request metadata never appear in response body/headers or snapshots.
    - Source: `web/app/api/chat/route.ts` has no diff from the start of this task.
    - Command: contract tests run offline against the local stub and exit nonzero on any missing case.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-05 --task 1</automated>
    <expected>Exit 0 with named coverage for every validation, abuse-bound, upstream-failure, success, header, source, and secret-boundary case.</expected>
  </verify>
  <done>The complete accepted `/api/chat` seam is frozen by deterministic tests without changing production behavior or contacting external services. Result fragment: T-01-13, T-01-15, T-01-16 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce public projection, route, query, source URL, and artifact privacy contracts</name>
  <files>web/tests/contracts/public-projection.contract.test.ts, web/tests/contracts/public-routes.contract.test.ts, web/tests/privacy/scan.mjs, web/tests/security/phase1-threat-results/01-05.json</files>
  <read_first>web/tests/contracts/public-projection.contract.test.ts, web/tests/contracts/public-routes.contract.test.ts, and web/tests/privacy/scan.mjs if present (otherwise treat absent paths as create targets); web/lib/public/contracts.ts; web/lib/episodes.ts; web/lib/connections.ts; web/tests/contracts/phase1-compatibility-manifest.json; 01-UI-SPEC.md Privacy-Safe Public Projection Contract; AGENTS.md; ISA.md anti-criteria ISC-119 through ISC-127</read_first>
  <behavior>
    - Public episode, connection, and chat objects contain only allowlisted keys and reject operator/private fields.
    - All five protected URLs and supported `/chat?q=` examples remain present; route source files may move later without changing paths.
    - Unsafe source protocols/domains, raw HTML injection, secrets, absolute paths, private links/payloads, provider errors, and prompt/response logs are rejected from scanned artifacts.
  </behavior>
  <action>Write contract tests that traverse representative repository data through explicit public allowlists, fail on the forbidden field vocabulary (`tasks`, `owners`, `leads`, `budgets`, `briefs`, `health`, `production`, `permissions`, private asset/payload fields), and validate source URLs/protocols according to the accepted public source contract. Add route/query manifest assertions including `/chat?q=` and preservation of unrelated query parameters. Implement a bounded scanner with explicit roots for source, `.next`, Storybook output, Playwright reports/traces, Lighthouse reports, snapshots, fixtures, logs, and planning artifacts. Avoid claiming the scanner is a universal secret detector; report named leak classes and fail closed on matches outside documented synthetic dummy allowlists. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-05 --task 2` so T-01-14 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Behavior: a synthetic fixture containing each forbidden field or unsafe URL makes the relevant test fail.
    - Privacy: real repository public data is checked by shape/hash, not copied into new fixture bodies.
    - Command: scanner supports `--check` and reports each scanned root plus a zero-violation summary.
    - Source: no auth/session/operator DTO or new external service is introduced.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-05 --task 2</automated>
    <expected>Exit 0; allowlisted public projections and protected route/query cases pass, and named leak classes report zero violations.</expected>
  </verify>
  <done>Public-only data, safe source navigation, and privacy-safe artifacts are executable release boundaries. Result fragment: T-01-14 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto">
  <name>Task 3: Populate contract and threat mappings in the validation architecture</name>
  <files>.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md, web/tests/security/phase1-threat-results/01-05.json</files>
  <read_first>.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md; all test files in this plan; this plan's threat model; 01-CONTEXT.md D-09 and D-13</read_first>
  <action>Replace the COMP-01 through COMP-04 and QUAL-02/QUAL-05 TBD mappings with the exact Plan `01-05`, Wave 5, task names, test files, commands, and T-01-13 through T-01-17 threat references. Mark only the rows whose real commands now pass as green. Retain `wave_0_complete: false` and `nyquist_compliant: false` because browser, visual, performance, aggregate, and rollback proof is not complete. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-05 --task 3` so T-01-17 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: each affected row names a real file and non-watch command.
    - Behavior: rows are green only after the exact command passes.
    - Command: no `TBD` remains in the affected rows; global completion flags remain false.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-05 --task 3</automated>
    <expected>Exit 0; affected rows are concrete and overall Nyquist/Wave 0 status remains incomplete.</expected>
  </verify>
  <done>The validation contract now points to real compatibility, projection, privacy, and threat proof. Result fragment: T-01-17 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Public episode/connection/chat projections, edge secret, request bounds, safe errors, source navigation, and privacy-safe artifacts.

## Trust Boundaries
| Boundary | Description |
|---|---|
| anonymous client → `/api/chat` | Untrusted JSON, message shape, length, and frequency cross into a server proxy. |
| Next Route Handler → local Worker stub | Server-only secret and request metadata cross a backend boundary. |
| repository/public data → DOM/artifacts | Private/operator fields and unsafe URLs must be excluded from public serialization and evidence. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-05.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-13 | Tampering | chat request/source URL | mitigate | high | medium | high | v5.0.0-1.2.2 | Task 1 | cd web &amp;&amp; npm run test:contracts -- tests/contracts/api-chat.contract.test.ts | defined | ASVS V5 input validation: exact JSON/message/length cases; allowlisted URL protocols/domains; no raw HTML plugin. |
| T-01-14 | Information Disclosure | public DTO/DOM/artifacts | mitigate | critical | medium | high | v5.0.0-15.3.1 | Task 2 | cd web &amp;&amp; npm run test:contracts -- tests/contracts/public-projection.contract.test.ts tests/contracts/public-routes.contract.test.ts &amp;&amp; npm run test:privacy -- --check | defined | ASVS V4/V8: explicit public allowlists, forbidden-field fixtures, multi-artifact bounded scan. |
| T-01-15 | Information Disclosure | edge secret/errors | mitigate | critical | medium | high | v5.0.0-15.3.1 | Task 1 | cd web &amp;&amp; npm run test:contracts -- tests/contracts/api-chat.contract.test.ts | defined | ASVS V8/V13: server-only dummy secret proof, generic safe failures, no provider/raw payload/log leakage. |
| T-01-16 | Denial of Service | `/api/chat` | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 1 | cd web &amp;&amp; npm run test:contracts -- tests/contracts/api-chat.contract.test.ts | defined | Preserve last-eight bound, non-empty/length validation, timeout, and no upstream call for rejected input; contract-test all. |
| T-01-17 | Repudiation | compatibility evidence | mitigate | high | low | high | v5.0.0-2.1.1 | Task 3 | node -e 'const s=require("fs").readFileSync(".planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md","utf8"); for(const id of ["COMP-01","COMP-02","COMP-03","COMP-04","QUAL-02","QUAL-05"]) {const l=s.split("\n").find(x=&gt;x.includes(`\| ${id} \|`)); if(!l\|\|l.includes("TBD")) process.exit(1)} if(!s.includes("wave_0_complete: false")\|\|!s.includes("nyquist_compliant: false")) process.exit(1)' | defined | Bind tests to owner-approved manifest/hash and expose exact case names/results in CI artifacts. |
</threat_model>

<verification>
Run all contract files and privacy scan offline; confirm the production route is unmodified and validation mappings are concrete.
</verification>

<success_criteria>
Protected public/API behavior and leak boundaries fail deterministically before any visual migration can proceed.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-05-SUMMARY.md` when done.
</output>
