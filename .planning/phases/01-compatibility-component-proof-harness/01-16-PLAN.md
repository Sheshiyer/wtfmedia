---
phase: 01-compatibility-component-proof-harness
plan: "16"
type: execute
wave: 17
depends_on: ["01-15"]
files_modified:
  - web/components/legacy/public/LegacyHomePage.tsx
  - web/components/domain/public/MigratedHomePage.tsx
  - web/app/page.tsx
  - web/components/domain/public/MigratedGuestStrip.tsx
  - web/components/domain/public/MigratedHomePage.stories.tsx
  - web/tests/journeys/home.spec.ts
  - web/tests/security/phase1-threat-results/01-16.json
autonomous: true
requirements: [COMP-01, COMP-02, COMP-04, DSYS-01, DSYS-02, DSYS-04, DSYS-06, DSYS-07, DSYS-08, DSYS-10, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]
must_haves:
  truths:
    - "Home is the fourth and final route-by-route visual migration, preserving `/`, public navigation meaning, real catalogue-shaped data, and rollback."
    - "The composition uses the approved public promise and distinct editorial hierarchy, not internal Control Room language, roadmap cards, model details, or fabricated metrics."
    - "Guest/media rail and signature effects use native ScrollRail, semantic tokens, pausable/reduced motion, and deterministic public fixtures."
  artifacts:
    - path: "web/components/domain/public/MigratedHomePage.tsx"
      provides: "Distinct approved public Home editorial composition"
    - path: "web/components/legacy/public/LegacyHomePage.tsx"
      provides: "Runnable prior Home rollback target"
  key_links:
    - from: "web/components/domain/public/MigratedGuestStrip.tsx"
      to: "web/components/patterns/ScrollRail.tsx"
      via: "native accessible public imagery rail"
---

<objective>
Retain and visibly migrate Home as the final protected public route.

Purpose: Complete D-06/D-07's four distinct editorial rooms without changing public meaning, data, navigation, privacy, budgets, or rollback.
Output: Legacy/candidate Home variants, migrated GuestStrip, deterministic Home states, and full route evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/app/page.tsx
@web/components/GuestStrip.tsx
@web/tests/contracts/legacy-presentation-dependencies.json
@web/components/patterns/PublicShell.tsx
@web/components/patterns/ScrollRail.tsx
@web/components/Marquee.tsx
@web/components/Wordmark.tsx
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@PRODUCT.md
@web/tests/performance/phase1-budgets.json
</context>

<must_haves>
  <truths>The Home route uses exact approved copy hierarchy and CTA destinations; all displayed counts derive from the accepted public dataset and state their catalogue scope.</truths>
  <artifacts>Stories/journey cover ready, hero/nav focus, marquee paused, reduced motion, deterministic public media fallback, and 320 reflow.</artifacts>
  <key_links>The App Router page uses the existing server-only selector with legacy default until Plan 01-20; no data/service/deployment change occurs.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-16 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-16.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-16, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto">
  <name>Task 1: Retain accepted Home and add the dual-variant root route</name>
  <files>web/components/legacy/public/LegacyHomePage.tsx, web/components/domain/public/MigratedHomePage.tsx, web/app/page.tsx, web/tests/security/phase1-threat-results/01-16.json</files>
  <read_first>web/components/legacy/public/LegacyHomePage.tsx and web/components/domain/public/MigratedHomePage.tsx if present (otherwise treat them as create targets); web/tests/contracts/phase1-baseline-approval.json; web/app/page.tsx; web/lib/public/public-ui-variant.ts; 01-CONTEXT.md D-06, D-07, D-12, D-16; 01-UI-SPEC.md Home contract</read_first>
  <action>Copy the accepted current Home implementation into `LegacyHomePage` with only import/export wiring changes and bind normalized content to the approved source hash. Create a fully functional candidate `MigratedHomePage` using the same public dataset/assets and route destinations. Make `web/app/page.tsx` a server wrapper selecting the two modules via `publicUiVariant()` with legacy default. Do not change `/`, add a new endpoint, move data, or expose the selector. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-16 --task 1` so T-01-49 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: retained Home normalizes to the accepted source; candidate uses only existing public data/assets.
    - Behavior: both variants serve `/` and preserve links to `/chat`, `/episodes`, and `/connections`.
    - Security: selector and forbidden internal fields are absent from client output.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-16 --task 1</automated>
    <expected>Exit 0; both root variants build and public route/projection contracts pass.</expected>
  </verify>
  <done>The accepted Home is recoverable and the root migration branch is isolated. Result fragment: T-01-49 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and behavior-test Home with a migrated-only GuestStrip rail</name>
  <files>web/components/domain/public/MigratedHomePage.tsx, web/components/domain/public/MigratedGuestStrip.tsx, web/components/domain/public/MigratedHomePage.stories.tsx, web/tests/security/phase1-threat-results/01-16.json</files>
  <read_first>MigratedHomePage; original GuestStrip as legacy reference only; approved legacy dependency graph; UI-SPEC Home Composition and Width/State; PRODUCT; DESIGN; ScrollRail and migrated brand APIs</read_first>
  <behavior>
    - Heading/copy/CTAs are `wtf media · public catalogue`, `ask the catalogue. get the moment.`, primary `ask wtf`, secondary `browse episodes`.
    - Composition contains public hero, one Ask proof, one Episodes proof, optional nonessential pausable marquee, ink closing band, and public-only footer supplied by shell.
    - 320 is one column with stacked full-width actions; 768 bounded editorial column; 1440 asymmetric but within 1400px.
  </behavior>
  <action>Build the distinct Home composition with semantic tokens, migrated-only canonical brand components, real public-shaped counts/media, approved copy, and route actions. Create `MigratedGuestStrip` as a new migrated-only ScrollRail consumer with meaningful public image alt text; do not edit original `GuestStrip`, whose approved hash and legacy imports must remain exact. Add focused Home story interactions now for CTA keyboard use, rail previous/next/edge state, media fallback, reduced motion, and 320 reflow. Remove internal/model/version/roadmap language and identical feature-card grids. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-16 --task 2` so T-01-50 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Copy: exact required public promise and CTA labels/destinations are present; banned internal/infrastructure/scale language is absent.
    - Data: any episode/show count derives from `data` and is labelled catalogue scope; no fabricated total/health.
    - Accessibility: CTAs, GuestStrip controls/items, marquee pause, images, focus, and reflow are keyboard/semantic.
    - Source: migrated files contain no raw color values or pointer-only required behavior.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-16 --task 2</automated>
    <expected>Exit 0; migrated Home and GuestStrip compile and consume the approved semantic system.</expected>
  </verify>
  <done>Home becomes a distinct WTF public publication room without internal product or fabricated evidence claims. Result fragment: T-01-50 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove Home brand, focus, motion, viewports, privacy, budgets, and rollback</name>
  <files>web/components/domain/public/MigratedHomePage.stories.tsx, web/tests/journeys/home.spec.ts, web/tests/security/phase1-threat-results/01-16.json</files>
  <read_first>Home story/journey if present; MigratedHomePage; MigratedGuestStrip; PublicShell and brand stories; UI-SPEC Home evidence; approved budgets and legacy baseline</read_first>
  <behavior>
    - Story/journey cover ready, both hero/nav actions focused, marquee paused, reduced motion, media fallback, GuestStrip controls, and exact 320/768/1440 layouts.
    - Migrated and legacy Home variants preserve routes/data meaning and meet approved performance/bundle budgets.
    - DOM/artifacts contain no operator fields, private links/payloads, infrastructure/model/version copy, or machine paths.
  </behavior>
  <action>Add deterministic Home stories and Playwright journey with fixed public media/font interception, keyboard and focus order, marquee/ScrollRail/reduced-motion behavior, exact copy/CTA assertions, page-overflow checks, serious/critical axe scan, privacy scan, approved budget checks, and dual-variant rollback. Capture unapproved visual candidates only. Re-run the already-migrated Episodes, Connections, and Chat smoke journeys after Home passes to catch shared-shell regressions. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-16 --task 3` so T-01-51 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Command: Home story/journey, three prior route smokes, privacy, performance subset, and root rollback pass.
    - Accessibility: zero serious/critical axe findings plus explicit focus/motion/rail assertions.
    - Visual: all required states/widths have deterministic candidates not marked approved.
    - Rollback: legacy root remains runnable without data change.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-16 --task 3</automated>
    <expected>Exit 0; Home and all prior migrated route smokes pass with legacy root rollback intact.</expected>
  </verify>
  <done>All four protected UI routes are now visibly migrated one at a time with their rollback paths retained. Result fragment: T-01-51 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Public brand/narrative truth, route/navigation continuity, public dataset privacy, motion/focus access, and root rollback.

## Trust Boundaries
| Boundary | Description |
|---|---|
| public repository data → Home claims/counts | Data can be overstated or private/internal claims invented. |
| external media → browser/evidence | Uncontrolled images/fonts can leak requests or destabilize snapshots. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-16.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-49 | Information Disclosure | Home DOM/artifacts | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 1 | cd web &amp;&amp; npm run build &amp;&amp; npm run test:contracts -- tests/contracts/public-routes.contract.test.ts tests/contracts/public-projection.contract.test.ts | defined | Public allowlist, banned internal/infrastructure copy, deterministic intercepted media, privacy scan. |
| T-01-50 | Tampering | catalogue claims/navigation | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 2 | cd web &amp;&amp; npm run test:components -- MigratedHomePage --grep "CTA\|GuestStrip\|reduced motion\|320" &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; node scripts/capture-legacy-presentation.mjs --check | defined | Derive counts from accepted data, exact copy/href assertions, no fabricated health/scale claims. |
| T-01-51 | Denial of Service | rail/motion/reflow | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 3 | cd web &amp;&amp; npm run test:components -- MigratedHomePage &amp;&amp; npm run test:browser -- tests/journeys/home.spec.ts tests/journeys/episodes.spec.ts tests/journeys/connections.spec.ts tests/journeys/chat.spec.ts &amp;&amp; npm run test:privacy -- --check &amp;&amp; npm run test:rollback -- --route=/ | defined | Native ScrollRail, pause/reduced motion, visible focus, 320/768/1440 and axe/browser proof. |
</threat_model>

<verification>
Run Home stories/journey, all prior route smokes, privacy, budgets, and root dual-variant rollback.
</verification>

<success_criteria>
Home completes the four-route visual migration with exact public narrative, WTF identity, real evidence, accessibility, budgets, and rollback.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-16-SUMMARY.md` when done.
</output>
