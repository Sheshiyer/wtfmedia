---
phase: 01-compatibility-component-proof-harness
plan: "23"
type: execute
wave: 10
depends_on: ["01-08", "01-10"]
files_modified:
  - web/components/patterns/ScrollRail.tsx
  - web/lib/public/url-state.ts
  - web/components/domain/public/MigratedEpisodesPage.tsx
  - web/tests/journeys/url-state.spec.ts
  - web/tests/security/phase1-threat-results/01-23.json
autonomous: true
requirements: [COMP-01, COMP-02, DSYS-04, DSYS-06, DSYS-07, DSYS-08, DSYS-10, QUAL-02, QUAL-03, QUAL-04, QUAL-06]
must_haves:
  truths:
    - "The first migrated proof foundation keeps `/episodes` and represents selection only as `?episode=<public-video-id>` per D-01 and D-02."
    - "ScrollRail provides native scroll, labelled controls, keyboard/touch/wheel access, visible focus, and immediate reduced-motion behavior per D-03."
    - "The migrated Episodes server boundary wraps the client URL-state consumer in Suspense without creating a path-segment episode route."
  artifacts:
    - path: "web/components/patterns/ScrollRail.tsx"
      provides: "Native multi-input horizontal rail primitive"
    - path: "web/lib/public/url-state.ts"
      provides: "Typed query parsing and push/replace helpers preserving unrelated parameters"
    - path: "web/components/domain/public/MigratedEpisodesPage.tsx"
      provides: "Server/Suspense boundary for the migrated Episodes workflow"
    - path: "web/tests/journeys/url-state.spec.ts"
      provides: "URL history, refresh/share, invalid-ID, focus, and route-shape proof"
  key_links:
    - from: "web/components/domain/public/MigratedEpisodesPage.tsx"
      to: "web/lib/public/url-state.ts"
      via: "Suspense-wrapped client selection consumer"
    - from: "web/tests/journeys/url-state.spec.ts"
      to: "/episodes?episode=<public-video-id>"
      via: "Back, Forward, refresh, share, invalid-ID, close, and unrelated-query assertions"
---

<objective>
Establish the Episodes URL-state, native ScrollRail, and server/Suspense foundation consumed by the drawer proof slice.

Purpose: Deliver D-01 through D-03 as an independently proven compatibility boundary before Plan 01-11 integrates public detail and route-wide evidence.
Output: Typed URL-state helpers, accessible ScrollRail, migrated Episodes server boundary, and focused browser proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@.planning/phases/01-compatibility-component-proof-harness/01-CONTEXT.md
@.planning/phases/01-compatibility-component-proof-harness/01-PATTERNS.md
@web/components/EpisodesBrowser.tsx
@web/components/DragRow.tsx
@web/lib/public/contracts.ts
@web/app/episodes/page.tsx
</context>

<must_haves>
  <truths>D-01, D-02, and D-03 are executable without changing `/episodes`, inventing a path route, or making query state client-only.</truths>
  <artifacts>The URL helper and ScrollRail expose stable typed contracts that Plan 01-11 consumes directly.</artifacts>
  <key_links>Selection changes preserve unrelated query parameters and browser history; native overflow remains the scrolling authority in every input and motion mode.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-23 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-23.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-23, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove URL-backed native Episodes navigation foundation</name>
  <files>web/components/patterns/ScrollRail.tsx, web/lib/public/url-state.ts, web/components/domain/public/MigratedEpisodesPage.tsx, web/tests/journeys/url-state.spec.ts, web/tests/security/phase1-threat-results/01-23.json</files>
  <read_first>01-CONTEXT.md D-01 through D-03 and D-09; 01-UI-SPEC.md `/episodes`, ScrollRail, URL-state, focus, responsive, and motion contracts; web/components/DragRow.tsx; web/components/EpisodesBrowser.tsx; web/app/episodes/page.tsx; web/lib/public/contracts.ts; retained variant wrapper from Plan 01-10</read_first>
  <behavior>
    - Opening a public episode pushes only the `episode` query key, preserves unrelated keys, and Back/Forward/refresh/share reproduce the same selection.
    - Closing uses history when opened in-page, uses replace for direct loads, and exposes a deterministic heading/results focus fallback for the drawer consumer.
    - Unknown, duplicated, malformed, or disallowed IDs resolve to a non-leaking unavailable state without a path-segment route.
    - ScrollRail remains native, labelled, keyboard/touch/wheel operable, visibly focused, start/middle/end aware, and immediately respects runtime reduced-motion changes at 320, 768, and 1440.
    - The migrated Episodes server boundary places the client query consumer below Suspense and retains the legacy/migrated server-only variant seam.
  </behavior>
  <action>Write focused tests first, then create the typed public URL-state parser and push/replace helpers, the D-03 native ScrollRail primitive, and the migrated Episodes server/Suspense boundary. Per D-01 and D-02, keep `/episodes` canonical, accept only allowlisted public video IDs, preserve unrelated query parameters, and define explicit in-page/direct-load close semantics plus the focus-return token consumed by Plan 01-11. Per D-03, retain native overflow as the authority; add labelled previous/next controls, keyboard/touch/wheel paths, start/end disabled states, visible focus, and immediate reduced-motion handling without a mandatory drag abstraction. The server boundary must not read client navigation hooks outside the Suspense child and must not expose the variant selector to the URL. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-23 --task 1` so T-01-32 executes from its immutable definition and receives an atomic privacy-safe result entry; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - URL: Back, Forward, refresh, share, close, invalid IDs, duplicates, encoding, and unrelated-query preservation pass against `/episodes`.
    - Route shape: neither `app/episodes/[episodeId]` nor any equivalent path-segment route exists.
    - Accessibility: rail name, controls, keyboard order, visible focus, reduced motion, and deterministic drawer focus-return token are asserted.
    - Responsive: 320, 768, and 1440 retain page reflow; only the labelled rail owns horizontal overflow.
    - Architecture: the server/Suspense boundary compiles and the selector remains server-only.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-23 --task 1</automated>
    <expected>Exit 0; build and focused browser proof confirm the URL-state, route-shape, ScrollRail, Suspense, and focus contracts.</expected>
  </verify>
  <done>Plan 01-11 can integrate the public EpisodeDrawer against a complete, typed, browser-proven navigation foundation. Result fragment: T-01-32 is recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Canonical `/episodes` URL meaning, public episode identifiers, navigation history, keyboard/focus access, and server/client rendering boundary.

## Trust Boundaries
| Boundary | Description |
|---|---|
| URL query → public ID parser | Untrusted IDs, duplicates, and encoding must not select private/arbitrary data or corrupt unrelated parameters. |
| input device → native ScrollRail | Pointer, keyboard, touch, wheel, and reduced-motion users require equivalent bounded navigation. |
| server page → client query consumer | Client navigation hooks must remain below Suspense without changing the public route. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-23.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-32 | Spoofing/Tampering/Denial of Service | Episodes URL-state, ScrollRail, and server boundary | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 1 | cd web &amp;&amp; npm run build &amp;&amp; npm run test:browser -- tests/journeys/url-state.spec.ts &amp;&amp; node -e 'const fs=require("fs"); if(fs.existsSync("app/episodes/[episodeId]")\|\|fs.existsSync("app/episodes/:id")) process.exit(1)' | defined | Allowlisted ID parsing, query preservation, native input parity, explicit focus/history semantics, Suspense boundary, and forbidden-route assertion. |
</threat_model>

<verification>
Run the exact threat-owned build and URL-state journey, then confirm Plan 01-11 imports the produced contracts without modifying these foundation files.
</verification>

<success_criteria>
Episodes has a stable URL, native rail, focus, motion, responsive, and Suspense foundation that preserves every locked compatibility boundary and is independently executable.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-23-SUMMARY.md` when done.
</output>
