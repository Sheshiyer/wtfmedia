---
phase: 01-compatibility-component-proof-harness
plan: "22"
type: execute
wave: 13
depends_on: ["01-12"]
files_modified:
  - web/components/patterns/PublicShell.tsx
  - web/components/patterns/brand/MigratedWordmark.tsx
  - web/components/patterns/brand/SignatureSparkle.tsx
  - web/components/patterns/brand/PausableMarquee.tsx
  - web/components/patterns/brand/OptionalPointerAccent.tsx
  - web/components/patterns/brand/BrandMotion.stories.tsx
  - web/tests/journeys/brand-motion.spec.ts
  - web/tests/security/phase1-threat-results/01-22.json
autonomous: true
requirements: [DSYS-02, DSYS-04, DSYS-06, DSYS-07, DSYS-08, DSYS-10, QUAL-03, QUAL-04]
must_haves:
  truths:
    - "Migrated brand effects are separate components scoped beneath the migrated shell; original Wordmark, Sparkle, Marquee, and CustomCursor remain untouched for legacy rendering."
    - "Signature effects are purposeful, pausable, native-cursor preserving, and stop immediately for reduced motion, forced colors, coarse pointers, or touch."
  artifacts:
    - path: "web/components/patterns/brand/PausableMarquee.tsx"
      provides: "Migrated-only pausable marquee with static reduced-motion equivalent"
    - path: "web/tests/journeys/brand-motion.spec.ts"
      provides: "Focused motion, pause, cursor-fallback, and legacy non-regression proof"
  key_links:
    - from: "web/components/patterns/PublicShell.tsx"
      to: "web/components/patterns/brand/*"
      via: "imports reachable only from the migrated shell branch"
---

<objective>
Migrate WTF wordmark and signature motion as an isolated migrated-shell slice after the shared shell boundary is proven.

Purpose: Split rollback-sensitive brand motion from shell extraction and preserve every legacy shared presentation dependency byte-for-byte.
Output: Migrated-only brand components, shell wiring, deterministic stories, and focused browser evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/components/patterns/PublicShell.tsx
@web/components/Wordmark.tsx
@web/components/Sparkle.tsx
@web/components/Marquee.tsx
@web/components/CustomCursor.tsx
@web/tests/contracts/legacy-presentation-dependencies.json
@web/tests/visual/legacy/phase1-legacy-baseline.manifest.json
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@PRODUCT.md
</context>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-22 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-22.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-22, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create migrated-only canonical wordmark and decorative sparkle</name>
  <files>web/components/patterns/brand/MigratedWordmark.tsx, web/components/patterns/brand/SignatureSparkle.tsx, web/components/patterns/brand/BrandMotion.stories.tsx</files>
  <read_first>web/components/Wordmark.tsx; web/components/Sparkle.tsx; web/components/patterns/PublicShell.tsx; approved legacy dependency graph and baseline manifest; 01-UI-SPEC.md Visual Direction, Color, Typography, Motion Contract, and Shared Public Shell Contract; PRODUCT.md canonical wordmark/palette/voice</read_first>
  <behavior>
    - Wordmark preserves canonical order, extrusion, committed colors, home-link semantics, and accessible text.
    - Sparkle is decorative, bounded, aria-hidden, token driven, and static/absent under reduced motion.
    - Legacy Wordmark and Sparkle hashes stay equal to the approved graph.
  </behavior>
  <action>Per D-07, create new migrated-only components; do not edit or re-export over original legacy dependencies. Add focused deterministic stories proving structure, tokens, names, reduced motion, and legacy hash equality.</action>
  <acceptance_criteria>
    - Wordmark: canonical order, extrusion, committed colors, home-link semantics, and accessible text match the approved brand contract.
    - Sparkle: decoration is bounded, `aria-hidden`, token-driven, and static or absent under reduced motion.
    - Isolation: original Wordmark/Sparkle bytes and approved legacy hashes remain identical; only migrated modules import the new components.
    - Evidence: deterministic focused stories pass without changing any legacy baseline.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm run test:components -- BrandMotion --grep "wordmark|sparkle" &amp;&amp; node scripts/capture-legacy-presentation.mjs --check</automated>
    <expected>Exit 0; canonical migrated wordmark/sparkle stories pass and every approved legacy dependency hash remains byte-identical.</expected>
  </verify>
  <done>Migrated marks are proven while legacy shared assets remain byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pausable marquee and optional pointer accent with immediate fallbacks</name>
  <files>web/components/patterns/brand/PausableMarquee.tsx, web/components/patterns/brand/OptionalPointerAccent.tsx, web/components/patterns/brand/BrandMotion.stories.tsx</files>
  <read_first>web/components/Marquee.tsx; web/components/CustomCursor.tsx; web/app/globals.css current cursor/motion rules; web/components/patterns/PublicShell.tsx; approved legacy dependency graph/baseline; 01-CONTEXT.md D-08; 01-UI-SPEC.md Global Interaction and Accessibility Contract plus Motion Contract</read_first>
  <behavior>
    - Marquee has visible pause/resume, no duplicated assistive content, and a static complete reduced-motion row.
    - Pointer accent never hides native cursor and disables immediately for all locked preference/device fallbacks.
    - Neither effect carries essential content or interaction.
  </behavior>
  <action>Per D-08, implement migrated-only effects with semantic tokens and runtime capability/media listeners. Extend focused stories with keyboard pause, media changes, forced-color/coarse-pointer fallbacks, and zero required motion. Leave original Marquee and CustomCursor untouched.</action>
  <acceptance_criteria>
    - Marquee: visible keyboard-operable pause/resume exists, assistive content is not duplicated, and reduced motion exposes one complete static row immediately.
    - Pointer: native cursor is never hidden and the accent disables immediately for reduced motion, forced colors, coarse pointer, and touch.
    - Semantics: neither effect contains essential content or owns a required interaction path.
    - Isolation: original Marquee/CustomCursor bytes and all approved legacy hashes remain identical.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm run test:components -- BrandMotion --grep "marquee|pointer" &amp;&amp; node scripts/capture-legacy-presentation.mjs --check</automated>
    <expected>Exit 0; pause and all immediate fallbacks pass while native cursor and every approved legacy dependency remain unchanged.</expected>
  </verify>
  <done>Signature motion is interruptible and optional without mutating legacy effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire migrated brand effects into PublicShell and prove both variants</name>
  <files>web/components/patterns/PublicShell.tsx, web/tests/journeys/brand-motion.spec.ts, web/tests/security/phase1-threat-results/01-22.json</files>
  <read_first>web/components/patterns/PublicShell.tsx; web/components/legacy/public/LegacyPublicShell.tsx; all migrated brand components/stories from Tasks 1-2; web/tests/journeys/public-shell.spec.ts; web/tests/rollback/legacy-presentation.spec.ts; approved legacy dependency graph/baseline; Playwright viewport/media fixtures; 01-CONTEXT.md D-07 and D-08; 01-UI-SPEC.md Shared Public Shell, Motion, Capture, and Phase-Wide Interaction Acceptance</read_first>
  <behavior>
    - Only migrated shell imports new brand components; legacy shell resolves approved originals and styles.
    - Migrated routes pass pause, reduced-motion, native-cursor, focus, viewport, axe, and performance assertions.
    - Legacy mode matches the approved pre-migration baseline at all three widths.
  </behavior>
  <action>Wire new components only into migrated PublicShell. Add browser proof for runtime preferences and no selector leak, then run the complete legacy presentation comparison at 320/768/1440. Capture migrated candidates without replacing approved baselines. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-22 --task 3` so T-01-68, T-01-69, T-01-70 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Wiring: only migrated PublicShell imports the new brand modules; legacy shell resolves the exact approved originals/styles.
    - Interaction: pause, runtime reduced-motion/forced-color/coarse-pointer changes, native cursor, keyboard focus, axe, and 320/768/1440 behavior pass.
    - Rollback: all approved legacy presentation comparisons remain byte/hash exact and no migrated selector leaks to client URL/state.
    - Evidence: `phase1-threat-results/01-22.json` contains exactly passing T-01-68, T-01-69, and T-01-70 entries with immutable owner/command parity.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-22 --task 3</automated>
    <expected>Exit 0; migrated motion/accessibility/privacy proofs pass at all widths, legacy presentation remains exact, and T-01-68..70 pass in the 01-22 fragment.</expected>
  </verify>
  <done>Brand motion is scoped, proven, and cannot alter owner-approved legacy presentation. Result fragment: T-01-68, T-01-69, T-01-70 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| migrated shell → signature effects | Effects must not become essential, inaccessible, or leak into legacy mode. |
| runtime preference → motion/pointer behavior | Preference changes must stop optional effects immediately. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-22.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-68 | Tampering | legacy shared dependencies | mitigate | high | medium | high | v5.0.0-2.1.1 | Task 3 | cd web &amp;&amp; node scripts/capture-legacy-presentation.mjs --check &amp;&amp; npm run test:browser -- tests/rollback/legacy-presentation.spec.ts | defined | New migrated modules only; approved legacy hashes remain exact. |
| T-01-69 | Denial of Service | motion/pointer access | mitigate | high | medium | high | v5.0.0-3.2.2 | Task 3 | cd web &amp;&amp; npm run test:browser -- tests/journeys/brand-motion.spec.ts | defined | Pause, preference fallbacks, native cursor, keyboard and viewport proof. |
| T-01-70 | Information Disclosure | shell/variant artifacts | mitigate | high | low | high | v5.0.0-15.3.1 | Task 3 | cd web &amp;&amp; npm run test:privacy -- --check | defined | Migrated-only imports, server selector, DOM/bundle/artifact scan. |
</threat_model>

<verification>
Run focused stories, migrated behavior, privacy scan, and complete 320/768/1440 legacy comparison.
</verification>

<success_criteria>
Brand motion is a bounded migrated-shell slice with exact legacy non-regression and focused interaction evidence.
</success_criteria>

<output>
Create .planning/phases/01-compatibility-component-proof-harness/01-22-SUMMARY.md when done.
</output>
