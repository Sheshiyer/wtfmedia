---
phase: 01-compatibility-component-proof-harness
plan: "19"
type: execute
wave: 20
depends_on: ["01-18"]
files_modified:
  - web/tests/visual/phase1-approval.json
  - .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
  - web/tests/security/phase1-threat-results/01-19.json
autonomous: false
requirements: [COMP-01, DSYS-02, DSYS-03, QUAL-04, QUAL-06]
must_haves:
  truths:
    - "The owner reviews every route/shared-component candidate at 320/768/1440 and explicitly approves or rejects WTF identity, provisional production-token use, and baseline replacement."
    - "The owner reviews the phase-wide automated result, budget comparisons, and rollback rehearsal before authorizing cutover."
  artifacts:
    - path: "web/tests/visual/phase1-approval.json"
      provides: "Hash-bound owner authority for visual baseline promotion and migrated-default cutover"
  key_links:
    - from: "web/tests/visual/phase1-candidate.json"
      to: "web/tests/visual/phase1-approval.json"
      via: "exact candidate hash set and explicit owner reference"
---

<objective>
Obtain the two locked human authorities: visual baseline replacement and final rollback/cutover approval.

Purpose: Satisfy D-14/D-16 without letting deterministic pixels self-approve brand quality or release authority.
Output: Owner-reviewed hash-bound approval artifact and validation checkpoint record.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/tests/visual/phase1-candidate.json
@web/tests/performance/phase1-budgets.json
@web/tests/contracts/phase1-baseline-approval.json
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
</context>

<must_haves>
  <truths>No approved snapshot, migrated default, or final validation flag changes before explicit owner approval.</truths>
  <artifacts>Approval binds every candidate hash, required state/viewport coverage, approved budget result, rollback result, production-token disposition, and cutover authorization.</artifacts>
  <key_links>Plan 01-20 must reject approval whose candidate/baseline/protected hash or coverage differs.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-19 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-19.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-19, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto">
  <name>Task 1: Validate the owner review packet is complete and internally consistent</name>
  <files>.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md, web/tests/security/phase1-threat-results/01-19.json</files>
  <read_first>web/tests/visual/phase1-candidate.json; 01-UI-SPEC.md full required state/capture matrices; web/tests/performance/phase1-budgets.json; rollback results; 01-VALIDATION.md</read_first>
  <action>Before presenting anything to the owner, run candidate mode through the deterministic merger's check-only path with the literal completed fragment set for Plans 01-01 through 01-18 plus completed dependency Plans 01-21 through 01-23. Exempt only the not-yet-recorded current/future approval/finalization threats T-01-58 through T-01-64; do not request nonexistent 01-19/01-20 fragments, infer plan ranges at runtime, or write the aggregate ledger. Schema-check the candidate manifest against every required route, changed shared component, applicable state, and exact viewport; verify every automated gate except approval is green, performance results use the approved budget hash, rollback covers both variants/all protected paths, and privacy scan is zero. Update the manual-verification mappings with Plan `01-19`, Wave 20, packet hash, and T-01-58/T-01-59, while retaining pending approval and false completion flags. Run a parser assertion across every concrete plan/wave mapping in `01-VALIDATION.md`: derive each plan's declared wave from its PLAN frontmatter and fail on any mismatch, including requiring Plan 01-19 to map to Wave 20. Finally invoke `node web/scripts/run-phase1-threat.mjs --plan 01-19 --task 1` so T-01-58 runs after candidate prerequisites are satisfied and is atomically added only to `phase1-threat-results/01-19.json`; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Coverage: zero missing/duplicate route-component-state-viewport entries.
    - Integrity: candidate, baseline, approved-budget, protected-source, and rollback hashes agree.
    - Behavior: no automated failure is hidden behind the human checkpoint.
    - Validation: owner approval remains pending, completion flags remain false, and every concrete recorded plan wave equals PLAN frontmatter.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-19 --task 1</automated>
    <expected>Exit 0; packet is complete, every automated gate passes, and human approval is the only remaining gate.</expected>
  </verify>
  <done>The owner sees a complete, consistent, privacy-safe packet rather than an incomplete visual sample. Result fragment: T-01-58 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve visual baselines, rollback, and migrated cutover</name>
  <files>web/tests/visual/phase1-approval.json, .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md, web/tests/security/phase1-threat-results/01-19.json</files>
  <read_first>web/tests/visual/phase1-approval.json and .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md; web/tests/visual/phase1-candidate.json; all candidate images referenced by it; performance comparison; rollback result; 01-UI-SPEC.md visual/copy/color/type/spacing contracts; PRODUCT.md; DESIGN.md; 01-CONTEXT.md D-14 and D-16</read_first>
  <action>Present the exact candidate matrix grouped by `/`, `/episodes`, `/connections`, `/chat`, and shared components at 320/768/1440, including applicable focus/loading/empty/error/open-overlay/reduced-motion states. Present approved-budget comparisons, complete `/api/chat` result, privacy result, and both-variant rollback rehearsal. Ask the owner to explicitly approve: (1) WTF identity/editorial quality; (2) any rendered production-orange use, or confirm none is rendered; (3) exact candidate hash set as replacement baselines; (4) rollback sufficiency; and (5) migrated-default cutover. On approval only, write `phase1-approval.json` with all hashes/coverage, owner reference, decisions, and `status: approved`; update only the manual approval rows in Validation. Do not yet promote snapshots, change selector default, or set final completion flags. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-19 --task 2` so T-01-59, T-01-60 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Human: owner explicitly approves all five named decisions or execution remains paused with issues recorded.
    - Source: approval binds the exact full candidate hash set and all source/budget/rollback hashes.
    - Behavior: any later pixel/hash/coverage drift invalidates approval.
    - Validation: visual/rollback approval rows may be green; Wave 0/Nyquist remain false until final aggregate.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-19 --task 2</automated>
    <expected>After explicit owner approval, exit 0; otherwise execution remains paused and no baseline/cutover occurs.</expected>
  </verify>
  <done>The owner has approved the exact visuals, rollback, and final migrated cutover without delegating aesthetic authority to pixel tests. Result fragment: T-01-59, T-01-60 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Brand authority, approved visual hashes, performance/rollback evidence, and cutover decision provenance.

## Trust Boundaries
| Boundary | Description |
|---|---|
| automated candidate evidence → human approval | Incomplete or mismatched evidence can mislead owner judgment. |
| owner decision → final promotion/cutover | Approval must bind exact artifacts and cannot be replayed after drift. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-19.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-58 | Tampering | visual approval and validation-wave mapping | mitigate | high | medium | high | v5.0.0-2.1.1 | Task 1 | cd web &amp;&amp; npm run verify:phase1 -- --candidate &amp;&amp; npm run test:privacy -- --check &amp;&amp; node scripts/verify-phase1.mjs --check-validation-waves | defined | Full matrix schema, exact hashes, source/budget/rollback binding, Plan 01-19 Wave 20, all-recorded-wave parser parity, and drift invalidation. |
| T-01-59 | Repudiation | owner/cutover authority | mitigate | high | low | high | v5.0.0-2.1.1 | Task 2 | node -e 'const a=require("./web/tests/visual/phase1-approval.json"),c=require("./web/tests/visual/phase1-candidate.json"); if(a.status!=="approved"\|\|a.candidate_sha256!==c.sha256\|\|!a.owner_approval_ref\|\|a.cutover_authorized!==true\|\|a.rollback_approved!==true) process.exit(1)' | defined | Explicit five-part approval reference and durable status; no implicit/auto approval. |
| T-01-60 | Information Disclosure | review packet | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 2 | node -e 'const a=require("./web/tests/visual/phase1-approval.json"),c=require("./web/tests/visual/phase1-candidate.json"); if(a.status!=="approved"\|\|a.candidate_sha256!==c.sha256\|\|!a.owner_approval_ref\|\|a.cutover_authorized!==true\|\|a.rollback_approved!==true) process.exit(1)' | defined | Privacy scan before review; public-safe filenames/metadata; no raw queries/sources/machine paths. |
</threat_model>

<verification>
Run candidate aggregate/privacy/schema checks, then validate the explicit owner approval artifact without promoting or cutting over.
</verification>

<success_criteria>
Visual identity, production-token disposition, rollback, and cutover are explicitly owner-approved and hash-bound to the complete passing candidate packet.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-19-SUMMARY.md` when done.
</output>
