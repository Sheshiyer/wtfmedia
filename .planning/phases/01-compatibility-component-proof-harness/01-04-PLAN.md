---
phase: 01-compatibility-component-proof-harness
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - web/eslint.config.mjs
  - web/vitest.config.ts
  - web/.storybook/main.ts
  - web/.storybook/preview.ts
  - web/stories/fixtures/public.ts
  - web/stories/HarnessSmoke.stories.tsx
  - web/playwright.config.ts
  - web/lighthouserc.cjs
  - web/tests/security/phase1-threat-results/01-04.json
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-06, DSYS-10]
must_haves:
  truths:
    - "Lint, node contracts, browser component stories, route journeys, screenshots, and performance collection each have an explicit non-watch configuration."
    - "All browser evidence uses pinned Chromium, deterministic public fixtures, stable paths, and 320/768/1440 viewport coverage."
  artifacts:
    - path: "web/vitest.config.ts"
      provides: "Separated unit, contracts, and Storybook browser projects"
    - path: "web/playwright.config.ts"
      provides: "Production-like public-route browser harness"
    - path: "web/.storybook/preview.ts"
      provides: "Global deterministic decorators and a11y error mode"
  key_links:
    - from: "web/stories/fixtures/public.ts"
      to: "Storybook and Playwright evidence"
      via: "synthetic public-only deterministic fixtures"
---

<objective>
Create the deterministic proof-harness configuration and one smoke path before application migration.

Purpose: Separate tool/config failures from later component behavior failures and establish the Nyquist execution substrate.
Output: ESLint, Vitest, Storybook, Playwright, Lighthouse configurations and a passing browser smoke story.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-compatibility-component-proof-harness/01-RESEARCH.md
@.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
@.planning/phases/01-compatibility-component-proof-harness/01-PATTERNS.md
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@web/package.json
@web/tsconfig.json
@video/eslint.config.mjs
</context>

<must_haves>
  <truths>Every planned automated command has a real configuration file and the smoke story executes in Chromium.</truths>
  <artifacts>Fixture decorators freeze locale/time and prevent live network; Storybook a11y is error mode; Playwright uses a production-like webServer and stable snapshot roots.</artifacts>
  <key_links>Vitest's Storybook project consumes the same `.storybook` configuration that reviewers see, rather than a separate component renderer.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-04 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-04.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-04, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto">
  <name>Task 1: Configure direct ESLint and separated Vitest projects</name>
  <files>web/eslint.config.mjs, web/vitest.config.ts</files>
  <read_first>web/vitest.config.ts if present (otherwise treat it as a create target); web/package.json; web/tsconfig.json; video/eslint.config.mjs; 01-RESEARCH.md Test Framework and Proposed Script Contract; 01-PATTERNS.md Harness configuration</read_first>
  <action>Create an ESM flat ESLint 9 configuration using the installed Next 15 flat-compatible rules, with generated directories ignored and zero-warning scripts. Configure Vitest 4 projects named `unit`, `contracts`, and `storybook`; node projects include only repository-owned test paths, while the Storybook project uses the installed Playwright browser provider and pinned Chromium. Preserve the `@/` alias and TypeScript strict behavior. Do not introduce jsdom, Jest, legacy Storybook test-runner, or watch defaults.</action>
  <acceptance_criteria>
    - Source: project names are exactly `unit`, `contracts`, and `storybook` with non-overlapping includes.
    - Behavior: node projects do not load browser-only stories; Storybook project uses browser mode.
    - Command: ESLint configuration loads and Vitest lists projects without starting watch mode.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm run lint -- --help &gt;/dev/null &amp;&amp; npm exec vitest -- --config vitest.config.ts --list</automated>
    <expected>Exit 0; ESLint and all three named projects load without config/peer errors or watch processes.</expected>
  </verify>
  <done>The code-quality and unit/contract/component test environments are explicit and isolated.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Storybook with deterministic public fixtures and a11y blocking</name>
  <files>web/.storybook/main.ts, web/.storybook/preview.ts, web/stories/fixtures/public.ts, web/stories/HarnessSmoke.stories.tsx, web/tests/security/phase1-threat-results/01-04.json</files>
  <read_first>web/.storybook/main.ts, web/.storybook/preview.ts, web/stories/fixtures/public.ts, and web/stories/HarnessSmoke.stories.tsx if present (otherwise treat absent paths as create targets); 01-UI-SPEC.md Deterministic Component and Visual Evidence and Privacy-Safe Public Projection Contract; 01-RESEARCH.md Storybook recommendations; web/lib/episodes.ts; web/lib/connections.ts; web/app/globals.css</read_first>
  <action>Configure `@storybook/nextjs-vite` with only repository story globs, the Vitest and a11y addons, global import of application styles, and `parameters.a11y.test = "error"`. Add reusable synthetic public-shaped fixtures containing no real transcript text, secrets, private links, model prompts, operator fields, or absolute paths. Freeze clock, locale, timezone, image/font requests, and fixture order through decorators/parameters. Create one `HarnessSmoke` story that exercises native button/link/focus semantics and deterministic state without inventing a speculative product component. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-04 --task 2` so T-01-09, T-01-11 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: Storybook uses the Next/Vite framework and both required addons.
    - Privacy: fixture keys are a subset of the approved public contracts and contain no forbidden-field names.
    - Behavior: smoke story renders, its interaction completes by keyboard, and a11y error mode reports no serious/critical violation.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-04 --task 2</automated>
    <expected>Exit 0; the smoke story renders and its interaction/a11y checks pass in pinned Chromium.</expected>
  </verify>
  <done>Storybook is a deterministic, privacy-safe browser proof surface rather than a decorative catalogue. Result fragment: T-01-09, T-01-11 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto">
  <name>Task 3: Configure production-like Playwright and repeated Lighthouse collection</name>
  <files>web/playwright.config.ts, web/lighthouserc.cjs, web/tests/security/phase1-threat-results/01-04.json</files>
  <read_first>web/playwright.config.ts and web/lighthouserc.cjs if present (otherwise treat them as create targets); 01-RESEARCH.md Route/E2E/visual framework, Visual Evidence Matrix, and Performance Baseline gate; 01-UI-SPEC.md Capture Contract; web/package.json; web/next.config.* if present</read_first>
  <action>Configure Playwright Test with one blocking `phase1-chromium` project, a production-like `npm run build` plus `npm run start` webServer, deterministic port/baseURL, trace-on-retry, stable repository-relative snapshot paths, and parameterized 320, 768, and 1440 viewport data. Disallow live external resources in blocking tests unless intercepted by a fixture. Configure Lighthouse CI for filesystem-only reports and five runs per protected UI route; do not write numeric assertions yet. Ensure neither config contacts Vercel, Cloudflare, a hosted visual service, or temporary public upload. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-04 --task 3` so T-01-10, T-01-12 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: all four UI paths are listed for Lighthouse collection with five runs and no numeric budget assertions.
    - Source: Playwright uses pinned Chromium, production server commands, and stable snapshot paths.
    - Behavior: configuration discovery exits without launching watch mode or requiring external credentials.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-04 --task 3</automated>
    <expected>Exit 0; Playwright lists tests/config and Lighthouse defines four routes, five runs, filesystem output, and no invented thresholds.</expected>
  </verify>
  <done>The browser, visual, accessibility, and performance collectors are reproducible and bounded to local deterministic execution. Result fragment: T-01-10, T-01-12 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Deterministic fixtures, browser evidence, accessibility proof, and build/test isolation.

## Trust Boundaries
| Boundary | Description |
|---|---|
| fixture/config → browser runtime | Test data and external requests can leak into DOM, traces, screenshots, or bundles. |
| local server → test runner | Uncontrolled ports/network or stale servers can falsify route evidence. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-04.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-09 | Information Disclosure | fixtures/artifacts | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 2 | cd web &amp;&amp; npm run test:components -- HarnessSmoke &amp;&amp; npm run test:privacy -- --check | defined | ASVS V8: synthetic allowlisted data, blocked external network, repository-relative artifact names, and privacy scan. |
| T-01-10 | Tampering | browser/visual evidence | mitigate | high | medium | high | v5.0.0-2.1.1 | Task 3 | cd web &amp;&amp; npm exec playwright -- test --config=playwright.config.ts --list &amp;&amp; node -e 'const c=require("./lighthouserc.cjs"); if(c.ci.collect.numberOfRuns!==5\|\|c.ci.upload.target!=="filesystem") process.exit(1)' | defined | Pin Chromium/project, freeze environment inputs, use stable snapshots and production-like server lifecycle. |
| T-01-11 | Denial of Service | accessibility | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 2 | cd web &amp;&amp; npm run test:components -- HarnessSmoke | defined | ASVS V4/V5 availability: a11y error mode plus real-browser keyboard/focus configuration; no canvas/pointer-only proof. |
| T-01-12 | Spoofing | stale server/build | mitigate | high | medium | high | v5.0.0-15.2.1 | Task 3 | cd web &amp;&amp; npm exec playwright -- test --config=playwright.config.ts --list &amp;&amp; node -e 'const c=require("./lighthouserc.cjs"); const u=c.ci.collect.url; if(c.ci.collect.numberOfRuns!==5\|\|u.length!==4\|\|c.ci.upload.target!=="filesystem") process.exit(1)' | defined | Playwright owns server readiness and uses freshly built production output on deterministic baseURL. |
</threat_model>

<verification>
Load every config, execute the smoke story in Chromium, list browser tests, and assert Lighthouse's non-numeric collection shape.
</verification>

<success_criteria>
All proof frameworks have real deterministic configurations and a passing smoke path before route/component code changes.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-04-SUMMARY.md` when done.
</output>
