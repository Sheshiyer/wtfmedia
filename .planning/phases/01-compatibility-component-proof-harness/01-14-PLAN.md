---
phase: 01-compatibility-component-proof-harness
plan: "14"
type: execute
wave: 15
depends_on: ["01-12", "01-13"]
files_modified:
  - web/components/ConnectionGraph.tsx
  - web/components/patterns/GraphWithList.tsx
  - web/components/domain/public/MigratedConnectionsPage.tsx
  - web/components/patterns/GraphWithList.stories.tsx
  - web/tests/contracts/connections-parity.test.ts
  - web/tests/journeys/connections.spec.ts
  - web/tests/security/phase1-threat-results/01-14.json
autonomous: true
requirements: [COMP-01, COMP-04, COMP-05, DSYS-04, DSYS-05, DSYS-06, DSYS-07, DSYS-08, DSYS-09, DSYS-10, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]
must_haves:
  truths:
    - "The redesigned `/connections` shows a public canvas and immediately adjacent equivalent semantic list from the exact same projection per D-11."
    - "The canvas is enhancement-only and hidden from assistive technology; the keyboard list exposes every node, edge, source, selection, and detail meaning."
    - "Reduced motion renders a final deterministic layout with zero continuing animation frames; normal motion settles and pauses offscreen."
  artifacts:
    - path: "web/components/patterns/GraphWithList.tsx"
      provides: "Shared-selection canvas plus equivalent semantic public list"
    - path: "web/tests/contracts/connections-parity.test.ts"
      provides: "Exact stable node/edge/source parity proof"
  key_links:
    - from: "web/components/patterns/GraphWithList.tsx"
      to: "web/lib/public/connections.ts"
      via: "single immutable PublicConnectionProjection prop"
---

<objective>
Migrate `/connections` to the approved expressive canvas plus exact semantic alternative.

Purpose: Preserve public evidence while removing canvas/pointer-only denial and preventing operator projection leakage.
Output: Migrated graph/list pattern, deterministic seeded motion, exact parity tests, stories, and route evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/lib/public/connections.ts
@web/components/ConnectionGraph.tsx
@web/components/domain/public/MigratedConnectionsPage.tsx
@web/components/legacy/public/LegacyConnectionsPage.tsx
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@.planning/phases/01-compatibility-component-proof-harness/01-PATTERNS.md
@web/tests/performance/phase1-budgets.json
</context>

<must_haves>
  <truths>D-11, COMP-05, and DSYS-09 pass from one projection; no required meaning depends on hover, pointer, color, canvas, or motion.</truths>
  <artifacts>Stories cover ready, pointer selection, keyboard selection, reduced-motion stable graph, empty, error, and narrow/wide layouts.</artifacts>
  <key_links>Canvas and list update one selected public node/edge detail state without sharing any operator interaction state.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-14 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-14.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-14, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement one-selection GraphWithList and exact semantic parity</name>
  <files>web/components/patterns/GraphWithList.tsx, web/tests/contracts/connections-parity.test.ts, web/tests/security/phase1-threat-results/01-14.json</files>
  <read_first>web/components/patterns/GraphWithList.tsx and web/tests/contracts/connections-parity.test.ts if present (otherwise treat them as create targets); web/lib/public/connections.ts; 01-UI-SPEC.md Connections Composition and Parity; 01-RESEARCH.md Pattern 4; web/app/connections/page.tsx current NodeRow/edge content; web/components/ui/AvailabilityState.tsx</read_first>
  <behavior>
    - Every projection node maps to one list item with identical ID, label, category, count, episode IDs/titles/source links.
    - Every projection edge maps to one semantic relationship with both endpoint IDs/labels and shared count.
    - List buttons/links are 44px, DOM ordered, Enter/Space operable, and update the same visible public detail as canvas selection.
    - Empty/error states retain navigation and expose no operator existence.
  </behavior>
  <action>Create `GraphWithList` with one readonly projection prop and one public selection state. Render the canvas as `aria-hidden` and non-tabbable, followed immediately by a semantic list/relationship region using native disclosure/buttons/links and non-color selected semantics. Write parity tests over rendered and projection stable IDs, edge keys, labels, counts, and source links, plus forbidden-field assertions. Do not maintain separately filtered graph/list arrays or duplicate evidence shaping in the component. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-14 --task 1` so T-01-42 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Command: exact node/edge/source parity passes and fails on an intentionally omitted/mutated item.
    - Accessibility: complete meaning/actions exist with canvas removed/hidden and keyboard only.
    - Security: detail/list props and DOM match the public allowlist only.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-14 --task 1</automated>
    <expected>Exit 0; visual and semantic identity sets are exactly equal and public-only.</expected>
  </verify>
  <done>The semantic alternative is the authoritative accessible representation, not a loose summary. Result fragment: T-01-42 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate canvas lifecycle, deterministic motion, and route composition</name>
  <files>web/components/ConnectionGraph.tsx, web/components/patterns/GraphWithList.tsx, web/components/domain/public/MigratedConnectionsPage.tsx, web/components/patterns/GraphWithList.stories.tsx, web/tests/security/phase1-threat-results/01-14.json</files>
  <read_first>web/components/domain/public/MigratedConnectionsPage.tsx; web/components/ConnectionGraph.tsx; web/components/patterns/GraphWithList.tsx; 01-UI-SPEC.md Connections Interaction/Motion/Width and copy contract; web/styles/tokens.css; web/styles/motion.css</read_first>
  <behavior>
    - Canvas uses semantic colors and deterministic seeded positions, settles then stops, pauses offscreen, and schedules zero RAF work in reduced motion.
    - Canvas pointer selection updates shared detail but no required content appears only on hover/drag.
    - 320/768 stack canvas/list; 1440 may use 3:2 layout while both remain visible.
  </behavior>
  <action>Refactor ConnectionGraph to receive normalized public nodes/edges and controlled selection, use semantic tokens, seed deterministically, stop after settle, pause offscreen, and render final layout immediately for reduced motion. Compose MigratedConnectionsPage with exact public explanation, GraphWithList, details, route links, and complete states. In this task add focused GraphWithList stories that assert selection sharing, semantic-list completeness, stable settle, zero-RAF reduced motion, and 320/768/1440 composition; Task 3 expands the route-wide evidence. Remove unsupported people/company/operator certainty. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-14 --task 2` so T-01-43 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Behavior: normal simulation reaches stable state and stops; reduced-motion RAF counter remains zero.
    - Accessibility: canvas is hidden/noninteractive to assistive technology while list remains complete.
    - Copy/privacy: page says recurring public themes and ideas and contains no internal field or unsupported certainty.
    - Responsive: all three widths preserve both projections and details without clipping/page overflow.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-14 --task 2</automated>
    <expected>Exit 0; migrated route components compile and exact parity remains green.</expected>
  </verify>
  <done>The visual graph is purposeful enhancement over a complete, same-data semantic public projection. Result fragment: T-01-43 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove Connections parity, selection, accessibility, viewports, privacy, budgets, and rollback</name>
  <files>web/components/patterns/GraphWithList.stories.tsx, web/tests/journeys/connections.spec.ts, web/tests/security/phase1-threat-results/01-14.json</files>
  <read_first>web/components/patterns/GraphWithList.stories.tsx and web/tests/journeys/connections.spec.ts if present (otherwise treat them as create targets); web/components/patterns/GraphWithList.tsx; web/components/domain/public/MigratedConnectionsPage.tsx; web/stories/fixtures/public.ts; 01-UI-SPEC.md required Connections evidence; web/tests/performance/phase1-budgets.json</read_first>
  <behavior>
    - Story/journey states cover ready, exact parity, pointer selection, keyboard list selection, reduced-motion zero RAF, empty, safe error, and narrow/wide layouts.
    - Migrated and retained variants both preserve `/connections` and public evidence meaning.
    - Route metrics remain within approved budgets and artifacts contain no forbidden fields.
  </behavior>
  <action>Add deterministic stories and Playwright route journeys at 320/768/1440 with seeded coordinates, explicit canvas/list parity assertions, keyboard disclosure/source navigation, shared selected-detail checks, reduced-motion runtime emulation/RAF instrumentation, serious/critical axe scans, page-overflow checks, public forbidden-field scan, approved performance comparisons, and dual-variant rollback. Capture unapproved visual candidates only and keep selector default legacy. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-14 --task 3` so T-01-44 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Command: stories, parity/contracts, browser, privacy, performance subset, and rollback pass.
    - Accessibility: complete keyboard/list workflow and zero serious/critical axe findings.
    - Motion: reduced-motion route has zero continuing RAF after render; allowed motion settles within tested bound.
    - Visual: candidates cover required states/widths without being marked approved.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-14 --task 3</automated>
    <expected>Exit 0; Connections passes all automated gates in migrated and rollback variants.</expected>
  </verify>
  <done>`/connections` is visibly migrated, semantically equivalent, public-only, accessible, budgeted, and recoverable. Result fragment: T-01-44 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Public graph evidence, semantic equivalence, operator projection boundary, keyboard/screen-reader access, and rendering availability.

## Trust Boundaries
| Boundary | Description |
|---|---|
| immutable projection → canvas/list DOM | Divergent renderers can omit or alter evidence meaning. |
| canvas simulation/pointer → user workflow | Motion, hover, or canvas-only controls can deny access. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-14.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-42 | Information Disclosure | graph/list/detail DOM | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 1 | cd web &amp;&amp; npm run test:contracts -- tests/contracts/connections-parity.test.ts tests/contracts/connections-public-projection.test.ts | defined | Public allowlist, same projection, forbidden-field and artifact scans; no operator imports/state. |
| T-01-43 | Tampering | renderer parity/selection | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 2 | cd web &amp;&amp; npm run test:components -- GraphWithList --grep "selection\|settle\|reduced\|responsive" &amp;&amp; npm run test:contracts -- tests/contracts/connections-parity.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint | defined | Exact stable ID/edge/source parity and shared controlled public selection state. |
| T-01-44 | Denial of Service | canvas/motion/accessibility | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 3 | cd web &amp;&amp; npm run test:components -- GraphWithList &amp;&amp; npm run test:browser -- tests/journeys/connections.spec.ts &amp;&amp; npm run test:privacy -- --check &amp;&amp; npm run test:rollback -- --route=/connections | defined | Canvas aria-hidden, full semantic list, keyboard targets, zero-RAF reduced mode, settle/offscreen pause, viewport/axe proof. |
</threat_model>

<verification>
Run parity/projection contracts, stories, route journey, privacy, performance subset, and dual-variant rollback.
</verification>

<success_criteria>
Connections is a distinct WTF editorial room whose expressive graph and complete semantic list expose identical public evidence and no operator data.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-14-SUMMARY.md` when done.
</output>
