---
phase: 01-compatibility-component-proof-harness
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/tests/security/phase1-threat-results/01-03.json
autonomous: true
requirements: [QUAL-01, QUAL-03, QUAL-04, QUAL-06, DSYS-04]
must_haves:
  truths:
    - "Only the exact owner-approved dependency versions enter package.json and package-lock.json."
    - "A pinned Chromium revision is available for component, route, accessibility, visual, and performance proof."
  artifacts:
    - path: "web/package.json"
      provides: "Exact Phase 1 proof-harness dependencies and named non-watch scripts"
    - path: "web/package-lock.json"
      provides: "Resolved dependency graph for reproducible local and CI installs"
  key_links:
    - from: "web/tests/package-legitimacy.json"
      to: "web/package.json"
      via: "pre-install exact-set assertion"
---

<objective>
Install the approved proof-harness dependency set in isolation from application migration.

Purpose: Establish a reproducible tool substrate and isolate fresh-package failures before any WTF UI files change.
Output: Exact package/lock changes, named scripts, and a verified pinned Chromium installation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-compatibility-component-proof-harness/01-RESEARCH.md
@web/tests/package-legitimacy.json
@web/package.json
@web/package-lock.json
</context>

<must_haves>
  <truths>Package bootstrap is separated from route/component migration and fails closed on approval drift.</truths>
  <artifacts>Package scripts expose lint, typecheck, unit, contracts, components, browser, a11y, visual, privacy, performance, rollback, and `verify:phase1` as non-watch commands.</artifacts>
  <key_links>The lockfile resolved versions must equal the approved exact versions and contain no unapproved top-level package.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-03 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-03.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-03, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto">
  <name>Task 1: Install the exact approved npm set and declare the proof command surface</name>
  <files>web/package.json, web/package-lock.json, web/tests/security/phase1-threat-results/01-03.json</files>
  <read_first>web/tests/package-legitimacy.json; 01-RESEARCH.md Planned Installation and Proposed Script Contract; web/package.json; web/package-lock.json; AGENTS.md</read_first>
  <action>Before invoking npm, compare the intended twelve name/version pairs byte-for-byte with the approved receipt and exit on any mismatch. From `web`, install `@radix-ui/react-dialog@1.1.23` as an exact dependency and the eleven exact dev dependencies from the receipt using npm's save-exact behavior. Preserve existing Next/React/Tailwind major versions and all current package changes. Add the exact non-watch script names from the research contract: `lint`, `typecheck`, `test:unit`, `test:contracts`, `test:components`, `build:storybook`, `test:browser`, `test:a11y`, `test:visual`, `test:privacy`, `test:performance`, `test:rollback`, and `verify:phase1`; each points to the planned config/runner path and must propagate failure. Do not add TanStack Table, Phosphor, shadcn, Cypress, Jest, Chromatic, or any unapproved package. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-03 --task 1` so T-01-SC-INSTALL execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: package.json uses exact versions for all twelve approved packages and retains Next 15, React 19, TypeScript 5.7, and Tailwind 3.
    - Source: package-lock resolves the same top-level versions and contains no new unapproved direct dependency.
    - Behavior: every Phase 1 script is non-watch and `verify:phase1` points to `node scripts/verify-phase1.mjs`.
    - Security: resolved package metadata contains no unexpected install/postinstall lifecycle script among the twelve direct additions; any finding blocks execution for owner review.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-03 --task 1</automated>
    <expected>Exit 0; the resolved direct set matches the approved receipt and all declared blocking scripts are non-watch.</expected>
  </verify>
  <done>The exact approved tool dependencies and script surface are reproducibly locked without application migration. Result fragment: T-01-SC-INSTALL are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto">
  <name>Task 2: Install and prove the Playwright Chromium revision</name>
  <files>web/package-lock.json, web/tests/security/phase1-threat-results/01-03.json</files>
  <read_first>web/tests/package-legitimacy.json; web/package.json; web/package-lock.json; 01-RESEARCH.md Environment Availability and Fresh-release caution</read_first>
  <action>Run `npm exec playwright install chromium` from `web` only after Task 1 succeeds. Print the resolved versions for Playwright, Vitest, Storybook, Vite, ESLint, and Radix Dialog; run import/CLI smoke probes that do not require the not-yet-created configs. Do not use a globally cached browser as proof and do not run live external application tests. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-03 --task 2` so T-01-07, T-01-08 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Command: Playwright reports an executable Chromium revision compatible with `@playwright/test@1.62.1`.
    - Command: exact package CLIs/imports resolve without peer-dependency errors.
    - Behavior: no application, route, component, planning authority, external service, or deployment file changes.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-03 --task 2</automated>
    <expected>Exit 0 with Playwright 1.62.1, Vitest 4.1.11, Storybook 10.5.9, and all imports resolvable.</expected>
  </verify>
  <done>The approved dependency graph and pinned Chromium can start the harness; no UI migration has begun. Result fragment: T-01-07, T-01-08 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Developer/CI execution environment, lockfile integrity, and repository source.

## Trust Boundaries
| Boundary | Description |
|---|---|
| approved receipt → npm registry | Network-delivered packages must resolve to the exact approved identities and versions. |
| package/browser installer → host cache | Installer code can write executable artifacts outside the repository. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-03.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-SC-INSTALL | Tampering | npm install | mitigate | high | medium | high | v5.0.0-15.1.1 | Task 1 | cd web &amp;&amp; npm ls --depth=0 &amp;&amp; node -e 'const p=require("./package.json"),a=require("./tests/package-legitimacy.json"); for(const x of a.packages){const v=p.dependencies?.[x.name]\|\|p.devDependencies?.[x.name]; if(v!==x.version) process.exit(1)}; if(!p.scripts["verify:phase1"]\|\|Object.values(p.scripts).some(x=&gt;/\bwatch\b/.test(x))) process.exit(1)' | defined | ASVS V14: exact pins, lockfile, receipt comparison, direct-dependency allowlist, and blocking owner gate already passed. |
| T-01-07 | Elevation of Privilege | lifecycle/browser installation | mitigate | high | low | high | v5.0.0-15.1.1 | Task 2 | cd web &amp;&amp; npm exec playwright -- --version &amp;&amp; npm exec vitest -- --version &amp;&amp; npm exec storybook -- --version &amp;&amp; node -e 'require.resolve("@radix-ui/react-dialog"); require.resolve("@axe-core/playwright"); require.resolve("@lhci/cli");' | defined | Inspect direct lifecycle metadata, install only pinned Chromium, and stop on unexpected scripts or peer resolution. |
| T-01-08 | Information Disclosure | npm/CLI output | mitigate | high | medium | high | v5.0.0-15.1.1 | Task 2 | cd web &amp;&amp; npm exec playwright -- --version &amp;&amp; npm exec vitest -- --version &amp;&amp; npm exec storybook -- --version &amp;&amp; node -e 'require.resolve("@radix-ui/react-dialog"); require.resolve("@axe-core/playwright"); require.resolve("@lhci/cli");' | defined | Do not print environment values, npm credentials, or absolute secret paths; ordinary version output only. |
</threat_model>

<verification>
Validate exact direct versions, lockfile resolution, script names, CLI imports, and pinned Chromium availability.
</verification>

<success_criteria>
The approved toolchain is installed exactly and independently of application migration, ready for deterministic harness configuration.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-03-SUMMARY.md` when done.
</output>
