---
phase: 01-compatibility-component-proof-harness
plan: "08"
type: execute
wave: 9
depends_on: ["01-21"]
files_modified:
  - web/styles/tokens.css
  - web/styles/themes.css
  - web/styles/motion.css
  - web/app/globals.css
  - web/tailwind.config.ts
  - web/tests/unit/tokens.test.ts
  - web/tests/security/phase1-threat-results/01-08.json
autonomous: true
requirements: [DSYS-01, DSYS-02, DSYS-03, DSYS-06, DSYS-07, DSYS-08, QUAL-03, QUAL-04, QUAL-06]
must_haves:
  truths:
    - "Repository-owned semantic tokens express the approved WTF palette, typography, spacing, shape, depth, focus, and motion contracts without a competing palette."
    - "Cream/ink remain dominant; production orange is defined once, contrast-tested, and not rendered on public routes without owner visual approval."
    - "Reduced motion and visible focus policies apply inside the migrated presentation boundary while the approved legacy CSS/theme/shared dependency graph remains byte-identical."
  artifacts:
    - path: "web/styles/tokens.css"
      provides: "Canonical semantic token definitions"
    - path: "web/tests/unit/tokens.test.ts"
      provides: "Token uniqueness, contrast, raw-color, and policy assertions"
  key_links:
    - from: "web/tailwind.config.ts"
      to: "web/styles/tokens.css"
      via: "Tailwind aliases resolve through CSS variables rather than duplicate hex values"
    - from: "web/tests/visual/legacy/phase1-legacy-baseline.manifest.json"
      to: "web/app/globals.css"
      via: "approved legacy hashes and 320/768/1440 comparison before and after scoped additions"
---

<objective>
Establish the repository-owned WTF visual and interaction foundation after budget approval.

Purpose: Implement D-05 and D-08 at the token/policy layer before the first Episodes proof slice.
Output: Semantic styles, Tailwind aliases, focus/motion rules, and executable token/contrast tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@DESIGN.md
@PRODUCT.md
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@.planning/phases/01-compatibility-component-proof-harness/01-PATTERNS.md
@web/app/globals.css
@web/tailwind.config.ts
@web/tests/performance/phase1-budgets.json
@web/tests/contracts/legacy-presentation-dependencies.json
@web/tests/visual/legacy/phase1-legacy-baseline.manifest.json
</context>

<must_haves>
  <truths>D-05 controlled hybrid adoption preserves the brand while replacing raw role usage; D-08 motion is purposeful, interruptible, pausable, and reduced immediately.</truths>
  <artifacts>Token schema covers the UI-SPEC's exact colors, four sizes/two weights, spacing set, radii, borders, depth, focus ring, durations, and easing.</artifacts>
  <key_links>All new token/theme/motion rules are rooted under the migrated shell marker; legacy mode continues to resolve the approved global rules and original shared dependencies unchanged.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-08 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-08.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-08, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test the canonical semantic token schema</name>
  <files>web/styles/tokens.css, web/styles/themes.css, web/tests/unit/tokens.test.ts, web/tests/security/phase1-threat-results/01-08.json</files>
  <read_first>web/styles/tokens.css, web/styles/themes.css, and web/tests/unit/tokens.test.ts if present (otherwise treat absent paths as create targets); 01-UI-SPEC.md Design System, Spacing, Typography, Color, Shape and Depth; DESIGN.md sections 7 through 10; PRODUCT.md Visual identity; web/app/globals.css; web/tailwind.config.ts</read_first>
  <behavior>
    - Canvas, foreground, editorial, live, attention, knowledge, information, and production roles are defined once with the approved values.
    - Typography exposes only the UI-SPEC's four interface sizes and 400/700 weights; Fraunces remains editorial-only.
    - Spacing is limited to 4, 8, 16, 24, 32, 48, 64; focus and semantic fill pairs meet WCAG AA.
    - Production orange has one definition, approved foreground/border pairs, and no migrated public consumer before owner visual approval.
  </behavior>
  <action>Per D-05, create canonical CSS custom properties for the exact UI-SPEC semantic roles, typography, spacing, radius, border, depth, z-index, focus, duration, and easing contracts. Keep raw brand values only in the token definition layer and preserve the wordmark exception. Add context mappings without creating operator policy/state. Write tests that parse the files, enforce unique definitions, compute required contrast, reject extra palette values and deprecated spacing/type weights, and ensure no public migrated path consumes `production` yet. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-08 --task 1` so T-01-23 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: all required semantic roles exist exactly once with approved values; no new hex value appears outside approved palette/surface values.
    - Command: body/fill and focus contrast assertions meet WCAG AA.
    - Behavior: the production token exists but a migrated-public consumer scan returns zero until owner visual approval.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-08 --task 1</automated>
    <expected>Exit 0; token uniqueness, palette, typography, spacing, contrast, and production-use gates pass.</expected>
  </verify>
  <done>The design authority is encoded as one testable semantic token source without palette drift. Result fragment: T-01-23 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto">
  <name>Task 2: Wire semantic aliases, visible focus, native cursor, and immediate reduced motion</name>
  <files>web/styles/motion.css, web/app/globals.css, web/tailwind.config.ts, web/tests/security/phase1-threat-results/01-08.json</files>
  <read_first>web/styles/motion.css if present (otherwise treat it as a create target); web/styles/tokens.css; web/styles/themes.css; web/app/globals.css; web/tailwind.config.ts; 01-UI-SPEC.md Global Interaction, Motion, and Responsive Shell; 01-PATTERNS.md current global pitfalls</read_first>
  <action>Import token/theme/motion styles from the existing global entry without rewriting approved legacy declarations. Scope every new alias, focus, target-size, overflow, cursor, transition, and reduced-motion rule beneath the migrated shell marker (for example a server-rendered data-public-ui-variant="migrated" root) so explicit legacy mode continues to use the approved CSS/theme/shared dependency graph byte-for-byte. Tailwind additions may expose CSS-variable aliases only to migrated components and must preserve all legacy theme keys and generated behavior. Remove forbidden cursor/smooth-scroll/transition patterns only from scoped migrated rules; do not mutate their approved legacy definitions. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-08 --task 2` so T-01-24, T-01-25 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: Tailwind semantic aliases contain `var(--...)`, not duplicate hex values.
    - Behavior: `cursor: none`, unconditional `scroll-behavior: smooth`, and `transition: all` are absent from active global/migrated rules.
    - Behavior: migrated reduced-motion rules disable all migrated loops and smooth scrolling; focus-visible is not clipped or color-only.
    - Rollback: legacy dependency hashes and approved screenshots/behavior at 320/768/1440 remain exact.
    - Command: token tests, lint, scoped-style test, and legacy presentation comparison pass; approved performance budgets remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-08 --task 2</automated>
    <expected>Exit 0; active globals use semantic aliases with focus/motion/native-cursor policies and no lint/type errors.</expected>
  </verify>
  <done>The shared visual substrate is accessible, reduced-motion aware, and ready for consumed primitives and Episodes. Result fragment: T-01-24, T-01-25 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Brand identity, contrast/focus accessibility, motion safety, and rendering performance.

## Trust Boundaries
| Boundary | Description |
|---|---|
| design authority → CSS/Tailwind tokens | Raw colors or role drift can misrepresent state and brand. |
| user media/forced-color preferences → rendered UI | Ignored preferences can deny access or induce harmful motion. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-08.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-23 | Tampering | semantic roles/palette | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 1 | cd web &amp;&amp; npm run test:unit -- tests/unit/tokens.test.ts | defined | Token parser enforces approved unique values and migrated-consumer semantic aliases. |
| T-01-24 | Denial of Service | focus/motion/reflow | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 2 | cd web &amp;&amp; npm run test:unit -- tests/unit/tokens.test.ts &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; node scripts/capture-legacy-presentation.mjs --check &amp;&amp; npm run test:browser -- tests/rollback/legacy-presentation.spec.ts | defined | WCAG/ASVS availability: two-layer focus, immediate reduced motion, native cursor, 44px targets, and no page overflow. |
| T-01-25 | Information Disclosure | state color | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 2 | cd web &amp;&amp; npm run test:unit -- tests/unit/tokens.test.ts &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; node scripts/capture-legacy-presentation.mjs --check &amp;&amp; npm run test:browser -- tests/rollback/legacy-presentation.spec.ts | defined | No state relies on color alone; production token is not rendered without owner approval and labelled semantics. |
</threat_model>

<verification>
Run token/contrast tests, lint, and typecheck; scan active globals for forbidden cursor, transition, smooth-scroll, and raw palette patterns.
</verification>

<success_criteria>
The approved WTF system is one semantic, accessible, performance-budgeted substrate ready for the Episodes proof slice.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-08-SUMMARY.md` when done.
</output>
