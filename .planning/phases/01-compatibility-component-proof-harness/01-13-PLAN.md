---
phase: 01-compatibility-component-proof-harness
plan: "13"
type: execute
wave: 14
depends_on: ["01-22"]
files_modified:
  - web/components/legacy/public/LegacyConnectionsPage.tsx
  - web/components/legacy/public/LegacyConnectionGraph.tsx
  - web/components/domain/public/MigratedConnectionsPage.tsx
  - web/app/connections/page.tsx
  - web/lib/public/connections.ts
  - web/tests/contracts/connections-public-projection.test.ts
  - web/tests/security/phase1-threat-results/01-13.json
autonomous: true
requirements: [COMP-01, COMP-02, COMP-04, COMP-05, DSYS-09, QUAL-02, QUAL-05]
must_haves:
  truths:
    - "The accepted Connections page/canvas remains available for rollback before its visual migration."
    - "One immutable PublicConnectionProjection allowlists public nodes/edges/source links and becomes the sole input for both visual and semantic renderers."
    - "No operator permissions, interaction state, tasks, owners, leads, budgets, briefs, health, or production fields enter the projection."
  artifacts:
    - path: "web/lib/public/connections.ts"
      provides: "Single allowlisted public graph/list projection"
    - path: "web/components/legacy/public/LegacyConnectionsPage.tsx"
      provides: "Runnable prior Connections experience"
  key_links:
    - from: "web/lib/connections.ts"
      to: "web/lib/public/connections.ts"
      via: "explicit normalization and field allowlist"
---

<objective>
Retain Connections rollback and establish the exact same-data public projection before redesigning graph/list renderers.

Purpose: Make D-11's privacy and semantic-equivalence boundary structural and testable.
Output: Legacy variants, server wrapper, typed immutable public projection, and projection/security contracts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/app/connections/page.tsx
@web/components/ConnectionGraph.tsx
@web/lib/connections.ts
@web/lib/public/contracts.ts
@web/lib/public/public-ui-variant.ts
@.planning/phases/01-compatibility-component-proof-harness/01-CONTEXT.md
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
</context>

<must_haves>
  <truths>D-11 is implemented at the data boundary before UI: both renderers must receive one object identity/serialized projection and cannot import raw/future operator DTOs.</truths>
  <artifacts>The projection exposes stable node ID, label, category, episode count/IDs/public links and edge endpoints/shared count only.</artifacts>
  <key_links>The page wrapper uses the same server-only selector/default-legacy policy established for Episodes.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-13 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-13.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-13, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto">
  <name>Task 1: Retain exact accepted Connections page and graph</name>
  <files>web/components/legacy/public/LegacyConnectionsPage.tsx, web/components/legacy/public/LegacyConnectionGraph.tsx</files>
  <read_first>web/components/legacy/public/LegacyConnectionsPage.tsx and web/components/legacy/public/LegacyConnectionGraph.tsx if present (otherwise treat them as create targets); web/tests/contracts/phase1-baseline-approval.json; web/app/connections/page.tsx; web/components/ConnectionGraph.tsx; web/lib/connections.ts; 01-CONTEXT.md D-11, D-12, D-16</read_first>
  <action>Copy the owner-approved current Connections page and ConnectionGraph into named legacy modules, adjusting imports/exports only for compilation. Preserve current public theme/idea meaning, dataset filtering, node/edge/source link behavior, canvas selection, and supporting content. Bind normalized retained content to approved source hashes. Do not add accessibility/privacy fixes to legacy modules or expose new fields.</action>
  <acceptance_criteria>
    - Source: normalized legacy modules match approved current source except recorded wiring.
    - Behavior: retained variant renders current graph/list-supporting content from current public data.
    - Privacy: no new data or future operator import is introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm run typecheck &amp;&amp; node scripts/capture-phase1-baseline.mjs --check</automated>
    <expected>Exit 0; retained Connections modules compile and approved sources remain unchanged.</expected>
  </verify>
  <done>The prior Connections experience is an exact runnable rollback target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize one immutable allowlisted public Connections projection</name>
  <files>web/lib/public/connections.ts, web/tests/contracts/connections-public-projection.test.ts, web/tests/security/phase1-threat-results/01-13.json</files>
  <read_first>web/tests/contracts/connections-public-projection.test.ts if present (otherwise treat it as a create target); web/lib/connections.ts; web/lib/public/contracts.ts; 01-UI-SPEC.md `/connections` Composition and Parity plus Privacy-Safe projection; 01-RESEARCH.md Pattern 4; REQUIREMENTS.md COMP-04, COMP-05, DSYS-09</read_first>
  <behavior>
    - Every graph node/edge has a stable public normalized entry and public source links derived from accepted data.
    - Projection contains no operator/private/permission/interaction field and is immutable to renderers.
    - Projection validation rejects unsafe/missing IDs, endpoints outside node set, duplicate keys, and unsafe source URLs.
  </behavior>
  <action>Define `PublicConnectionProjection`, `PublicConnectionNode`, and `PublicConnectionEdge` types and one normalization function over the existing public `ConnectionsData`. Include only the D-11/UI-SPEC allowlist and deterministic ordering/seed material. Freeze or treat returned arrays as readonly so canvas/list selection cannot mutate shared evidence. Test exact stable ID/edge-key/source-link sets, endpoint integrity, allowlist keys, immutability, and forbidden-field rejection. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-13 --task 2` so T-01-39, T-01-40 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: exported public types contain only approved keys and readonly collections.
    - Behavior: contract test proves exact node/edge/source identity and rejects duplicates/dangling endpoints.
    - Security: forbidden-field and unsafe URL fixtures fail; no operator module import exists.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-13 --task 2</automated>
    <expected>Exit 0; one immutable, valid, public-only projection is ready for both renderers.</expected>
  </verify>
  <done>Connections privacy and same-data parity are enforced before canvas/list implementation. Result fragment: T-01-39, T-01-40 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add dual-variant Connections route seam</name>
  <files>web/components/domain/public/MigratedConnectionsPage.tsx, web/app/connections/page.tsx, web/tests/contracts/connections-public-projection.test.ts, web/tests/security/phase1-threat-results/01-13.json</files>
  <read_first>web/components/domain/public/MigratedConnectionsPage.tsx if present (otherwise treat it as a create target); web/tests/contracts/connections-public-projection.test.ts; web/components/legacy/public/LegacyConnectionsPage.tsx; web/lib/public/connections.ts; web/lib/public/public-ui-variant.ts; web/app/connections/page.tsx; web/tests/contracts/public-routes.contract.test.ts</read_first>
  <behavior>
    - Both variants serve `/connections` and preserve public navigation/copy/data meaning.
    - Candidate branch is fully functional and consumes only PublicConnectionProjection.
    - Server-only selector is absent from browser artifacts and defaults legacy before acceptance.
  </behavior>
  <action>Create a fully functional behavior-equivalent `MigratedConnectionsPage` that consumes the normalized public projection and current renderers, then make the App Router page a server wrapper selecting legacy/candidate modules via `publicUiVariant()`. Keep `/connections` unchanged and do not create operator graph state or a new API. Extend contract tests to start both variants and assert the same public projection identity/allowlist. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-13 --task 3` so T-01-41 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Behavior: both variants return HTTP 200 and equivalent normalized public IDs/source links.
    - Security: selector and forbidden operator fields are absent from client output.
    - Command: public route and projection contracts pass for both variants.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-13 --task 3</automated>
    <expected>Exit 0; the retained and candidate `/connections` routes are compatible and public-only.</expected>
  </verify>
  <done>Connections has a safe rollback seam and a single tested evidence projection ready for visual/list migration. Result fragment: T-01-41 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Public connection evidence, stable IDs/source links, operator/private field boundary, and rollback availability.

## Trust Boundaries
| Boundary | Description |
|---|---|
| raw repository graph → public projection | Future/private fields or malformed relations must be omitted/rejected. |
| shared projection → canvas/list state | Renderer interaction state must not mutate or diverge evidence truth. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-13.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-39 | Information Disclosure | public projection | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 2 | cd web &amp;&amp; npm run test:contracts -- tests/contracts/connections-public-projection.test.ts &amp;&amp; npm run test:privacy -- --check | defined | ASVS V4/V8: explicit allowlist, forbidden-field tests, no operator imports, artifact scan. |
| T-01-40 | Tampering | node/edge identity | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 2 | cd web &amp;&amp; npm run test:contracts -- tests/contracts/connections-public-projection.test.ts &amp;&amp; npm run test:privacy -- --check | defined | Deterministic readonly projection, duplicate/dangling endpoint rejection, exact ID/source tests. |
| T-01-41 | Denial of Service | rollback | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 3 | cd web &amp;&amp; npm run build &amp;&amp; npm run test:contracts -- tests/contracts/connections-public-projection.test.ts tests/contracts/public-routes.contract.test.ts | defined | Exact legacy retention and both variants build/serve one unchanged URL without data mutation. |
</threat_model>

<verification>
Typecheck retained files, run projection integrity/privacy tests, build both variants, and run protected route contracts.
</verification>

<success_criteria>
Connections is ready for redesign with exact rollback and one non-leaking immutable public projection shared by all representations.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-13-SUMMARY.md` when done.
</output>
