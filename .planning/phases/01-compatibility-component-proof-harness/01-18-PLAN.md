---
phase: 01-compatibility-component-proof-harness
plan: "18"
type: execute
wave: 19
depends_on: ["01-17"]
files_modified:
  - web/scripts/verify-phase1.mjs
  - web/scripts/lib/phase1-threat-results.mjs
  - web/tests/rollback/verify.mjs
  - .github/workflows/phase1.yml
  - web/.gitignore
  - .gitignore
  - web/tests/visual/phase1-candidate.json
  - .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
  - web/tests/security/phase1-threat-results/01-18.json
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]
must_haves:
  truths:
    - "`cd web && npm run verify:phase1` is the single blocking local/CI command and propagates every child failure per D-13."
    - "Rollback builds/smokes retained and migrated variants across all four routes and `/api/chat` without data, deployment, or external-service mutation."
    - "A complete privacy-safe visual review packet exists, but the ordinary aggregate remains blocked until owner approval."
  artifacts:
    - path: "web/scripts/verify-phase1.mjs"
      provides: "Static ordered fail-fast Phase 1 aggregate"
    - path: "web/tests/rollback/verify.mjs"
      provides: "All-route dual-variant build/smoke rehearsal"
    - path: ".github/workflows/phase1.yml"
      provides: "CI invocation of the same locked aggregate"
  key_links:
    - from: ".github/workflows/phase1.yml"
      to: "web/scripts/verify-phase1.mjs"
      via: "`cd web && npm run verify:phase1` only"
---

<objective>
Wire every proof into one fail-fast command, rehearse rollback, and assemble the owner visual/cutover review packet.

Purpose: Make D-13/D-14/D-16 operational without self-approving visuals or mutating external systems.
Output: Aggregate runner, rollback rehearsal, CI workflow, ignore policy, candidate review manifest, and near-final validation state.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/package.json
@.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
@web/tests/visual/public-routes.spec.ts
@web/tests/performance/phase1-budgets.json
@web/lib/public/public-ui-variant.ts
@web/tests/contracts/phase1-baseline-approval.json
@web/tests/contracts/legacy-presentation-dependencies.json
@web/tests/visual/legacy/phase1-legacy-baseline.manifest.json
@.planning/phases/01-compatibility-component-proof-harness/01-CONTEXT.md
</context>

<must_haves>
  <truths>Candidate mode may prepare review evidence, but the unqualified locked command must fail when owner visual approval is missing or hashes differ.</truths>
  <artifacts>Review manifest contains route/state/viewport/component hashes, browser/container identity, fixture IDs, baseline/protected hashes, performance results, and rollback result without private content.</artifacts>
  <key_links>Generated Storybook/coverage/browser/Lighthouse outputs are ignored; approved visual manifests/baselines remain explicitly trackable.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-18 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-18.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-18, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement all-route dual-variant rollback rehearsal</name>
  <files>web/tests/rollback/verify.mjs, web/tests/security/phase1-threat-results/01-18.json</files>
  <read_first>web/tests/rollback/verify.mjs if present (otherwise treat it as a create target); all route variant wrappers and legacy modules; web/lib/public/public-ui-variant.ts; web/tests/contracts/phase1-compatibility-manifest.json; 01-CONTEXT.md D-12 and D-16; 01-RESEARCH.md Retained Route Variants</read_first>
  <behavior>
    - Fresh legacy and migrated builds each serve `/`, `/episodes`, `/connections`, `/chat`, and `/api/chat` contract fixtures.
    - Supported URLs/queries/navigation/public fields remain equivalent; no data file changes occur.
    - Rehearsal records build/source hashes and restores process environment/temporary outputs without touching deployment config.
  </behavior>
  <action>Per D-16, create a bounded Node orchestrator that runs fresh builds/servers for `legacy` and `migrated`, invokes the shared route/API contract and smoke matrix against each, records results/hashes, and cleans only its explicit temporary/generated process artifacts. It must compare data-file hashes before/after, never use git reset/checkout/clean/stage/commit, and never call Vercel/Cloudflare/live RAG. Fail immediately on any variant or contract failure. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-18 --task 1` so T-01-57 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Command: both variants pass all five protected paths in one rehearsal.
    - Integrity: repository public data and protected source hashes are unchanged except planned migrated files.
    - Security: no external deployment/service endpoint is contacted and selector is not client-controlled.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-18 --task 1</automated>
    <expected>Exit 0 with explicit legacy/migrated results for all protected paths and unchanged data hashes.</expected>
  </verify>
  <done>Rollback mechanics restore the prior public experience locally/CI without data or external-system changes. Result fragment: T-01-57 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the fail-fast aggregate and identical CI gate</name>
  <files>web/scripts/verify-phase1.mjs, web/scripts/lib/phase1-threat-results.mjs, .github/workflows/phase1.yml, web/.gitignore, .gitignore, web/tests/security/phase1-threat-results/01-18.json</files>
  <read_first>web/scripts/verify-phase1.mjs, web/scripts/lib/phase1-threat-results.mjs, and .github/workflows/phase1.yml if present (otherwise treat them as create targets); every Phase 1 PLAN frontmatter and threat register; completed dependency fragments under web/tests/security/phase1-threat-results/; web/.gitignore and .gitignore; web/package.json; 01-RESEARCH.md Proposed Script Contract and verifier sequence; all created configs/test commands; AGENTS.md; 01-CONTEXT.md D-13</read_first>
  <behavior>
    - Static sequence runs baseline preflight, lint, types, unit/contracts, component/a11y, Storybook build, production build, route/browser/a11y/visual, performance/RAG, privacy, rollback, and approval checks.
    - First nonzero child exits aggregate nonzero with section/command identity; no failure is swallowed or downgraded.
    - CI performs exact install/pinned Chromium then runs only the locked aggregate from `web`.
    - The verifier parses immutable threat definitions from PLAN files, validates each `v5.0.0-X.Y.Z` identifier/owner/command, and obtains pass/fail evidence only through deterministic validation of per-plan fragments.
    - Candidate mode check-merges only an explicit completed-plan fragment list in natural plan/threat-ID order, treats the whole current/future plan set through a literal exemption list, and never reads a partial current-plan fragment or creates/updates the aggregate ledger; unqualified mode later consumes the final aggregate owned by Plan 01-20.
  </behavior>
  <action>Per D-13, implement `verify-phase1.mjs` with a static ordered child-command table, inherited stdio, explicit environment preflight, signal handling, immediate exit-code propagation, repository-relative artifact paths, and no live external service. Create `web/scripts/lib/phase1-threat-results.mjs` as a read-only fragment validation/check-merge module: it loads an explicit ordered plan list, parses every Phase 1 immutable definition, sorts by natural plan then natural threat-ID order, and rejects unknown plans, absent expected fragments or IDs, duplicate IDs, extra IDs, plan/task/command drift, schema mismatches, prohibited fields, non-passing high/critical entries, future timestamps, credentials/secrets, raw output, request/prompt/private payload fields, and machine-local paths. Candidate/check-only mode operates in memory and never creates or mutates `web/tests/security/phase1-threat-results.json`. Candidate scope comes from a literal completed-plan list, not a glob, range, inferred plan number, partial current-plan fragment, or nonexistent future fragment. For Plan 01-18, require only fragments for completed dependency plans 01-01 through 01-17 plus 01-21, 01-22, and 01-23; do not read 01-18.json even though Tasks 1-2 have produced entries. Treat current Plan 01-18 and future Plans 01-19/01-20 as a literal non-scope set with exact threat exemptions T-01-55 through T-01-64. Reject an exemption belonging to any selected completed plan, a request for a current/future-plan fragment, or any exemption outside that exact literal list. The module exposes validated naturally ordered data but has no aggregate-write capability; Plan 01-20 Task 2 exclusively owns the final merge script and aggregate ledger. The unqualified verifier never writes fragments or the aggregate; it reads the final aggregate, has no exemptions, and requires exact 72-definition parity before its child sequence can succeed. Add `--check-validation-waves` plus self-tests for all rejection cases, deterministic order, candidate no-write behavior, aggregate self-reference, and validation-wave drift. Add one CI workflow using the repository's supported Node line, `npm ci`, pinned Chromium installation, and exactly `cd web &amp;&amp; npm run verify:phase1`. Update ignore rules for generated outputs while re-including approved artifacts. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-18 --task 2` so T-01-55 executes into the 01-18 fragment; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Behavior: injecting a failing child returns the same nonzero code and prevents later sections.
    - Security gate: immutable definition parity and fragment/aggregate schemas are enforced; injected missing, malformed, failed, duplicate, unknown-plan, future-timestamp, drifted, private, future-scoped, over-exempted, or self-referential cases are rejected.
    - Ownership: candidate/check-only mode leaves `web/tests/security/phase1-threat-results.json` absent or byte-identical and writes only `phase1-threat-results/01-18.json` through the task runner.
    - Source: no `|| true`, watch flag, live endpoint, secret echo, duplicated CI command set, or broad destructive cleanup.
    - Ignore policy: generated artifacts are ignored and approved visual manifest/baseline paths are trackable by last-match re-includes.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-18 --task 2</automated>
    <expected>Exit 0; failure propagation and fail-closed high/critical threat-ledger self-tests pass, and CI contains one aggregate invocation without duplicated child gates.</expected>
  </verify>
  <done>One deterministic command owns all automated Phase 1 proof locally and in CI. Result fragment: T-01-55 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble the unapproved visual, performance, and rollback review packet</name>
  <files>web/tests/visual/phase1-candidate.json, .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md, web/tests/security/phase1-threat-results/01-18.json</files>
  <read_first>web/tests/visual/phase1-candidate.json if present (otherwise treat it as a create target); web/tests/visual/public-routes.spec.ts; generated candidate hash metadata; web/tests/performance/phase1-baseline.json; web/tests/performance/phase1-budgets.json; rollback output; 01-UI-SPEC.md Capture Contract; 01-VALIDATION.md</read_first>
  <action>After Task 1 has recorded T-01-57 and Task 2 has recorded T-01-55 in `phase1-threat-results/01-18.json`, run `cd web &amp;&amp; npm run verify:phase1 -- --candidate`. Candidate mode invokes the deterministic validation module's in-memory check-merge with only the literal completed fragment set 01-01 through 01-17 plus 01-21 through 01-23; it does not read the partial current 01-18 fragment. The exact current/future threat exemption list is T-01-55 through T-01-64. It must not require current/future 01-18/01-19/01-20 fragments and must leave the aggregate absent or byte-identical. Then write the privacy-safe candidate manifest and retain pending owner/Validation flags. Keep mutable command evidence in per-plan fragments only. Finally invoke the task runner so T-01-56 is atomically added to the 01-18 fragment after candidate prerequisites pass.</action>
  <acceptance_criteria>
    - Command: candidate aggregate passes every automated child and reports only approval pending.
    - Coverage: candidate manifest contains the full UI-SPEC route/shared-state/viewport matrix and both rollback variants.
    - Privacy: bounded scan passes on candidate manifest and generated artifacts.
    - Validation: all mappings concrete; `wave_0_complete` and `nyquist_compliant` remain false.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-18 --task 3</automated>
    <expected>Exit 0; every automated gate passes, the packet is complete/privacy-safe, and owner approval remains pending.</expected>
  </verify>
  <done>All automated evidence and rollback proof are ready for owner judgment without self-approving pixels or cutover. Result fragment: T-01-56 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Blocking gate integrity, CI parity, retained variants, public data, visual/performance evidence, and repository privacy.

## Trust Boundaries
| Boundary | Description |
|---|---|
| child commands → aggregate/CI result | Swallowed failures or command drift can falsely approve release. |
| generated artifacts → committed review packet | Reports may contain secrets, private payloads, machine paths, or unstable content. |
| variant process config → public clients | Rollback selector must remain server-only and local/CI bounded. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-18.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-55 | Tampering/Repudiation | aggregate/CI | mitigate | high | low | high | v5.0.0-2.1.1 | Task 2 | cd web &amp;&amp; node scripts/verify-phase1.mjs --self-test &amp;&amp; node scripts/verify-phase1.mjs --self-test-threat-ledger | defined | Static commands, fail-fast exit propagation, identical CI entry, named logs, hash-bound manifests, and fail-closed threat ledger. |
| T-01-56 | Information Disclosure | reports/review packet | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 3 | cd web &amp;&amp; npm run verify:phase1 -- --candidate &amp;&amp; npm run test:privacy -- --check | defined | Synthetic fixtures, bounded scanner, repository-relative metadata, ignored generated outputs, approved-only re-includes. |
| T-01-57 | Denial of Service | rollback/cutover | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 1 | cd web &amp;&amp; npm run test:rollback | defined | Both fresh variants pass all protected contracts without data/external mutation before owner review. |
</threat_model>

<verification>
Run rollback, aggregate self-test, CI parity check, full candidate aggregate, privacy scan, and manifest coverage/schema checks.
</verification>

<success_criteria>
One blocking command and its CI twin cover all proof, rollback works, and a complete privacy-safe owner review packet exists without approval/cutover claims.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-18-SUMMARY.md` when done.
</output>
