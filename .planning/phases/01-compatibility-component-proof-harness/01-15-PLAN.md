---
phase: 01-compatibility-component-proof-harness
plan: "15"
type: execute
wave: 16
depends_on: ["01-14"]
files_modified:
  - web/components/legacy/public/LegacyChatPage.tsx
  - web/components/domain/public/MigratedChatPage.tsx
  - web/components/domain/public/AskComposer.tsx
  - web/components/domain/public/ConversationThread.tsx
  - web/components/patterns/SourcePanel.tsx
  - web/app/chat/page.tsx
  - web/components/domain/public/MigratedChatPage.stories.tsx
  - web/tests/journeys/chat.spec.ts
  - web/tests/security/phase1-threat-results/01-15.json
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, DSYS-04, DSYS-05, DSYS-06, DSYS-07, DSYS-08, DSYS-10, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]
must_haves:
  truths:
    - "`/chat` remains canonical and preserves accepted one-time `q` auto-submit, request/body reader, streaming/chunk, source/citation, header, status, no-store, and safe-error behavior per D-09/D-10."
    - "Public ModelPicker, model labels, routing/fallback infrastructure copy, and retry-by-model controls are absent; the diagnostic source file remains unexposed for any future internal feature gate."
    - "Grounded synthesis, sources, abstention, loading, safe error, offline retry, and live status remain structurally distinct and truthful."
  artifacts:
    - path: "web/components/domain/public/MigratedChatPage.tsx"
      provides: "One consistent accessible Ask WTF public experience"
    - path: "web/components/patterns/SourcePanel.tsx"
      provides: "Safe cited public source navigation with verified timing only"
  key_links:
    - from: "web/components/domain/public/MigratedChatPage.tsx"
      to: "/api/chat"
      via: "unchanged accepted request and incremental response-reader contract"
---

<objective>
Retain the prior Chat UI and visibly migrate `/chat` without modifying the complete `/api/chat` production contract.

Purpose: Deliver D-10's single Ask WTF identity while protecting every compatibility, grounding, source, privacy, accessibility, performance, and rollback seam.
Output: Legacy/candidate Chat variants, migrated composer/thread/source components, deterministic states, and full browser/API contract proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/app/chat/page.tsx
@web/app/api/chat/route.ts
@web/components/ModelPicker.tsx
@web/tests/contracts/api-chat.contract.test.ts
@web/tests/contracts/phase1-baseline-approval.json
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@.planning/phases/01-compatibility-component-proof-harness/01-PATTERNS.md
@web/tests/performance/phase1-budgets.json
</context>

<must_haves>
  <truths>D-10 removes only public model selection/infrastructure presentation; accepted compatibility logic may still consume X-Model/X-Fallback without displaying it.</truths>
  <artifacts>Stories cover empty, query-loading, incremental, grounded timestamped/untimed, abstention, safe error, offline retry, focus/live status, reduced motion, and three widths.</artifacts>
  <key_links>Production `web/app/api/chat/route.ts` must remain byte-for-byte unchanged by this plan and all API contract tests must continue to pass.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-15 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-15.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-15, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto">
  <name>Task 1: Retain the accepted Chat UI and create the server-selected route seam</name>
  <files>web/components/legacy/public/LegacyChatPage.tsx, web/components/domain/public/MigratedChatPage.tsx, web/app/chat/page.tsx</files>
  <read_first>web/components/legacy/public/LegacyChatPage.tsx and web/components/domain/public/MigratedChatPage.tsx if present (otherwise treat them as create targets); web/tests/contracts/phase1-baseline-approval.json; web/app/chat/page.tsx; web/app/api/chat/route.ts; web/lib/public/public-ui-variant.ts; 01-CONTEXT.md D-09, D-10, D-12</read_first>
  <action>Per D-10, copy the owner-approved current client Chat page into `LegacyChatPage` with only import/export wiring changes and bind it to the approved source hash. Create `MigratedChatPage` initially as a fully functional behavior-equivalent client branch. Make `web/app/chat/page.tsx` a server wrapper selecting legacy/migrated modules with pre-acceptance legacy default while preserving the existing Suspense/query behavior inside each client implementation. Do not edit `/api/chat`, ModelPicker, model utilities, edge configuration, data, or external services.</action>
  <acceptance_criteria>
    - Source: retained Chat normalizes to accepted current source; `/api/chat` source hash is unchanged.
    - Behavior: both variants serve `/chat` and one-time `/chat?q=` auto-submit semantics.
    - Security: selector/env is absent from client output and no production external call occurs in tests.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm run build &amp;&amp; npm run test:contracts -- tests/contracts/api-chat.contract.test.ts tests/contracts/public-routes.contract.test.ts</automated>
    <expected>Exit 0; both Chat UI variants build and the frozen API/route contracts remain green.</expected>
  </verify>
  <done>The prior Chat UI is recoverable and the migration branch is isolated without touching `/api/chat`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build one public Ask WTF composer, thread, and safe source panel</name>
  <files>web/components/domain/public/AskComposer.tsx, web/components/domain/public/ConversationThread.tsx, web/components/patterns/SourcePanel.tsx, web/components/domain/public/MigratedChatPage.tsx, web/components/domain/public/MigratedChatPage.stories.tsx, web/tests/security/phase1-threat-results/01-15.json</files>
  <read_first>web/components/domain/public/AskComposer.tsx, web/components/domain/public/ConversationThread.tsx, web/components/patterns/SourcePanel.tsx, and web/components/domain/public/MigratedChatPage.tsx if present (otherwise treat absent paths as create targets); 01-UI-SPEC.md `/chat` Composition, Interaction/State, Responsive Layout, `/api/chat` seam, and public allowlist; web/app/chat/page.tsx accepted client logic; web/app/api/chat/route.ts; web/lib/public/contracts.ts; web/components/ui/LiveRegion.tsx; web/components/ui/AvailabilityState.tsx</read_first>
  <behavior>
    - Persistent label/placeholder/button copy matches UI-SPEC; blank/active submits disabled; Enter respects composition events.
    - `q` auto-submits once, unrelated params survive, request shape and body reader remain accepted, incremental text does not move focus, and auto-scroll stops when reader scrolls up.
    - Sources/citation numbers remain matched; verified time alone renders timestamp; abstention shows explicit unsupported-claim label with no fabricated source/role/owner/time.
    - Safe errors preserve accepted public error text and expose `try again`; provider/stack/secret/request/model details are absent.
  </behavior>
  <action>Per D-10, extract AskComposer, ConversationThread, and SourcePanel around accepted fetch/header/reader/link behavior. Keep ReactMarkdown without raw HTML, safe links, one Ask WTF identity, same-path retry, stable bounded LiveRegion, offline composed input, and distinct question/synthesis/source/abstention/error semantics. In this task add focused MigratedChatPage story interactions for blank/active submit, IME, q auto-submit once, controlled chunks, focus/scroll retention, source mapping, abstention, safe retry, and ModelPicker absence; Task 3 expands all viewport and route evidence. Do not import/render ModelPicker in public migrated modules. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-15 --task 2` so T-01-45, T-01-46 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Behavior: accepted query/request/stream/source/error cases pass unchanged; IME composition cannot submit prematurely.
    - Source: public migrated imports contain no `ModelPicker`, `MODELS`, model label, or retry-by-model presentation.
    - Security: no raw HTML, unsafe URL, provider detail, secret name/value, request ID, or model routing appears in DOM/artifacts.
    - Accessibility: persistent label, 44px submit/retry, focus retention, polite bounded live status, and readable source order pass.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-15 --task 2</automated>
    <expected>Exit 0; migrated Chat components compile and the complete API/public projection contract is unchanged.</expected>
  </verify>
  <done>Public Ask WTF has one evidence-first identity with complete accepted transport and safe state behavior. Result fragment: T-01-45, T-01-46 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove Chat transport, states, accessibility, viewports, privacy, budgets, and rollback</name>
  <files>web/components/domain/public/MigratedChatPage.stories.tsx, web/tests/journeys/chat.spec.ts, web/tests/security/phase1-threat-results/01-15.json</files>
  <read_first>web/components/domain/public/MigratedChatPage.stories.tsx and web/tests/journeys/chat.spec.ts if present (otherwise treat them as create targets); web/components/domain/public/MigratedChatPage.tsx; AskComposer.tsx; ConversationThread.tsx; SourcePanel.tsx; web/tests/support/rag-stub.mjs; 01-UI-SPEC.md required Chat evidence; web/tests/performance/phase1-budgets.json</read_first>
  <behavior>
    - Stories/journey cover empty, `q` auto-submit loading, controlled multi-chunk response, grounded timestamped/untimed sources, abstention, safe 400/503, offline retry, keyboard focus, live status, reader scroll retention, reduced motion, and all widths.
    - Every `/api/chat` contract case still passes and production route hash remains unchanged.
    - Both Chat UI variants remain runnable and migrated results fit approved budgets.
  </behavior>
  <action>Add deterministic stories and a Playwright journey backed only by the local RAG stub. Assert exact request count/body, response chunk accumulation, header/source decoding, citation-source identity, verified/untimed timing, safe error recovery, no duplicate hydration submit, IME handling, bounded live announcements, manual-scroll preservation, 320/768/1440 reflow, serious/critical axe, privacy scan, approved performance/RAG budgets, and dual-variant rollback. Add negative DOM assertions for ModelPicker/model/infrastructure copy. Capture unapproved visual candidates only. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-15 --task 3` so T-01-47, T-01-48 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Command: stories, complete API contracts, browser journey, privacy, performance/RAG subset, and rollback pass.
    - Accessibility: zero serious/critical axe plus explicit keyboard/focus/live/scroll assertions.
    - Privacy: no provider/secret/model routing/private field in DOM, bundle, logs, traces, screenshots, or errors.
    - Source: `/api/chat` hash matches baseline approval.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-15 --task 3</automated>
    <expected>Exit 0; `/chat` is visually migrated while the full accepted API seam, privacy, budgets, and rollback remain green.</expected>
  </verify>
  <done>`/chat` is a distinct accessible WTF evidence room with no public infrastructure picker and no API contract regression. Result fragment: T-01-47, T-01-48 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Complete `/api/chat` contract, edge secret, public questions/answers/sources, safe errors, browser focus/live state, and Chat rollback.

## Trust Boundaries
| Boundary | Description |
|---|---|
| anonymous composer → `/api/chat` | Untrusted content and repeated requests cross into bounded server processing. |
| API text/headers/source URLs → markdown/DOM | Content can create XSS, unsafe navigation, false timing, or infrastructure disclosure. |
| streaming async state → user focus/scroll/live region | Aggressive updates can deny reading/composing access. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-15.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-45 | Tampering/XSS | markdown/citations/source URLs | mitigate | critical | medium | high | v5.0.0-1.2.2 | Task 2 | cd web &amp;&amp; npm run test:components -- MigratedChatPage --grep "submit\|IME\|stream\|sources\|error\|focus" &amp;&amp; npm run test:contracts -- tests/contracts/api-chat.contract.test.ts tests/contracts/public-projection.contract.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint | defined | No raw HTML, safe URL/link contract, exact citation mapping, verified timing only, contract/browser tests. |
| T-01-46 | Information Disclosure | errors/model/secret/provider | mitigate | critical | medium | high | v5.0.0-15.3.1 | Task 2 | cd web &amp;&amp; npm run test:components -- MigratedChatPage --grep "submit\|IME\|stream\|sources\|error\|focus" &amp;&amp; npm run test:contracts -- tests/contracts/api-chat.contract.test.ts tests/contracts/public-projection.contract.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint | defined | Safe accepted errors, no public ModelPicker/infrastructure copy, bundle/DOM/log/artifact scans. |
| T-01-47 | Denial of Service | chat requests/async UI | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 3 | cd web &amp;&amp; npm run test:components -- MigratedChatPage &amp;&amp; npm run test:contracts -- tests/contracts/api-chat.contract.test.ts tests/contracts/rag-latency.test.ts &amp;&amp; npm run test:browser -- tests/journeys/chat.spec.ts &amp;&amp; npm run test:privacy -- --check &amp;&amp; npm run test:rollback -- --route=/chat | defined | Preserve size/last-eight/timeout/active-submit bounds, IME guard, offline manual retry, no per-token announcements. |
| T-01-48 | Denial of Service | focus/scroll/live region | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 3 | cd web &amp;&amp; npm run test:components -- MigratedChatPage &amp;&amp; npm run test:contracts -- tests/contracts/api-chat.contract.test.ts tests/contracts/rag-latency.test.ts &amp;&amp; npm run test:browser -- tests/journeys/chat.spec.ts &amp;&amp; npm run test:privacy -- --check &amp;&amp; npm run test:rollback -- --route=/chat | defined | Incremental text never moves focus; near-end auto-scroll only; bounded polite status; keyboard/viewports/axe tests. |
</threat_model>

<verification>
Run complete API/RAG contracts, Chat stories/journey, privacy, performance, source-hash, and dual-variant rollback checks.
</verification>

<success_criteria>
Public Ask WTF is visibly migrated and more accessible while every accepted request, transport, source, status, header, error, privacy, and rollback behavior remains intact.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-15-SUMMARY.md` when done.
</output>
