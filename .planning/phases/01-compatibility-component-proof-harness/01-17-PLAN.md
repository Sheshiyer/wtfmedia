---
phase: 01-compatibility-component-proof-harness
plan: "17"
type: execute
wave: 18
depends_on: ["01-16"]
files_modified:
  - web/tests/journeys/public-routes.spec.ts
  - web/tests/journeys/viewports.spec.ts
  - web/tests/accessibility/public-routes.spec.ts
  - web/tests/visual/public-routes.spec.ts
  - web/tests/component-trace.json
  - web/tests/unit/component-trace.test.ts
  - .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
  - web/tests/security/phase1-threat-results/01-17.json
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, DSYS-01, DSYS-02, DSYS-03, DSYS-04, DSYS-05, DSYS-06, DSYS-07, DSYS-08, DSYS-09, DSYS-10, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]
must_haves:
  truths:
    - "One cross-route matrix proves all protected UI routes and `/api/chat` together rather than relying on isolated happy paths."
    - "Every shipped component is traceable from tokens through primitive/pattern/domain layers to a protected-route consumer and complete deterministic states."
    - "Keyboard, focus, names, live regions, reduced motion, graph parity, reflow, privacy, visuals, and budgets are checked across the full migrated surface."
  artifacts:
    - path: "web/tests/journeys/public-routes.spec.ts"
      provides: "Phase-wide protected route/API compatibility journey"
    - path: "web/tests/component-trace.json"
      provides: "Token-to-consumer trace manifest"
  key_links:
    - from: "web/tests/component-trace.json"
      to: "web/tests/unit/component-trace.test.ts"
      via: "file/export/story/state/consumer reachability assertions"
---

<objective>
Close cross-route, accessibility, viewport, visual-candidate, and component-trace proof gaps before aggregation.

Purpose: Ensure all four migrations and `/api/chat` pass as one protected product surface with no missing Nyquist row.
Output: Phase-wide journeys, accessibility/viewports/visual specs, component trace manifest/test, and complete validation mappings.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@web/tests/contracts/phase1-compatibility-manifest.json
@web/tests/contracts/api-chat.contract.test.ts
@web/tests/performance/phase1-budgets.json
@web/tests/contracts/legacy-presentation-dependencies.json
@web/tests/visual/legacy/phase1-legacy-baseline.manifest.json
@web/components/patterns/brand/BrandMotion.stories.tsx
@.planning/phases/01-compatibility-component-proof-harness/01-21-SUMMARY.md
@.planning/phases/01-compatibility-component-proof-harness/01-22-SUMMARY.md
@web/tests/journeys/home.spec.ts
@web/tests/journeys/episodes.spec.ts
@web/tests/journeys/connections.spec.ts
@web/tests/journeys/chat.spec.ts
</context>

<must_haves>
  <truths>No protected route, state category, viewport, or high-risk trust boundary is omitted from phase-wide proof.</truths>
  <artifacts>Visual spec produces candidate evidence only; owner approval and baseline promotion remain blocked in later plans.</artifacts>
  <key_links>The validation table maps every one of 21 requirement IDs to a real plan/task/file/command/threat and retains false completion flags.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-17 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-17.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-17, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the phase-wide compatibility and viewport journey matrix</name>
  <files>web/tests/journeys/public-routes.spec.ts, web/tests/journeys/viewports.spec.ts</files>
  <read_first>web/tests/journeys/public-routes.spec.ts and web/tests/journeys/viewports.spec.ts if present (otherwise treat them as create targets); all four route journeys; web/tests/contracts/phase1-compatibility-manifest.json; 01-CONTEXT.md D-06, D-09, D-12, D-16; 01-UI-SPEC.md Phase-Wide Interaction Acceptance</read_first>
  <behavior>
    - `/`, `/episodes`, `/connections`, `/chat`, and `/api/chat` pass together under migrated and retained variants.
    - Supported bookmarks, unrelated queries, `/chat?q=`, episode deep link, navigation destinations, public semantics, source links, and complete API matrix retain meaning.
    - 320/768/1440 have no hidden action or horizontal page overflow except labelled ScrollRail regions.
  </behavior>
  <action>Compose existing route/API assertions into one deterministic Playwright matrix that runs migrated routes together and a bounded retained-variant smoke without duplicating source logic. Exercise cross-route navigation, Back/Forward, query preservation, selected episode, canvas/list details, chat source navigation, and exact `/api/chat` contracts. Parameterize all workflows at exact viewports and assert visible actions/reflow/overflow. Keep external resources stubbed and selector server-only.</action>
  <acceptance_criteria>
    - Command: one matrix covers all five protected paths in the same run and reports route/state names on failure.
    - Compatibility: all manifest examples and API cases are referenced by test ID with no omission.
    - Responsive: each workflow completes at all three widths without hidden action/page overflow.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm run test:browser -- tests/journeys/public-routes.spec.ts tests/journeys/viewports.spec.ts &amp;&amp; npm run test:contracts</automated>
    <expected>Exit 0; all protected routes/API and all three viewport workflows pass together.</expected>
  </verify>
  <done>Phase-wide compatibility and reflow are proven as one connected public product.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build phase-wide accessibility and visual-candidate matrices</name>
  <files>web/tests/accessibility/public-routes.spec.ts, web/tests/visual/public-routes.spec.ts, web/tests/security/phase1-threat-results/01-17.json</files>
  <read_first>web/tests/accessibility/public-routes.spec.ts and web/tests/visual/public-routes.spec.ts if present (otherwise treat them as create targets); all migrated component stories and route journeys; 01-UI-SPEC.md Required State Matrix and Capture Contract; 01-VALIDATION.md QUAL-03/QUAL-04 rows; web/playwright.config.ts</read_first>
  <behavior>
    - Accessibility matrix covers keyboard, focus, accessible names, live regions, reduced motion, serious/critical axe, and graph/list equivalence in applicable open/result/error states.
    - Visual matrix captures every route and changed shared component at 320/768/1440 for applicable ready/loading/empty/error/focus/open-overlay/reduced-motion states.
    - Candidate pixels/hashes are never self-approved.
  </behavior>
  <action>Create route/state accessibility tests with explicit non-axe behavior assertions and a deterministic visual capture spec that freezes environment/assets, waits for fonts/images, disables nonessential animation for comparison, and names outputs only with public route/state/viewport identifiers. Generate candidate evidence into the configured ignored review output and emit machine-readable hash metadata for the next plan; do not update approved snapshots or owner manifests. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-17 --task 2` so T-01-53, T-01-54 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Accessibility: every changed workflow has serious/critical axe plus explicit keyboard/focus/name/live/motion checks.
    - Visual: required route/shared-component state/viewport Cartesian coverage is enumerable and no candidate is marked approved.
    - Privacy: filenames/metadata contain no title/source/query/private/machine-local text.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-17 --task 2</automated>
    <expected>Exit 0; accessibility passes and deterministic unapproved visual candidates/hashes are produced for every required surface.</expected>
  </verify>
  <done>All human-review pixels are prepared only after automated accessibility/state proof passes. Result fragment: T-01-53, T-01-54 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove component reachability and finalize validation mappings</name>
  <files>web/tests/component-trace.json, web/tests/unit/component-trace.test.ts, .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md, web/tests/security/phase1-threat-results/01-17.json</files>
  <read_first>web/tests/component-trace.json and web/tests/unit/component-trace.test.ts if present (otherwise treat them as create targets); docs/design/COMPONENT-INVENTORY.md; all Phase 1 component/story files; legacy dependency/baseline manifests; 01-VALIDATION.md; all completed plan summaries through this wave, including 01-21 and 01-22; this plan's threat model</read_first>
  <behavior>
    - Every shipped component maps to token dependencies, primitive/pattern/domain layer, exported file, deterministic story/state set, and named route consumer.
    - No speculative/deferred operator/table/icon primitive appears as shipped.
    - Validation has exact plan/task/wave/threat/file/command mappings for all 21 requirements while completion flags remain false.
  </behavior>
  <action>Create a machine-readable component trace manifest and a test that resolves every file/export/import/consumer/story/state reference, detects cycles/wrong-layer imports, rejects orphan/speculative components, and confirms all four routes are reachable. Update every remaining `01-VALIDATION.md` row with exact task/plan/wave/threat mappings and only mark green when its real command has passed. Do not mark Wave 0 or Nyquist complete until Plan 01-20's final aggregate passes. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-17 --task 3` so T-01-52 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Command: trace test finds zero missing exports/stories/consumers and zero wrong-direction dependencies.
    - Coverage: all 21 requirement rows have no TBD task/plan/wave/threat/file/command fields.
    - Behavior: `wave_0_complete` and `nyquist_compliant` remain false.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-17 --task 3</automated>
    <expected>Exit 0; component reachability and all validation mappings are complete while final status remains pending.</expected>
  </verify>
  <done>Every shipped component and requirement is reachable, testable, and mapped without claiming final acceptance. Result fragment: T-01-52 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Cross-route compatibility, complete accessibility/state/visual evidence, component trace integrity, and public privacy.

## Trust Boundaries
| Boundary | Description |
|---|---|
| isolated route tests → phase-wide conclusion | Missing combinations can create false acceptance. |
| browser state → screenshots/traces/reports | Sensitive or unstable content can leak or falsify evidence. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-17.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-52 | Tampering | coverage/trace manifests | mitigate | high | medium | high | v5.0.0-2.1.1 | Task 3 | cd web &amp;&amp; npm run test:browser -- tests/journeys/public-routes.spec.ts tests/journeys/viewports.spec.ts &amp;&amp; npm run test:unit -- tests/unit/component-trace.test.ts | defined | Enumerate exact route/state/viewport/requirement matrices; resolve files/exports/consumers; reject missing/orphan entries. |
| T-01-53 | Information Disclosure | visual/test artifacts | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 2 | cd web &amp;&amp; npm run test:a11y &amp;&amp; npm run test:visual -- --candidate | defined | Synthetic fixtures, controlled network, bounded names, privacy scan before owner review. |
| T-01-54 | Denial of Service | phase-wide accessibility | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 2 | cd web &amp;&amp; npm run test:a11y &amp;&amp; npm run test:visual -- --candidate | defined | Explicit keyboard/focus/live/motion/reflow/parity plus serious/critical axe in applicable states. |
</threat_model>

<verification>
Run cross-route/browser/contracts, all-width, a11y, candidate visual, component trace, and validation coverage checks.
</verification>

<success_criteria>
All protected surfaces and components are jointly proven and fully mapped, with only aggregate wiring and owner visual/rollback authority remaining.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-17-SUMMARY.md` when done.
</output>
