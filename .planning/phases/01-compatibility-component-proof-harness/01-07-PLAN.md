---
phase: 01-compatibility-component-proof-harness
plan: "07"
type: execute
wave: 7
depends_on: ["01-06"]
files_modified:
  - web/tests/performance/phase1-budgets.json
  - .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
  - web/tests/security/phase1-threat-results/01-07.json
autonomous: false
requirements: [QUAL-06]
must_haves:
  truths:
    - "The owner reviews measured samples and explicitly approves exact numeric route, bundle, interaction, and RAG budgets before visual migration."
    - "The approved budget artifact remains traceable to the baseline and any owner adjustments."
  artifacts:
    - path: "web/tests/performance/phase1-budgets.json"
      provides: "Owner-approved blocking numeric budgets for the Phase 1 verifier"
  key_links:
    - from: "web/tests/performance/phase1-budget-proposal.json"
      to: "web/tests/performance/phase1-budgets.json"
      via: "owner approval or explicit adjusted values with rationale"
---

<objective>
Convert measured candidate budgets into an owner-approved blocking contract.

Purpose: Fulfill D-15's human authority gate before any protected route is visually changed.
Output: Exact approved budget artifact and validation approval reference.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/tests/performance/phase1-baseline.json
@web/tests/performance/phase1-budget-proposal.json
@.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
@.planning/phases/01-compatibility-component-proof-harness/01-CONTEXT.md
</context>

<must_haves>
  <truths>No visual migration plan can run until this checkpoint passes.</truths>
  <artifacts>Approved budgets include route/metric values, units, baseline hash, proposal hash, owner reference, and any adjustment rationale.</artifacts>
  <key_links>All later performance tests load only the approved artifact and reject hash drift or missing metrics.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-07 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-07.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-07, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve exact performance, bundle, interaction, and RAG budgets</name>
  <files>web/tests/performance/phase1-budgets.json, web/tests/security/phase1-threat-results/01-07.json</files>
  <read_first>web/tests/performance/phase1-budgets.json if present (otherwise treat it as a create target); web/tests/performance/phase1-baseline.json; web/tests/performance/phase1-budget-proposal.json; 01-CONTEXT.md D-15; 01-RESEARCH.md Performance Baseline and Budget Gate</read_first>
  <action>Per D-15, present all five raw route runs, normalized summaries, bundle/resource measurements, controlled interaction/RAG samples, and every candidate's derivation. Ask the owner to approve the proposal exactly or supply explicit adjusted values and rationale. On approval only, write `phase1-budgets.json` with `status: approved`, every required metric, units, baseline/proposal SHA-256 values, environment identity, owner approval reference, and adjustment rationale where applicable. Do not begin tokens, primitives, shell, or route migration without approval. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-07 --task 1` so T-01-21 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Human: owner explicitly approves exact numeric values for all listed route, bundle, interaction, and RAG dimensions.
    - Data: approved artifact has no missing metric, null threshold, unit mismatch, or undocumented adjustment.
    - Source: artifact binds the current baseline and proposal hashes.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-07 --task 1</automated>
    <expected>After owner approval, exit 0 with complete exact budgets; otherwise execution remains paused.</expected>
  </verify>
  <done>The owner has authorized the numeric regression boundary that later visual work must respect. Result fragment: T-01-21 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto">
  <name>Task 2: Bind the approved budget artifact into validation</name>
  <files>.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md, web/tests/security/phase1-threat-results/01-07.json</files>
  <read_first>web/tests/performance/phase1-budgets.json; .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md; this plan's threat model</read_first>
  <action>Record Plan `01-07`, Wave 7, owner approval reference, artifact hash, exact test command, and T-01-21 under QUAL-06 and Manual-Only Verifications. Mark budget approval complete but keep Wave 0/Nyquist false until all harness files and aggregate proof pass. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-07 --task 2` so T-01-22 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: QUAL-06 names the approved artifact and owner reference.
    - Behavior: later migration remains blocked if artifact status/hash changes.
    - Command: Wave 0 and Nyquist flags remain false.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-07 --task 2</automated>
    <expected>Exit 0; budget authority is traceable while overall validation remains incomplete.</expected>
  </verify>
  <done>Approved budgets are a durable prerequisite for every subsequent visual plan. Result fragment: T-01-22 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Blocking performance/reliability thresholds and owner decision provenance.

## Trust Boundaries
| Boundary | Description |
|---|---|
| measured proposal → owner decision | Numbers can be loosened, copied, or misrepresented without evidence. |
| approved artifact → later verifier | Drift or missing metrics could silently disable a regression gate. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-07.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-21 | Tampering/Repudiation | budget approval | mitigate | high | low | high | v5.0.0-2.1.1 | Task 1 | node -e 'const b=require("./web/tests/performance/phase1-budgets.json"); if(b.status!=="approved"\|\|!b.owner_approval_ref\|\|!b.baseline_sha256\|\|!b.proposal_sha256) process.exit(1); const required=["home","episodes","connections","chat","bundle","interactions","rag"]; if(required.some(k=&gt;!b.budgets[k])) process.exit(1)' | defined | ASVS V14: hash-bound baseline/proposal, explicit owner reference, complete schema, adjustment rationale, and later fail-closed loading. |
| T-01-22 | Denial of Service | over-tight or absent budgets | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 2 | node -e 'const s=require("fs").readFileSync(".planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md","utf8"),b=require("./web/tests/performance/phase1-budgets.json"); if(!s.includes("01-07")\|\|!s.includes(b.owner_approval_ref)\|\|!s.includes("T-01-21")\|\|!s.includes("nyquist_compliant: false")) process.exit(1)' | defined | Owner reviews raw distributions and units; every required dimension must be present before approval. |
</threat_model>

<verification>
Schema-check the approval artifact, its hashes, complete metric coverage, and validation reference.
</verification>

<success_criteria>
Exact evidence-backed budgets are owner-approved, complete, and blocking before visual migration begins.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-07-SUMMARY.md` when done.
</output>
