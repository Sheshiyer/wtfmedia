---
phase: 01-compatibility-component-proof-harness
plan: "10"
type: execute
wave: 9
depends_on: ["01-21"]
files_modified:
  - web/lib/public/public-ui-variant.ts
  - web/components/legacy/public/LegacyEpisodesBrowser.tsx
  - web/components/legacy/public/LegacyDragRow.tsx
  - web/components/legacy/public/LegacyEpisodesPage.tsx
  - web/components/domain/public/MigratedEpisodesPage.tsx
  - web/app/episodes/page.tsx
  - web/tests/rollback/episodes-variant.spec.ts
  - web/tests/security/phase1-threat-results/01-10.json
autonomous: true
requirements: [COMP-01, COMP-02, QUAL-02, QUAL-06]
must_haves:
  truths:
    - "The exact accepted Episodes route/component behavior remains available as a retained legacy variant before any Episodes visual change."
    - "A server-only local/CI selector chooses legacy or migrated code without a public query switch, data mutation, or deployment change."
  artifacts:
    - path: "web/lib/public/public-ui-variant.ts"
      provides: "Narrow server-only legacy/migrated selector with pre-acceptance legacy default"
    - path: "web/components/legacy/public/LegacyEpisodesPage.tsx"
      provides: "Rollback copy of the owner-approved Episodes route"
  key_links:
    - from: "web/app/episodes/page.tsx"
      to: "legacy and migrated Episodes variants"
      via: "server-only `WTF_PUBLIC_UI_VARIANT` selection"
---

<objective>
Create the Episodes rollback seam before implementing the first proof slice.

Purpose: Satisfy D-12/D-16 while keeping the current URL and behavior available throughout migration.
Output: Exact legacy copies, a fully functional candidate branch, a server-only selector, and dual-variant smoke proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/tests/contracts/phase1-baseline-approval.json
@web/app/episodes/page.tsx
@web/components/EpisodesBrowser.tsx
@web/components/DragRow.tsx
@web/lib/episodes.ts
@.planning/phases/01-compatibility-component-proof-harness/01-CONTEXT.md
@.planning/phases/01-compatibility-component-proof-harness/01-PATTERNS.md
</context>

<must_haves>
  <truths>The retained variant matches the owner-approved hashes/content at creation; the selector is not browser-controlled and defaults to legacy before phase acceptance.</truths>
  <artifacts>The migrated branch is behavior-equivalent and buildable at this seam, so both variants are real runnable experiences before later edits.</artifacts>
  <key_links>Every later route variant uses the same narrow selector and final rollback verifier; no production environment is changed in Phase 1.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-10 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-10.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-10, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto">
  <name>Task 1: Retain exact accepted Episodes and DragRow implementations</name>
  <files>web/components/legacy/public/LegacyEpisodesBrowser.tsx, web/components/legacy/public/LegacyDragRow.tsx, web/components/legacy/public/LegacyEpisodesPage.tsx, web/tests/security/phase1-threat-results/01-10.json</files>
  <read_first>web/components/legacy/public/LegacyEpisodesBrowser.tsx, web/components/legacy/public/LegacyDragRow.tsx, and web/components/legacy/public/LegacyEpisodesPage.tsx if present (otherwise treat absent paths as create targets); web/tests/contracts/phase1-baseline-approval.json; web/app/episodes/page.tsx; web/components/EpisodesBrowser.tsx; web/components/DragRow.tsx; web/lib/episodes.ts; 01-CONTEXT.md D-12 and D-16</read_first>
  <action>Copy the owner-approved current Episodes page, EpisodesBrowser, and DragRow behavior into named legacy modules, adjusting imports/exports only as required to compile. Preserve public fields, transcript JSON→text fallback, source/timestamp links, Ask WTF links, current local active-state behavior, and visual output. Bind a comment-free machine check to the approved source hashes rather than refactoring legacy code. Do not clean up accessibility issues inside retained modules; the migrated branch owns those corrections. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-10 --task 1` so T-01-29 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: a normalization/hash check proves each retained module differs from its accepted source only in module/import/export wiring recorded by an explicit allowlist.
    - Behavior: legacy modules render the current public Episodes experience and use the same public dataset.
    - Privacy: no new data, private field, or source corpus copy enters legacy modules.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-10 --task 1</automated>
    <expected>Exit 0; retained modules compile and the approved source baseline remains intact.</expected>
  </verify>
  <done>The prior Episodes experience is preserved as an exact runnable rollback target. Result fragment: T-01-29 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto">
  <name>Task 2: Add the server-only variant seam with a fully functional candidate branch</name>
  <files>web/lib/public/public-ui-variant.ts, web/components/domain/public/MigratedEpisodesPage.tsx, web/app/episodes/page.tsx, web/tests/security/phase1-threat-results/01-10.json</files>
  <read_first>web/lib/public/public-ui-variant.ts and web/components/domain/public/MigratedEpisodesPage.tsx if present (otherwise treat absent paths as create targets); web/app/episodes/page.tsx; web/components/legacy/public/LegacyEpisodesPage.tsx; web/components/EpisodesBrowser.tsx; 01-RESEARCH.md Retained Route Variants pattern; AGENTS.md external/deployment boundary</read_first>
  <action>Implement `publicUiVariant()` returning only `"legacy" | "migrated"` from the non-secret server environment key `WTF_PUBLIC_UI_VARIANT`, with the pre-acceptance default set to `legacy`. Create `MigratedEpisodesPage` as a fully functional behavior-equivalent candidate that uses the current public data, headings, navigation, and `EpisodesBrowser`; it is the branch Plan 01-11 will redesign. Make the App Router page a server wrapper selecting the two modules. Never expose the selector as a query parameter, client bundle value, cookie, external config mutation, or deployment action. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-10 --task 2` so T-01-30 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Behavior: both selector values render HTTP 200 at `/episodes` and expose equivalent accepted public data/navigation before the redesign.
    - Security: query strings/cookies cannot select a variant; environment value is not serialized to client output.
    - Source: default is explicitly legacy until final Plan 01-20 changes it after phase-wide approval.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-10 --task 2</automated>
    <expected>Exit 0; the server wrapper and both complete variants compile without changing `/episodes`.</expected>
  </verify>
  <done>Episodes can be exercised in both accepted and candidate variants without a public control or data change. Result fragment: T-01-30 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove dual-variant Episodes rollback and URL continuity</name>
  <files>web/tests/rollback/episodes-variant.spec.ts, web/tests/security/phase1-threat-results/01-10.json</files>
  <read_first>web/tests/rollback/episodes-variant.spec.ts if present (otherwise treat it as a create target); web/lib/public/public-ui-variant.ts; web/app/episodes/page.tsx; web/tests/contracts/phase1-compatibility-manifest.json; web/tests/contracts/public-routes.contract.test.ts; 01-CONTEXT.md D-09, D-12, D-16</read_first>
  <behavior>
    - Legacy and migrated builds both serve `/episodes`, preserve unrelated query parameters, public headings/count/source links, and navigation destinations.
    - No `/episodes/:id` or `web/app/episodes/[episodeId]` route exists.
    - Switching variants requires only local/CI server configuration and no data mutation.
  </behavior>
  <action>Add a non-watch Playwright/runner spec that starts each variant in isolation, checks the accepted Episodes contract and supported query examples, and records build/source hashes. Assert the selector does not appear in browser JS/DOM/URLs. Include an explicit filesystem assertion that Phase 1 has not created a path-segment episode route. Keep this as the first route-specific rollback proof consumed by the final aggregate rehearsal. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-10 --task 3` so T-01-31 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Command: both variants pass the same compatibility assertions.
    - Source: no `app/episodes/[episodeId]` directory exists and no route uses `/episodes/:id`.
    - Security: browser artifacts do not contain `WTF_PUBLIC_UI_VARIANT` or operator/private fields.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-10 --task 3</automated>
    <expected>Exit 0; both variants preserve `/episodes` and no path-segment episode route exists.</expected>
  </verify>
  <done>The Episodes migration has a tested rollback seam before the first visual proof changes land. Result fragment: T-01-31 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Accepted Episodes behavior, public query semantics, rollback availability, and private server configuration.

## Trust Boundaries
| Boundary | Description |
|---|---|
| server environment → route selector | A public actor must not control or infer implementation variant through URL/client state. |
| current source → retained legacy copy | Refactoring or hash drift could make rollback non-equivalent. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-10.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-29 | Tampering | legacy copy/selector | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 1 | cd web &amp;&amp; npm run typecheck &amp;&amp; node scripts/capture-phase1-baseline.mjs --check | defined | Bind retained source normalization to approved hashes and exercise both builds against one manifest. |
| T-01-30 | Information Disclosure | variant environment/client | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 2 | cd web &amp;&amp; npm run build &amp;&amp; npm run typecheck | defined | Server-only narrow selector; scan bundles/DOM/URLs for key/value absence. |
| T-01-31 | Denial of Service | rollback | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 3 | cd web &amp;&amp; npm run test:browser -- tests/rollback/episodes-variant.spec.ts &amp;&amp; node -e 'const fs=require("fs"); if(fs.existsSync("app/episodes/[episodeId]")\|\|fs.existsSync("app/episodes/:id")) process.exit(1)' | defined | Both variants build and serve `/episodes`; no data migration; final rehearsal reuses this proof. |
</threat_model>

<verification>
Build/typecheck, execute both selector values, compare accepted contract assertions, and assert no path-segment route or client-visible selector.
</verification>

<success_criteria>
The exact prior Episodes experience is recoverable and the candidate branch is real, isolated, and ready for the first proof slice.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-10-SUMMARY.md` when done.
</output>
