---
phase: 01-compatibility-component-proof-harness
plan: "11"
type: execute
wave: 11
depends_on: ["01-09", "01-23"]
files_modified:
  - web/components/domain/public/EpisodeDrawer.tsx
  - web/components/EpisodesBrowser.tsx
  - web/components/EpisodesBrowser.stories.tsx
  - web/tests/journeys/episodes.spec.ts
  - web/tests/journeys/focus.spec.ts
  - web/tests/journeys/motion.spec.ts
  - web/tests/security/phase1-threat-results/01-11.json
autonomous: true
requirements: [COMP-01, COMP-02, COMP-04, DSYS-02, DSYS-04, DSYS-05, DSYS-06, DSYS-07, DSYS-08, DSYS-10, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]
must_haves:
  truths:
    - "EpisodesBrowser consumes the proven URL-state and ScrollRail foundation from Plan 01-23 as the first migrated vertical proof slice (D-01)."
    - "The URL-backed EpisodeDrawer integrates Back, Forward, refresh, share, invalid-ID, close, and focus-return behavior defined by D-02."
    - "EpisodeDrawer exposes only D-04 public detail and verified timing; no operator/private field or guessed timestamp appears."
  artifacts:
    - path: "web/components/domain/public/EpisodeDrawer.tsx"
      provides: "URL-controlled accessible public episode detail"
  key_links:
    - from: "web/components/EpisodesBrowser.tsx"
      to: "web/lib/public/url-state.ts"
      via: "selected public video ID derived from the `episode` query key"
    - from: "web/components/domain/public/EpisodeDrawer.tsx"
      to: "web/components/ui/Drawer.tsx"
      via: "controlled open/close and focus-return contract"
---

<objective>
Integrate and prove EpisodesBrowser with the URL-backed public-detail drawer over the completed ScrollRail/URL-state foundation.

Purpose: Validate the tokens/primitives/harness in a real protected workflow while preserving URL, data, navigation, accessibility, performance, privacy, and rollback contracts.
Output: Drawer-integrated Episodes modules, complete deterministic stories/journeys, and a passing route-specific gate in both motion modes and all three viewports.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@.planning/phases/01-compatibility-component-proof-harness/01-CONTEXT.md
@.planning/phases/01-compatibility-component-proof-harness/01-PATTERNS.md
@web/components/EpisodesBrowser.tsx
@web/components/DragRow.tsx
@web/components/ui/Drawer.tsx
@web/components/domain/public/MigratedEpisodesPage.tsx
@web/lib/episodes.ts
@web/tests/performance/phase1-budgets.json
</context>

<must_haves>
  <truths>D-01 through D-04 and D-09 are jointly asserted by Plans 01-23 and 01-11; the protected route remains `/episodes` and no path-segment route is created.</truths>
  <artifacts>Stories cover rail start/middle/end and drawer loading/timestamped/untimed/unavailable/error/invalid-ID states; journeys cover keyboard, URL history, focus, motion, viewports, privacy, and budgets.</artifacts>
  <key_links>The selected ID resolves only through the supplied public episode map; source/timestamp links use accepted public URLs and no operator projection.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-11 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-11.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-11, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate EpisodesBrowser into the accessible public EpisodeDrawer</name>
  <files>web/components/domain/public/EpisodeDrawer.tsx, web/components/EpisodesBrowser.tsx, web/components/EpisodesBrowser.stories.tsx, web/tests/security/phase1-threat-results/01-11.json</files>
  <read_first>web/components/domain/public/EpisodeDrawer.tsx if present (otherwise treat it as a create target); 01-CONTEXT.md D-01 through D-04; 01-UI-SPEC.md `/episodes`, EpisodeDrawer, responsive layout, copy, and public allowlist; web/components/EpisodesBrowser.tsx; web/lib/episodes.ts; web/components/ui/Drawer.tsx; web/components/patterns/ScrollRail.tsx; web/lib/public/contracts.ts</read_first>
  <behavior>
    - Valid ID opens a labelled modal and focuses its title; same-page close returns to the invoking card/rail position; direct-load close focuses the Episodes heading/results region.
    - Browser Back closes and Forward reopens; refresh/share reopen the same episode; unknown IDs show a non-leaking unavailable notice without a drawer.
    - Drawer preserves JSON transcript → plain-text fallback and explicit loading/timestamped/untimed/unavailable/offline/safe-error states.
    - Public detail is limited to title, show, thumbnail, duration, available views, transcript context/excerpt, verified source/timestamp links, and existing Ask WTF entry points.
  </behavior>
  <action>Per D-04, replace local `active` state and bespoke overlay with the Plan 01-23 URL selection/`ScrollRail` foundation and the proven WTF Drawer. Resolve IDs only against the public episode map; preserve unrelated queries, transcript fallback, Ask WTF links, source rel behavior, and formatters. Render exact state/copy contracts, verified-time-only affordances, and responsive drawer layouts while excluding every private/operator field. Add focused EpisodesBrowser story interactions in this task for valid/invalid ID, open/close/Escape, focus return/fallback, transcript states, safe source links, and all three responsive drawer modes; Task 2 expands these into the full evidence matrix. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-11 --task 1` so T-01-33 executes from its immutable definition and receives an atomic privacy-safe result entry; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: public drawer props/types contain only the D-04/UI-SPEC allowlist.
    - Behavior: all open/close/history/focus paths and transcript states have deterministic fixture coverage.
    - Accessibility: modal labelling, focus containment, visible close, Escape/backdrop, inert background, scroll containment, and fallback focus pass.
    - Responsive: 320, 768, and 1440 have no page overflow or hidden action; only labelled rail overflow is horizontal.
    - Security: verified timestamps alone produce time query/controls; unsafe or untimed sources do not.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-11 --task 1</automated>
    <expected>Exit 0; migrated source compiles and the public allowlist/source/timing contracts pass.</expected>
  </verify>
  <done>EpisodesBrowser is the complete locked first proof slice, URL-backed and public-only. Result fragment: T-01-33 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove Episodes stories, keyboard/focus, viewports, motion, privacy, performance, and rollback</name>
  <files>web/components/EpisodesBrowser.stories.tsx, web/tests/journeys/episodes.spec.ts, web/tests/journeys/focus.spec.ts, web/tests/journeys/motion.spec.ts, web/tests/security/phase1-threat-results/01-11.json</files>
  <read_first>web/components/EpisodesBrowser.stories.tsx, web/tests/journeys/episodes.spec.ts, web/tests/journeys/focus.spec.ts, and web/tests/journeys/motion.spec.ts if present (otherwise treat absent paths as create targets); web/components/EpisodesBrowser.tsx; web/components/domain/public/EpisodeDrawer.tsx; web/stories/fixtures/public.ts; 01-UI-SPEC.md required Episodes evidence; web/tests/performance/phase1-budgets.json; web/tests/rollback/episodes-variant.spec.ts</read_first>
  <behavior>
    - Stories cover page-ready, empty filter, rail start/middle/end, and every required drawer state with stable public fixture order.
    - Journeys cover keyboard card activation, Escape/close/backdrop, Back/Forward/refresh, focus return/fallback, immediate reduced motion, and viewport reflow.
    - Migrated Episodes metrics remain inside approved route/bundle/interaction budgets and legacy variant remains runnable.
  </behavior>
  <action>Create deterministic stories and Playwright journeys with frozen fonts/images/transcripts, serious/critical axe scans after relevant states open, explicit focus assertions, `prefers-reduced-motion` emulation including immediate runtime changes, overflow checks at exact viewports, forbidden-field/source URL scans, and approved-budget assertions. Record visual candidates as unapproved evidence only; do not replace approved baselines or change the default selector. Run the legacy/migrated variant proof after the migrated route tests pass. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-11 --task 2` so T-01-34 and T-01-35 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Command: stories, Episodes route journey, focus, motion, privacy, performance subset, and rollback variant tests all pass.
    - Accessibility: zero serious/critical axe findings plus explicit keyboard/focus behavior; canvas is not involved.
    - Visual: deterministic candidates exist for all specified states/widths but are not marked owner-approved.
    - Performance: all values are compared to `phase1-budgets.json` with no threshold changes.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-11 --task 2</automated>
    <expected>Exit 0; the first proof slice passes every automated contract while visual approval remains pending.</expected>
  </verify>
  <done>Episodes is the first fully proven migrated protected route and its legacy rollback remains intact. Result fragment: T-01-34, T-01-35 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Public episode IDs/details/transcripts, browser history/query integrity, safe source navigation, keyboard/focus access, and rollback path.

## Trust Boundaries
| Boundary | Description |
|---|---|
| URL query → public episode resolver | Untrusted IDs/encoding must not select arbitrary/private data or break other parameters. |
| transcript/source data → Drawer DOM/navigation | Content and URLs can create XSS, unsafe navigation, false timing, or artifact leaks. |
| modal/rail → keyboard and assistive technology | Pointer-only or broken focus can deny the workflow. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-11.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-33 | Information Disclosure | EpisodeDrawer DTO/artifacts | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 1 | cd web &amp;&amp; npm run test:components -- EpisodesBrowser --grep "drawer\|focus\|invalid\|transcript" &amp;&amp; npm run test:browser -- tests/journeys/url-state.spec.ts --grep "open\|close\|focus" &amp;&amp; npm run test:contracts -- tests/contracts/public-projection.contract.test.ts &amp;&amp; npm run typecheck | defined | ASVS V4/V8: explicit D-04 allowlist, forbidden-field scan, synthetic fixtures, no private transcript/source bodies. |
| T-01-34 | Tampering/XSS | transcript/source links | mitigate | critical | medium | high | v5.0.0-1.2.2 | Task 2 | cd web &amp;&amp; npm run test:components -- EpisodesBrowser &amp;&amp; npm run test:browser -- tests/journeys/episodes.spec.ts tests/journeys/focus.spec.ts tests/journeys/motion.spec.ts &amp;&amp; npm run test:privacy -- --check &amp;&amp; npm run test:rollback -- --route=/episodes | defined | Render text safely, no raw HTML, allowlisted public URLs, verified timing only, noreferrer external links. |
| T-01-35 | Denial of Service | rail/drawer accessibility | mitigate | high | medium | high | v5.0.0-2.2.1 | Task 2 | cd web &amp;&amp; npm run test:components -- EpisodesBrowser &amp;&amp; npm run test:browser -- tests/journeys/episodes.spec.ts tests/journeys/focus.spec.ts tests/journeys/motion.spec.ts &amp;&amp; npm run test:privacy -- --check &amp;&amp; npm run test:rollback -- --route=/episodes | defined | Native scroll, visible controls, keyboard paths, focus trap/return, reduced motion, 320 reflow, axe + explicit journeys. |
</threat_model>

<verification>
Run all Episodes stories/journeys/contracts/privacy/performance/rollback subsets and confirm no path-segment route or unapproved visual baseline replacement.
</verification>

<success_criteria>
EpisodesBrowser, ScrollRail, and URL-backed public drawer form the first accepted-quality migrated slice without breaking compatibility, privacy, accessibility, budgets, or rollback.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-11-SUMMARY.md` when done.
</output>
