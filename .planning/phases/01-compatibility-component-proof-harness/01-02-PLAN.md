---
phase: 01-compatibility-component-proof-harness
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - web/tests/package-legitimacy.json
  - .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
  - web/tests/security/phase1-threat-results/01-02.json
autonomous: false
requirements: [QUAL-01, QUAL-03, QUAL-04, QUAL-06, DSYS-04]
must_haves:
  truths:
    - "No npm package or Chromium revision is installed until the owner verifies every [ASSUMED] package against its canonical registry and source repository."
    - "The approval is version-specific and cannot authorize a silently changed package set."
  artifacts:
    - path: "web/tests/package-legitimacy.json"
      provides: "Blocking approval receipt for the exact Phase 1 harness package set"
  key_links:
    - from: "web/tests/package-legitimacy.json"
      to: "web/package.json"
      via: "exact name/version checksum checked by Plan 01-03 before npm install"
---

<objective>
Satisfy the package-legitimacy gate immediately before dependency installation.

Purpose: Prevent supply-chain substitution while honoring the research audit's `[ASSUMED]` status and unavailable slopcheck result.
Output: Owner-verified exact package approval and validation record; no packages installed.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-compatibility-component-proof-harness/01-RESEARCH.md
@.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
@web/tests/contracts/phase1-baseline-approval.json
</context>

<must_haves>
  <truths>The package checkpoint is blocking and never auto-approved; `[ASSUMED]` status remains visible.</truths>
  <artifacts>The receipt lists canonical npm and source URLs, exact versions, approval reference, and package-set hash.</artifacts>
  <key_links>Plan 01-03 exits before installation unless the receipt is approved and exactly matches the intended package set.</key_links>
</must_haves>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-02 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-02.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-02, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the exact ASSUMED package set</name>
  <files>web/tests/package-legitimacy.json, web/tests/security/phase1-threat-results/01-02.json</files>
  <read_first>web/tests/package-legitimacy.json if present (otherwise treat it as a create target); 01-RESEARCH.md Standard Stack, Planned Installation, Package Legitimacy Audit, and Fresh-release caution; web/package.json; web/tests/contracts/phase1-baseline-approval.json</read_first>
  <action>Present one table with the exact versions `eslint@9.39.5`, `storybook@10.5.9`, `@storybook/nextjs-vite@10.5.9`, `@storybook/addon-vitest@10.5.9`, `@storybook/addon-a11y@10.5.9`, `vite@8.2.1`, `vitest@4.1.11`, `@vitest/browser-playwright@4.1.11`, `@playwright/test@1.62.1`, `@axe-core/playwright@4.13.0`, `@lhci/cli@0.15.1`, and `@radix-ui/react-dialog@1.1.23`. Link each canonical npm page and upstream repository from the research audit, call out the fresh Storybook/Vitest publication date, and require explicit owner approval. Do not run npm, npm exec, npx, or any browser installer in this plan. On approval only, write the receipt with `status: approved`, exact entries, source URLs, approval reference, timestamp, and deterministic package-set SHA-256. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-02 --task 1` so T-01-SC-APPROVAL execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Human: owner explicitly approves all twelve exact package/version pairs after reviewing canonical registry/source links.
    - Source: every entry remains labelled `[ASSUMED]` with `slopcheck: unavailable`; none is represented as verified by slopcheck.
    - Behavior: any name/version difference makes the next plan's preflight fail closed.
    - Command: `git diff -- web/package.json web/package-lock.json` is unchanged by this plan.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-02 --task 1</automated>
    <expected>After explicit approval, exit 0 with twelve exact ASSUMED entries; otherwise execution remains paused and no package files change.</expected>
  </verify>
  <done>The owner has verified the exact package identities and versions without installing them. Result fragment: T-01-SC-APPROVAL are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto">
  <name>Task 2: Record the package gate without advancing Nyquist status</name>
  <files>.planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md, web/tests/security/phase1-threat-results/01-02.json</files>
  <read_first>web/tests/package-legitimacy.json; .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md; this plan's T-01-SC-APPROVAL threat</read_first>
  <action>Record Plan `01-02`, Wave 2, approval reference, package-set hash, and T-01-SC-APPROVAL under Test Infrastructure and Wave 0 Requirements. Keep every infrastructure file that does not exist marked pending, and retain `wave_0_complete: false` plus `nyquist_compliant: false`. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-02 --task 2` so T-01-05, T-01-06 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Source: Validation names `01-02`, the approval artifact, and T-01-SC-APPROVAL.
    - Behavior: approval does not imply installation or harness readiness.
    - Command: completion flags remain false.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-02 --task 2</automated>
    <expected>Exit 0; the gate is traceable and Phase 1 validation remains draft/incomplete.</expected>
  </verify>
  <done>The package approval is durable, exact, and visibly distinct from installation or test completion. Result fragment: T-01-05, T-01-06 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Assets
- Package-lock integrity, developer/CI execution environment, and repository credentials reachable by install scripts.

## Trust Boundaries
| Boundary | Description |
|---|---|
| npm registry metadata → repository dependency set | Registry names can be confused or replaced; all proposed packages are `[ASSUMED]`. |
| owner approval → installer | Approval must bind the exact immutable list, not a package family or range. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-02.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-SC-APPROVAL | Tampering | npm package set | mitigate | high | medium | high | v5.0.0-15.1.1 | Task 1 | node -e 'const a=require("./web/tests/package-legitimacy.json"); if(a.status!=="approved"\|\|a.packages.length!==12\|\|a.packages.some(p=&gt;p.slopcheck!=="unavailable"\|\|p.disposition!=="ASSUMED"\|\|!p.registry_url\|\|!p.source_url)) process.exit(1)' | defined | ASVS V14 dependency/config integrity: blocking human verification of canonical npm/source pages and exact versions before install; reject drift by package-set hash. |
| T-01-05 | Spoofing | similarly named packages | mitigate | high | medium | high | v5.0.0-15.1.1 | Task 2 | node -e 'const s=require("fs").readFileSync(".planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md","utf8"); if(!s.includes("01-02")\|\|!s.includes("T-01-SC-APPROVAL")\|\|!s.includes("wave_0_complete: false")\|\|!s.includes("nyquist_compliant: false")) process.exit(1)' | defined | Require canonical scope/name, registry URL, repository URL, age/download evidence, and exact version for all twelve entries. |
| T-01-06 | Elevation of Privilege | install lifecycle scripts | mitigate | high | low | high | v5.0.0-15.1.1 | Task 2 | node -e 'const s=require("fs").readFileSync(".planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md","utf8"); if(!s.includes("01-02")\|\|!s.includes("T-01-SC-APPROVAL")\|\|!s.includes("wave_0_complete: false")\|\|!s.includes("nyquist_compliant: false")) process.exit(1)' | defined | Preserve audit evidence that registry metadata reports no postinstall; Plan 01-03 inspects resolved scripts before accepting lockfile. |
</threat_model>

<verification>
Confirm the package receipt and unchanged package files; do not run any installation command in this plan.
</verification>

<success_criteria>
The exact package set is owner-approved and hash-bound, and dependency installation remains the next immediate gated action.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-component-proof-harness/01-02-SUMMARY.md` when done.
</output>
