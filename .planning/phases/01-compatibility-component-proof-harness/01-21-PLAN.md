---
phase: 01-compatibility-component-proof-harness
plan: "21"
type: execute
wave: 8
depends_on: ["01-07", "01-05"]
files_modified:
  - web/scripts/capture-legacy-presentation.mjs
  - web/tests/contracts/legacy-presentation-dependencies.json
  - web/tests/visual/legacy/phase1-legacy-baseline.manifest.json
  - web/tests/visual/legacy/public-routes.spec.ts-snapshots/
  - web/tests/rollback/legacy-presentation.spec.ts
  - .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md
  - web/tests/security/phase1-threat-results/01-21.json
autonomous: false
requirements: [COMP-01, COMP-02, QUAL-02, QUAL-04, QUAL-05]
must_haves:
  truths:
    - "Before any global, token, shell, route, or shared-brand mutation, the complete accepted legacy presentation boundary is hash-bound and owner-approved."
    - "The legacy boundary includes global CSS, Tailwind/theme configuration, layout, all protected route entries, and every transitive shared presentation dependency."
    - "Approved pre-migration visual and behavioral evidence exists at 320, 768, and 1440 for all four protected UI routes."
  artifacts:
    - path: "web/tests/contracts/legacy-presentation-dependencies.json"
      provides: "Complete repository-relative legacy CSS/theme/route/shared-component dependency graph and hashes"
    - path: "web/tests/visual/legacy/phase1-legacy-baseline.manifest.json"
      provides: "Owner-approved 320/768/1440 pre-migration visual and behavior baseline"
  key_links:
    - from: "web/scripts/capture-legacy-presentation.mjs"
      to: "web/tests/contracts/legacy-presentation-dependencies.json"
      via: "transitive import/style dependency discovery and SHA-256 binding"
    - from: "web/tests/rollback/legacy-presentation.spec.ts"
      to: "web/tests/visual/legacy/phase1-legacy-baseline.manifest.json"
      via: "exact route/state/viewport screenshot and behavioral comparison"
---

<objective>
Freeze and owner-approve the complete pre-migration public presentation before scoped tokens or migrated components are introduced.

Purpose: Ensure legacy mode restores the actual prior CSS, theme, shared dependencies, visuals, and behavior—not merely route contracts.
Output: Complete legacy dependency graph, deterministic three-viewport baseline, behavioral comparison suite, approval receipt, and validation mapping.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-compatibility-component-proof-harness/01-CONTEXT.md
@.planning/phases/01-compatibility-component-proof-harness/01-RESEARCH.md
@.planning/phases/01-compatibility-component-proof-harness/01-UI-SPEC.md
@web/tests/contracts/phase1-baseline-approval.json
@web/app/globals.css
@web/tailwind.config.ts
@web/app/layout.tsx
@web/app/page.tsx
@web/app/episodes/page.tsx
@web/app/connections/page.tsx
@web/app/chat/page.tsx
</context>

<execution_result_contract>
The `<threat_model>` rows below are immutable definitions. Every task owning one or more threat rows is incomplete until it invokes `node web/scripts/run-phase1-threat.mjs --plan 01-21 --task &lt;task-number&gt;`. The runner parses the exact commands from the owning PLAN, executes each unique command once, and atomically writes only `web/tests/security/phase1-threat-results/01-21.json` through same-directory temporary-file rename. The fragment schema is `{ schema_version, plan, results }`; `results` is keyed by threat ID and each value contains exactly `plan`, `task`, `command_id`, `command`, `exit_status`, bounded digest-based `evidence`, ISO-8601 `completed_at`, and `status: passed|failed`. It rejects threat IDs not owned by 01-21, schema/owner/task/command drift, future timestamps, prohibited fields, credentials, secret values, raw stdout/stderr, request or prompt bodies, private payloads, environment values, and machine-local paths. A failed command is recorded as failed before the runner returns the same nonzero exit. No task writes any other plan fragment, the aggregate ledger, a PLAN definition, or mutable pass/fail state into `01-VALIDATION.md`.
</execution_result_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inventory and hash the complete legacy presentation dependency boundary</name>
  <files>web/scripts/capture-legacy-presentation.mjs, web/tests/contracts/legacy-presentation-dependencies.json, web/tests/security/phase1-threat-results/01-21.json</files>
  <read_first>Approved compatibility manifest/receipt; globals; Tailwind config; protected route entries; layout; every transitive repository-owned presentation import, including all shared components named in CONTEXT</read_first>
  <behavior>
    - Starting from globals, theme config, layout, and all protected routes resolves every repository-owned CSS/module dependency transitively.
    - Every entry records repository-relative path, role, dependents, dependencies, and SHA-256; missing or unresolved presentation dependencies fail.
    - Raw bodies, absolute paths, environment values, prompts, responses, and private payloads never enter the artifact.
  </behavior>
  <action>Per D-12, create a read-only deterministic capture command that parses CSS and TypeScript/TSX imports from all protected public presentation roots. Classify global style/theme/shell/route/shared roles, include discovered transitive dependencies rather than a hand-maintained partial list, bind the graph to the approved compatibility manifest, and fail on unresolved local imports, drift, absolute paths, or omitted roots. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-21 --task 1` so T-01-65 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Coverage: globals, Tailwind/theme config, layout, all four protected route entries, and every resolved repository-owned transitive presentation dependency appear exactly once.
    - Integrity: every node has repository-relative path, role, dependency/dependent edges, SHA-256, and binding to the approved compatibility manifest; unresolved, omitted, duplicate, or drifted nodes fail.
    - Privacy: the graph contains no source bodies, absolute paths, environment values, prompts/responses, or private payloads.
    - Evidence: `phase1-threat-results/01-21.json` contains exactly one passing T-01-65 entry bound to Task 1 and its immutable command.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-21 --task 1</automated>
    <expected>Exit 0; the complete transitive presentation graph is hash-bound, privacy-safe, drift-free, and T-01-65 passes in the 01-21 fragment.</expected>
  </verify>
  <done>The complete accepted CSS/theme/route/shared-component graph is privacy-safe and hash-bound before mutation. Result fragment: T-01-65 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture pre-migration visual and behavioral evidence at all required widths</name>
  <files>web/tests/visual/legacy/phase1-legacy-baseline.manifest.json, web/tests/visual/legacy/public-routes.spec.ts-snapshots/, web/tests/rollback/legacy-presentation.spec.ts, web/tests/security/phase1-threat-results/01-21.json</files>
  <read_first>Legacy dependency graph; UI-SPEC capture/state matrix; approved compatibility receipt; Playwright config; deterministic fixtures</read_first>
  <behavior>
    - All four UI routes capture ready plus applicable focus, open/selected, loading, empty, safe-error, and reduced-motion states at 320, 768, and 1440.
    - Behavior checks preserve navigation, query meaning, keyboard paths, focus targets, sources, and current public data semantics.
    - Hashes bind pinned Chromium/container/fixture/dependency identity and are not self-approved.
  </behavior>
  <action>Per D-14, build the accepted current presentation fresh and capture deterministic screenshots and behavioral traces with frozen fonts/media/network. Create a rollback comparison spec that asserts the route/state/viewport matrix against the manifest, not only HTTP success. Keep status pending and fail on missing coverage, unstable assets, or private/machine-local metadata. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-21 --task 2` so T-01-66 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Coverage: `/`, `/episodes`, `/connections`, and `/chat` include ready plus every applicable focus/open/loading/empty/safe-error/reduced-motion state at 320, 768, and 1440.
    - Behavior: navigation, supported query meaning, keyboard order, focus targets, source semantics, and current public data semantics match the accepted baseline.
    - Authority: the manifest remains `pending-owner-approval` and binds exact browser/container/fixture/dependency hashes without self-approval.
    - Privacy: the artifact scan passes and T-01-66 is the only new Task 2 entry in `phase1-threat-results/01-21.json`.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-21 --task 2</automated>
    <expected>Exit 0; every required route/state/viewport capture and behavior comparison passes, privacy is clean, approval remains pending, and T-01-66 passes.</expected>
  </verify>
  <done>Complete deterministic pre-migration visual and behavioral evidence exists at 320/768/1440. Result fragment: T-01-66 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve the complete pre-migration legacy presentation baseline</name>
  <files>web/tests/visual/legacy/phase1-legacy-baseline.manifest.json, .planning/phases/01-compatibility-component-proof-harness/01-VALIDATION.md, web/tests/security/phase1-threat-results/01-21.json</files>
  <read_first>Dependency graph, visual/behavior manifest, all captures, D-12, D-14, D-16, and threat register</read_first>
  <action>Present the complete inventory and every route/state/viewport capture. On explicit approval only, set status approved and record graph/snapshot hashes, browser/container/fixture identity, owner reference, and accepted known behavior. Map Plan 01-21 and T-01-65..67 into Validation. Plans 01-08 and 01-10 fail if approval, coverage, or any protected legacy hash drifts. Before completing the task, invoke `node web/scripts/run-phase1-threat.mjs --plan 01-21 --task 3` so T-01-67 execute from their immutable definitions and receive atomic privacy-safe result entries; any failed or missing entry blocks completion.</action>
  <acceptance_criteria>
    - Human: explicit owner approval covers the complete dependency graph and every required route/state/viewport capture.
    - Receipt: status is `approved` and owner reference, dependency graph hash, snapshot hashes, browser/container/fixture identity, accepted behavior, and 320/768/1440 coverage are present exactly once.
    - Mapping: Validation names Plan 01-21, Wave 8, and T-01-65 through T-01-67 without changing completion flags.
    - Evidence: T-01-67 passes in `phase1-threat-results/01-21.json`; drift in any protected legacy hash blocks dependents.
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/run-phase1-threat.mjs --plan 01-21 --task 3</automated>
    <expected>After explicit owner approval, exit 0 with an approved hash-bound three-viewport oracle, exact Validation mapping, and passing T-01-67; otherwise remain paused.</expected>
  </verify>
  <done>The owner-approved pre-migration presentation is an executable rollback oracle before shared mutation. Result fragment: T-01-67 are recorded `passed` for this exact plan/task/command before completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| current public source → legacy manifest | Incomplete traversal or stale hashes could bless a partial rollback target. |
| browser capture → owner approval | Unstable or private evidence could mislead approval or disclose data. |

## STRIDE Threat Register

These rows are immutable design-time threat definitions. Their definition status remains `defined`; execution never edits PLAN files. The owning task executes the exact command through the Phase 1 threat runner and records mutable pass/fail evidence only in `web/tests/security/phase1-threat-results/01-21.json`. Missing, malformed, drifted, or failed high/critical entries block candidate checking and final aggregation.

| Threat ID | Category | Component | Disposition | Severity | Likelihood | Impact | ASVS 5.0 L1 | Owning task | Automated command | Definition status | Mitigation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01-65 | Tampering | dependency graph | mitigate | high | medium | high | v5.0.0-2.1.1 | Task 1 | cd web &amp;&amp; node scripts/capture-legacy-presentation.mjs --check | defined | Resolve all transitive imports, hash every node, reject omissions/drift. |
| T-01-66 | Information Disclosure | baseline artifacts | mitigate | high | medium | high | v5.0.0-15.3.1 | Task 2 | cd web &amp;&amp; npm run test:browser -- tests/rollback/legacy-presentation.spec.ts &amp;&amp; npm run test:privacy -- --check | defined | Synthetic fixtures, bounded metadata, repository-relative paths, privacy scan. |
| T-01-67 | Repudiation | legacy approval | mitigate | high | low | high | v5.0.0-2.1.1 | Task 3 | node -e 'const m=require("./web/tests/visual/legacy/phase1-legacy-baseline.manifest.json"); if(m.status!=="approved"\|\|!m.owner_approval_ref\|\|!m.dependency_graph_sha256\|\|!["320","768","1440"].every(x=&gt;m.coverage?.includes(x))) process.exit(1)' | defined | Owner reference binds exact graph, pixels, behavior, browser, and coverage hashes. |
</threat_model>

<verification>
Require graph capture, all route/state/viewport comparisons, privacy scan, and explicit owner approval before dependent plans start.
</verification>

<success_criteria>
Legacy mode has an owner-approved visual and behavioral oracle at 320/768/1440 before any global or shared presentation change.
</success_criteria>

<output>
Create .planning/phases/01-compatibility-component-proof-harness/01-21-SUMMARY.md when done.
</output>
