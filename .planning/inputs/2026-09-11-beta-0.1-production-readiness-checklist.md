# Beta 0.1 production-readiness checklist

**Recorded:** 2026-09-11
**Scope:** The authenticated Beta 0.1 release lane only. It does not complete
the broader v1.0 roadmap or authorize an external action.

## Evidence classification

This checklist keeps source, environment, and acceptance evidence separate:

| Marker | Meaning | Can satisfy a staging or production gate? |
|---|---|---|
| `VERIFIED-SOURCE` | Reproducible from this checkout with Git or local commands | No, unless the gate is explicitly source-only |
| `STAGING-DEPLOYED` | Exact source candidate is deployed to paired staging web/edge with receipts | Only the named staging gate |
| `REPORTED` | Supplied in a handoff or conversation without a host-visible receipt | No |
| `STAGING-LIVE` | Canonical staging hostname/TLS/ingress proof and signed-out behavior are verified for that candidate | Only the named staging gate |
| `STAGING-OBSERVED-PARTIAL` | Real persona/runtime observation for partial matrices only | No |
| `STAGING-BLOCKED` | Functional blocker remains open and blocks the named gate | No |
| `REPORTED-SUPERSEDED` | Prior report remains in the chain but cannot be used as active evidence | No |
| `STAGING-ACCEPTED` | Exact deployed revision, environment receipts, and real authenticated IAB observations | Only the named staging gate |
| `PRODUCTION-ACCEPTED` | Owner-authorized production evidence for the exact deployed revision | Only the named production gate |

## Canonical staging-SHA governance

- **Canonical packet SHA:** `BETA-REL-02` is bound to `f6bbf584fa5c9a07cc218f0211f3c5020bb4f3f7` (`f6bbf58`).
- Promotions and subsequent deployment rows in this packet must use either that full SHA or an explicitly documented runtime-equivalent artifact tuple with deployment receipts.
- If acceptance requires an evidence-bearing source change, update `BETA-REL-02` only after the replacement full SHA is deployed to the paired staging Workers and receives fresh source, deployment, and acceptance receipts; the former SHA then becomes historical provenance.
- `abb5413` and `32ec582` remain historical lineage artifacts and must not be used as the active `BETA-REL-02` identity.
- Truncated or guessed IDs (for example, `078f582`) are invalid for staging promotion unless a full SHA equivalence receipt is recorded.

## Current evidence register

| Claim | Classification | Evidence / limitation |
|---|---|---|
| PR #77's `beta_0.1` content is locally integrated | `VERIFIED-SOURCE` | `d3b4590fb86966b2a3b4c69ab2c8fea9aa0a8b7a` is a local merge commit on `rag/alpha-answer-accuracy`; its parents are `d4e45b44f3527b768408069f191951c2c3f492cb` and `7ec8298664a40b7b138f2b8d042d89858645cb67`. |
| User-reported merge `3eebf57` | `REPORTED` | `git cat-file -t 3eebf57` is unavailable in this host's object database. Do not substitute the reported SHA for `d3b4590` or treat either SHA as deployed. |
| Exact source candidate | `STAGING-DEPLOYED` | `f6bbf584fa5c9a07cc218f0211f3c5020bb4f3f7` contains `32ec582` plus retried/typed chat reads, canonical account/logout and preferences surfaces, archived-history queries, and a live Sessions manager. It is deployed only to the paired staging Workers; production was not changed. |
| Exact-candidate checks | `VERIFIED-SOURCE` | At `f6bbf58`: Edge 380/380, web unit 219/219, web contracts 96/96, TypeScript, lint, production/OpenNext build (89 pages), privacy 0 violations across 400 files, and `git diff --check` pass. Architecture freshness is regenerated by the documentation receipt that follows this code commit. |
| Earlier local runtime report | `REPORTED-SUPERSEDED` | The earlier report of a local full-stack flow and remote D1 migrations was not itself environment evidence. The current named-profile staging receipts below independently establish the actual `wtfmedia-ops-staging` migration state and deployments; the earlier report still proves nothing about production. |
| Existing `v0.3.3-beta.2` tag | `VERIFIED-SOURCE` | The remote annotated tag exists and peels to `66f434ab152fd2776c961aed21926e76b5df9204`, while `docs/releases/v0.3.3-beta.2.md` says the candidate was neither a tag nor a deployment receipt. This release-history contradiction must be resolved explicitly; the tag cannot be reused or silently reinterpreted. |
| Remote PR topology | `VERIFIED-SOURCE` | GitHub reports PR #77 still open from `beta_0.1` into `release/beta`, while PR #48 is open from `rag/alpha-answer-accuracy` into `main`. The local branch is ahead of its remote. Pushing it would materially expand PR #48, so remote delivery requires an explicit owner decision. |
| Pavun57 precedence | `VERIFIED-SOURCE` | PR #48 head `d4e45b4` is an ancestor of the candidate. PR #49 head `1e40ef2` is not an ancestor as a patch, but its chat-history, request-budget, local-secret-ignore, and match-confidence intent is preserved. Current Beta UI/routes/Clerk/RBAC/admin/Settings remain authoritative. |
| Beta lineage and surface map | `VERIFIED-SOURCE` | `v0.3.3-beta.2` is older `main`, `release/beta` is the merged Member lane, `beta_0.1` adds principal/RBAC/admin plus a role-based operator chat fork, Pavun57 owns RAG behavior, and `abb5413` is their local staged predecessor. See the dedicated lineage audit; do not use "latest Beta" as an exact ref. |
| Beta hostname availability | `STAGING-LIVE` | Cloudflare accepted `beta-staging.wtfhq.in` as the `wtfmedia-web-staging` Custom Domain. Its authoritative nameservers return Cloudflare A records, TLS exact-host probes pass, `/` redirects to `/beta`, and the signed-out principal endpoint returns `401`. Codex IAB now resolves the canonical hostname and authenticated the existing super-admin. `beta.wtfhq.in` remains unassigned live infrastructure. |
| Super-admin IAB observation | `STAGING-OBSERVED-PARTIAL` | At web version `b67a0833-3eda-4c53-b6ed-056969200350`, two independent IAB passes confirm the existing super-admin resolves to `/beta/workspace`, opens the shared Alpha-derived `/beta/chat`, reopens owner-scoped `cnv_*` conversations, sees clamped history cards, no bottom dock or ingest destination, the reduced Settings map, and the real D1-backed operator roster. Operator conversation and roster endpoints returned 200 with no HTTP 4xx/5xx observed. This is one-persona visual/runtime evidence only and does not satisfy the full persona or viewport gates. |
| Staging grounded-chat readiness | `STAGING-BLOCKED` | The isolated staging catalogue has 0 R2 objects, 0 KV catalogue keys, and 0 Vectorize records. Chat requests persist, then truthfully fall back without grounded evidence. A provenance-safe staging corpus operation is separately owner-gated; production data must not be copied down. |

## Environment and client-handoff hostname contract

| Lane | Canonical client URL | Public ingress | Paired edge | Binding boundary |
|---|---|---|---|---|
| Public Alpha/current production | `https://wtfhq.in` | `wtfmedia-web` | `wtfmedia-edge` | Existing production resources; unchanged by Beta hostname work |
| Beta staging acceptance | `https://beta-staging.wtfhq.in` | `wtfmedia-web-staging` Custom Domain | `wtfmedia-edge-staging` through `WTFMEDIA_EDGE` | Staging Clerk and staging D1/R2/KV/Vectorize/queues only |
| Beta production/client handoff | `https://beta.wtfhq.in` | planned dedicated `wtfmedia-web-beta` Custom Domain | planned dedicated `wtfmedia-edge-beta` through `WTFMEDIA_EDGE` | Explicit production bindings after approval; no inherited staging binding and no duplicate ingest consumer |
| Diagnostic origin | environment-specific `workers.dev` URL | Direct Worker diagnostic only | Never a client handoff URL | Reachability does not prove Custom Domain, binding, revision, or authorization identity |

The staging Beta hostname is live; the production Beta hostname remains a
planning target. The web Worker is the only public ingress. The edge Worker
remains reachable from web through the paired service binding. Each Beta
hostname root must resolve or redirect canonically to `/beta`; host-only
session cookies must prevent Alpha, staging, and production-Beta sessions from
mixing.

## Release criteria

### A. Source closure before any staging request

- [x] **BETA-REL-01:** Runtime/UI closure is commit `79a0285`; release/migration guardrails are commit `b7f5bec`; planning and generated architecture are commit `2c4007d`.
- [x] **BETA-REL-02:** The exact staging-deployed source candidate is `f6bbf58`, which contains the prior candidate, Pavun57 chat/backend precedence reconciliation, and canonical staging host routing.
- [x] **BETA-REL-03:** At the source-equivalent `f6bbf58` candidate, Edge 380/380, web unit 219/219, web contracts 96/96, typecheck, lint, production/OpenNext build (89 pages), privacy 0/400, and `git diff --check` pass. `architecture` freshness is recorded in the exact-candidate receipt row above.
- [x] **BETA-REL-04:** Source review confirms the single-shell contract: `/beta` resolves the principal landing; both admitted principal kinds use role-safe `/beta/chat`; operators retain `/beta/workspace`; edge/D1 remains authoritative; public Alpha stays separate; `/beta/preview` is absent; and fake member/fallback data is not used as evidence.
- [ ] **BETA-REL-04A:** Reconcile the existing `v0.3.3-beta.2` remote tag against its source-only release note and record the owner decision. The next candidate manifests use the unused `0.3.3-beta.3`; do not delete, move, reuse, or reinterpret the historical tag implicitly.
- [x] **BETA-REL-04B:** Remote delivery topology is explicit: leave Pavun57's PR #48 head `rag/alpha-answer-accuracy` untouched; after the exact candidate passes the remaining staging gates, fast-forward the dedicated remote `beta_0.1` branch with an explicit `HEAD:beta_0.1` refspec and update PR #77 into `release/beta`. Never push the current local branch by its checked-out name, force-push either branch, or use a moving branch name as the accepted release identity.
- [x] **BETA-REL-04C:** Apply the owner precedence map: current work governs UI, canonical Beta/Ops routing, Clerk/auth landing and return flow, navigation pill, RBAC, admin, and Settings; Pavun57 governs chat, inference, retrieval, citations, and backend chat behavior. Shared files are resolved symbol-by-symbol.
- [x] **BETA-REL-04D:** `web/wrangler.jsonc` maps `beta-staging.wtfhq.in` only to `wtfmedia-web-staging`; its `WTFMEDIA_EDGE` service binding names only `wtfmedia-edge-staging`. Edge staging declares only staging D1/R2/KV/Vectorize/queues and the canonical origin/authorized party. Planned `beta.wtfhq.in` remains absent from live routes and requires its dedicated production-Beta pair.
- [ ] **BETA-REL-04E:** Exact-host tests now prove both Beta roots map to `/beta`, `wtfhq.in` and `workers.dev` do not receive the Beta-host redirect, spoofed suffixes fail safely, the app config does not enable Clerk satellite/domain sharing, and no diagnostic origin is emitted by staging config. Close this row only after IAB verifies the actual Clerk cookie is host-only on the canonical staging hostname.
- [x] **BETA-REL-04F:** Exact Git ancestry distinguishes the historical beta tags, `origin/release/beta`, PR #77's `beta_0.1`, Pavun57's RAG head, local merge `d3b4590`, and staged `f6bbf58` (with `abb5413` and `32ec582` retained as historical lineage rows); the surface authority is recorded in the Beta 0.3 lineage audit.
- [x] **BETA-REL-04G:** `/beta/chat` now uses the accepted Member/Alpha-derived presentation for both principal kinds through typed owner-scoped adapters, including member/operator boundaries, retrieval surfaces, and evidence/citation rendering. Member `mcnv_*` and operator `cnv_*` stores, lifecycle capabilities, RBAC/admin surfaces, and Pavun57 inference behavior remain distinct and intact.
- [x] **BETA-REL-04H:** Settings has one canonical account/memory/sessions/appearance lane for every admitted principal, with capability-gated release/users/audit additions for operators. Account exposes Clerk logout; Memory exposes the real immutable create/archive preference lifecycle; Sessions reads active plus archived owner history. Member conversations expose confirmed tombstone-backed Delete, while operator history remains truthfully archive-only until a reviewed delete/tombstone contract exists.

### B. Staging acceptance for the exact candidate

- [x] **BETA-REL-05:** Before applying migrations, D1 Time Travel returned bookmark `0000004a-00000000-000050e3-726c447ceba084a14759af047d82a48e`; the staging migration inventory showed only `0012`–`0015` pending.
- [x] **BETA-REL-06:** Wrangler applied `0012_member_chat_deletion.sql` through `0015_principal_profiles_email_guard.sql` to `wtfmedia-ops-staging`; post-apply inventory reports no migrations pending. Read-only readiness confirms the required tables, one active super-admin for the owner account, nine profile mirrors, and staging member-Beta state `preview` without recording private row content.
- [x] **BETA-REL-07:** *(Historical provenance preserved)* Candidate `abb5413` is deployed to `wtfmedia-edge-staging` as version `3b4aa27f-6c62-4077-a432-897f10002039`. Wrangler readback shows staging-only bindings and the canonical Clerk authorized party; `/v1/health` returns HTTP 200 with `environment=staging`, `service=wtfmedia-edge-staging`, and `index=wtfmedia-catalogue-staging-v1`. This remains historical to the current `f6bbf58` packet identity.
- [x] **BETA-REL-08:** *(Historical provenance preserved)* Candidate `32ec582` is deployed to `wtfmedia-web-staging` as version `b67a0833-3eda-4c53-b6ed-056969200350`. Wrangler retains `beta-staging.wtfhq.in`, `ASSETS`, self-reference, and only the `wtfmedia-edge-staging` service binding. Staging edge remains version `3b4aa27f-6c62-4077-a432-897f10002039`; production web/edge versions remained unchanged. This remains historical to the current `f6bbf58` packet identity.
- [x] **BETA-REL-08B:** Authenticated super-admin IAB proves the repaired staging shell: shared chat presentation, persisted operator history, zero document-width overflow, no chat/settings bottom dock, no ingest destination, reduced settings navigation, and a real D1-backed operator roster. This closes the named regression only, not BETA-REL-09 through BETA-REL-11.
- [x] **BETA-REL-08C:** Candidate `f6bbf58` is deployed as edge version `427b1ea7-4e2a-4a8e-86c1-1920c0741e6c` and web version `3668390f-1dc8-438e-9dab-19c331b732ce`. Authenticated super-admin IAB confirms chat history loads through the operator route, Account shows logout, duplicate workspace memory/session links are absent, Preferences loads its real empty owner ledger and create form, and Sessions loads both current conversations through `includeArchived=1`. No archive, delete, memory, roster, release, Clerk, DNS, or production mutation was performed during acceptance.
- [ ] **BETA-REL-08A:** Populate or otherwise approve a provenance-safe staging evidence corpus and prove non-zero, internally consistent R2/KV/Vectorize receipts before using grounded Ask WTF as an acceptance test. This gate forbids copying production data down as an implicit shortcut.
- [ ] **BETA-REL-09:** At `https://beta-staging.wtfhq.in`, use the Codex in-app browser to prove signed-out denial/recovery and Member A/Member B isolation across private chat, history, archive/delete, settings, logout, and fresh reauthentication.
- [ ] **BETA-REL-10:** In the Codex in-app browser, prove editor, admin, super-admin, and suspended/revoked route, screen, action, direct-URL, and server-policy behavior, including the shared `/beta/chat` role-safe history view; super-admin landing must not create a member row.
- [ ] **BETA-REL-11:** Record the real authenticated `https://beta-staging.wtfhq.in` flow at 1382x887, 1382x1180, and 320x710, including environment identity, drawer focus, composer/source reachability, long titles, session actions, and no protected-content flash.
- [ ] **BETA-REL-12:** Rehearse and record the staging rollback for the exact web/edge versions and confirm that the public Alpha routes and paired data planes remain isolated.

IAB note: the canonical hostname resolves in Codex IAB and the existing
super-admin authenticates. The `f6bbf58` candidate evidence adds the no-surprises account/preferences/sessions fixes on top of earlier `32ec582` work. Grounded evidence still cannot be produced because staging R2, KV, and Vectorize are empty. Operator permanent Delete remains unavailable because that server store has no reviewed delete/tombstone contract. Do not close BETA-REL-09 through BETA-REL-11 from this partial one-persona observation.

### C. Production promotion after staging acceptance

- [ ] **BETA-REL-13:** Obtain a fresh owner authorization naming the accepted staging evidence, BETA-REL-02 SHA, production target, release window, and rollback owner.
- [ ] **BETA-REL-14:** Capture a production Clerk configuration receipt authorizing exactly `https://beta.wtfhq.in`, including issuer/JWKS/authorized-party and redirect/domain policy presence; record no secrets or session material and do not reuse the staging Clerk project.
- [ ] **BETA-REL-15:** Capture a production D1 backup/recovery and migration inventory receipt before any production migration.
- [ ] **BETA-REL-16:** Apply the reviewed production migrations for BETA-REL-02 and record the post-apply migration/readiness receipt separately from staging.
- [ ] **BETA-REL-17:** Deploy and verify the exact BETA-REL-02 Edge revision to dedicated `wtfmedia-edge-beta` before web traffic changes; record version, health, explicit production binding presence, and absence of any duplicate ingest consumer.
- [ ] **BETA-REL-18:** Deploy and verify the exact BETA-REL-02 web revision to dedicated `wtfmedia-web-beta`, attach the `beta.wtfhq.in` Custom Domain, and record version, certificate/domain activation, `ASSETS`, and binding only to `wtfmedia-edge-beta`.
- [ ] **BETA-REL-19:** Record `beta.wtfhq.in` custom-domain and traffic attribution to the exact production-Beta web revision, then perform the approved read-only production smoke without assuming reachability proves revision identity; `wtfhq.in` must remain attributed to public Alpha.
- [ ] **BETA-REL-20:** Rehearse or execute the approved rollback path with exact prior web/edge versions, D1 recovery decision, domain/traffic recovery, and post-rollback read-only smoke.

All production promotion rows (`BETA-REL-13` through `BETA-REL-20`) therefore require explicit receipts tied to the then-current full SHA in `BETA-REL-02` (currently `f6bbf58`) and may not use `078f582` or any other SHA unless an equivalence receipt is attached.

## Explicitly deferred from the Beta 0.1 exit decision

These remain work items, not hidden promises or evidence gaps that may be
silently inferred from the Beta release:

- **BETA-DEFER-01 — provider and YouTube persistence:** AI Route and YouTube
  Analytics remain non-persisted local previews, synthetic, and non-authoritative.
  No fake production corpus shortcuts are authorized until a separately approved
  provider integration, data contract, and lifecycle exists.
- **BETA-DEFER-02 — scheduled jobs:** YouTube/provider refresh, reporting, and
  other scheduled jobs require separately approved source access, schedule,
  failure handling, and operational receipts.
- **BETA-DEFER-03 — long-context compaction:** no claim of automatic
  compaction, durable full-session continuity, or context-checkpoint recovery
  is allowed until paging, checkpoint, failure, and evaluation criteria pass.
- **BETA-DEFER-04 — full Alpha alignment:** the release may verify the
  explicitly listed authenticated Beta routes and viewports, but it does not
  certify a wholesale merge, complete visual/interaction parity, or all future
  Alpha capability adoption.

## Handoff rule

Future agents must append evidence to the applicable checklist row rather than
upgrading a `VERIFIED-SOURCE` or `REPORTED` fact into `STAGING-ACCEPTED` or
`PRODUCTION-ACCEPTED`. A failed, unknown, missing, or mismatched exact-revision
receipt blocks the corresponding release decision.
