# Beta 0.1 production-readiness checklist

**Recorded:** 2026-09-11
**Scope:** The authenticated Beta 0.1 release lane only. It does not complete
the broader v1.0 roadmap or authorize an external action.

## Evidence classification

This checklist keeps source, environment, and acceptance evidence separate:

| Marker | Meaning | Can satisfy a staging or production gate? |
|---|---|---|
| `VERIFIED-SOURCE` | Reproducible from this checkout with Git or local commands | No, unless the gate is explicitly source-only |
| `STAGING-DEPLOYED` | Exact source candidate is deployed to its changed staging service and paired runtime versions are receipted | Only the named staging gate |
| `REPORTED` | Supplied in a handoff or conversation without a host-visible receipt | No |
| `STAGING-LIVE` | Canonical staging hostname/TLS/ingress proof and signed-out behavior are verified for that candidate | Only the named staging gate |
| `STAGING-OBSERVED-PARTIAL` | Real persona/runtime observation for partial matrices only | No |
| `STAGING-BLOCKED` | Functional blocker remains open and blocks the named gate | No |
| `REPORTED-SUPERSEDED` | Prior report remains in the chain but cannot be used as active evidence | No |
| `STAGING-ACCEPTED` | Exact deployed revision, environment receipts, and real authenticated IAB observations | Only the named staging gate |
| `PRODUCTION-ACCEPTED` | Owner-authorized production evidence for the exact deployed revision | Only the named production gate |

## Canonical staging-SHA governance

- **Canonical packet SHA:** `BETA-REL-02` is bound to `409a8ae205c515e453477703bc2342744231006d` (`409a8ae`).
- Promotions and subsequent deployment rows in this packet must use either that full SHA or an explicitly documented runtime-equivalent artifact tuple with deployment receipts.
- If acceptance requires an evidence-bearing source change, update `BETA-REL-02` only after the replacement full SHA is deployed to every changed staging service and receives fresh source, deployment, and acceptance receipts; unchanged paired services must retain exact-version readback. The former SHA then becomes historical provenance.
- `2c04bc2`, `748c0e4`, `5229f99`, `25019a3`, `f6bbf58`, `abb5413`, and `32ec582` remain historical lineage artifacts and must not be used as the active `BETA-REL-02` identity.
- Truncated or guessed IDs (for example, `078f582`) are invalid for staging promotion unless a full SHA equivalence receipt is recorded.

## Current evidence register

| Claim | Classification | Evidence / limitation |
|---|---|---|
| PR #77's `beta_0.1` content is locally integrated | `VERIFIED-SOURCE` | `d3b4590fb86966b2a3b4c69ab2c8fea9aa0a8b7a` is a local merge commit on `rag/alpha-answer-accuracy`; its parents are `d4e45b44f3527b768408069f191951c2c3f492cb` and `7ec8298664a40b7b138f2b8d042d89858645cb67`. |
| User-reported merge `3eebf57` | `REPORTED` | `git cat-file -t 3eebf57` is unavailable in this host's object database. Do not substitute the reported SHA for `d3b4590` or treat either SHA as deployed. |
| Exact source candidate | `STAGING-DEPLOYED` | `409a8ae205c515e453477703bc2342744231006d` removes every Beta ingest destination while preserving Alpha `/ops/ingest`, keeps Alpha as the sole evidence authority, and repairs real Clerk logout when the public-key branch was unavailable at client bundle time. Web version `b4e4ef19-2990-4f1b-bdf1-c9d8d8556792` serves that exact candidate; Edge version `71f8c529-1040-4248-8e36-351741fc4edb` is runtime-equivalent because no Edge runtime source changed after `2c04bc2`. Production was not changed. |
| Exact-candidate checks | `VERIFIED-SOURCE` | At `409a8ae`: Edge 388/388, web unit 225/225, web contracts 97/97, TypeScript, lint, production/OpenNext build (89 pages), privacy 0/400, architecture freshness across 631 inputs, and `git diff --check` pass. |
| Earlier local runtime report | `REPORTED-SUPERSEDED` | The earlier report of a local full-stack flow and remote D1 migrations was not itself environment evidence. The current named-profile staging receipts below independently establish the actual `wtfmedia-ops-staging` migration state and deployments; the earlier report still proves nothing about production. |
| Existing `v0.3.3-beta.2` tag | `VERIFIED-SOURCE` | Reconciled by owner decision on 2026-09-12: the remote annotated tag exists and peels to `66f434ab152fd2776c961aed21926e76b5df9204`, whose packages still declare `0.3.3-beta.1`; it is preserved immutably as historically inconsistent provenance, not the current Beta or a deployment receipt. It will not be deleted, moved, reused, or reinterpreted. |
| Remote PR topology | `VERIFIED-SOURCE` | PR #48 and PR #77 remain untouched. The owner-authorized delivery lane is a clean new `codex/*` remote branch at the exact final documentation head, not `rag/alpha-answer-accuracy` or `beta_0.1`; PR #77 may advance only after the remaining acceptance gates pass. No force-push is permitted. |
| Alpha/Beta precedence | `VERIFIED-SOURCE` | Alpha's canonical `/api/chat` owns corpus, ingest, retrieval, inference, grounding, citations, timestamps, enriched moments, and request budgets. Beta owns its UI/routes, Clerk identity, D1 admission/RBAC/admin, and owner-scoped persistence/lifecycle. PR #48 remains protected source lineage, not a second Beta inference authority. |
| Beta lineage and surface map | `VERIFIED-SOURCE` | `v0.3.3-beta.2` is older `main`, `release/beta` is the merged Member lane, `beta_0.1` adds principal/RBAC/admin plus a role-based operator chat fork, Pavun57 owns RAG behavior, and `abb5413` is their local staged predecessor. See the dedicated lineage audit; do not use "latest Beta" as an exact ref. |
| Beta hostname availability | `STAGING-LIVE` | Cloudflare accepted `beta-staging.wtfhq.in` as the `wtfmedia-web-staging` Custom Domain. Its authoritative nameservers return Cloudflare A records, TLS exact-host probes pass, `/` redirects to `/beta`, and the signed-out principal endpoint returns `401`. Codex IAB now resolves the canonical hostname and authenticated the existing super-admin. `beta.wtfhq.in` remains unassigned live infrastructure. |
| Super-admin IAB observation | `STAGING-OBSERVED-PARTIAL` | At final staging Edge `71f8c529-1040-4248-8e36-351741fc4edb` and Web `b4e4ef19-2990-4f1b-bdf1-c9d8d8556792`, authenticated IAB preserved the Alpha-derived answer, grounded citation header, rich source sheet, owner history, archive actions, composer, long-title clamping, drawer focus return, and zero horizontal overflow at 1382x887, 1382x1180, and 320x710. Real Clerk logout then redirected to `/sign-in`; direct protected-chat navigation exposed no private conversation. This remains one-persona evidence; Member A/B, editor, admin, and suspended/revoked observations remain open. |
| Alpha-overlay grounded-chat readiness | `STAGING-ACCEPTED` | Staging health returns `inferenceService=wtfmedia-web` and `corpusAuthority=alpha_public_api`. Resource-binding readback names only `DB=wtfmedia-ops-staging` and `WTFMEDIA_ALPHA_WEB -> wtfmedia-web`; no AI, Vectorize, R2, KV, queue, or ingest resource exists. The separately authorized versioned secret deployment removed inert `INGEST_TOKEN`; final secret-name readback lists only Clerk, Edge transport, and origin-proof secrets. `/v1/admin/enqueue` and `/beta/preview` remain absent. |

## Environment and client-handoff hostname contract

| Lane | Canonical client URL | Public ingress | Paired edge | Binding boundary |
|---|---|---|---|---|
| Public Alpha/current production | `https://wtfhq.in` | `wtfmedia-web` | `wtfmedia-edge` | Existing production resources; unchanged by Beta hostname work |
| Beta staging acceptance | `https://beta-staging.wtfhq.in` | `wtfmedia-web-staging` Custom Domain | `wtfmedia-edge-staging` through `WTFMEDIA_EDGE` | Staging Clerk + `wtfmedia-ops-staging`; Edge calls Alpha through `WTFMEDIA_ALPHA_WEB -> wtfmedia-web`; no Beta corpus or ingest resources; inert `INGEST_TOKEN` removed through a separately authorized versioned deployment |
| Beta production/client handoff | `https://beta.wtfhq.in` | planned dedicated `wtfmedia-web-beta` Custom Domain | planned dedicated `wtfmedia-edge-beta` through `WTFMEDIA_EDGE` | Explicit identity/persistence bindings plus reviewed Alpha chat binding after approval; no duplicate corpus or ingest consumer |
| Diagnostic origin | environment-specific `workers.dev` URL | Direct Worker diagnostic only | Never a client handoff URL | Reachability does not prove Custom Domain, binding, revision, or authorization identity |

The staging Beta hostname is live; the production Beta hostname remains a
planning target. The web Worker is the only public ingress. The edge Worker
remains reachable from web through the paired service binding. Each Beta
hostname root must resolve or redirect canonically to `/beta`; host-only
session cookies must prevent Alpha, staging, and production-Beta sessions from
mixing.

## Release criteria

### A. Source closure before any staging request

- [x] **BETA-REL-01:** Runtime/UI closure is commit `79a0285`; release/migration guardrails are commit `b7f5bec`; planning and generated architecture are commit `2c4007d`.
- [x] **BETA-REL-02:** The exact staging-deployed runtime candidate is `409a8ae205c515e453477703bc2342744231006d`. Alpha is its sole inference/evidence authority; Beta is an authenticated identity, authorization, administration, and owner-scoped persistence overlay without a second ingest surface.
- [x] **BETA-REL-03:** At exact runtime candidate `409a8ae`, Edge 388/388, web unit 225/225, web contracts 97/97, typecheck, lint, production/OpenNext build (89 pages), privacy 0/400, architecture freshness across 631 inputs, and `git diff --check` pass.
- [x] **BETA-REL-04:** Source review confirms the single-shell contract: `/beta` resolves the principal landing; both admitted principal kinds use role-safe `/beta/chat`; operators retain `/beta/workspace`; edge/D1 remains authoritative; public Alpha stays separate; `/beta/preview` is absent; and fake member/fallback data is not used as evidence.
- [x] **BETA-REL-04A:** Owner reconciliation preserves `v0.3.3-beta.2` immutably as historically inconsistent provenance at `66f434ab152fd2776c961aed21926e76b5df9204`, not the current Beta or a deployment receipt. It will not be deleted, moved, reused, or reinterpreted; the aligned but still-unpublished candidate manifests remain `0.3.3-beta.3`.
- [x] **BETA-REL-04B:** Remote delivery topology is explicit: leave PR #48 and PR #77 untouched; publish the accepted work first as a clean new `codex/*` branch with an exact `HEAD:refs/heads/<name>` refspec. Do not update `beta_0.1` until all remaining acceptance gates pass. Never force-push or use a moving branch name as the accepted release identity.
- [x] **BETA-REL-04C:** Apply the owner precedence map: Beta governs UI, canonical Beta/Ops routing, Clerk/auth landing and return flow, navigation, edge/D1 admission/RBAC/admin, Settings, and owner-scoped persistence/lifecycle. Alpha's canonical `/api/chat` governs corpus, ingest, inference, retrieval, grounding, citations, timestamps, moments, and request budgets. Shared files are resolved symbol-by-symbol.
- [x] **BETA-REL-04D:** `web/wrangler.jsonc` maps `beta-staging.wtfhq.in` only to `wtfmedia-web-staging`; its `WTFMEDIA_EDGE` service binding names only `wtfmedia-edge-staging`. Edge staging resource bindings are only `DB=wtfmedia-ops-staging` and `WTFMEDIA_ALPHA_WEB -> wtfmedia-web`; it has no AI, Vectorize, R2, KV, queue, or ingest resource. Separately authorized secret-name readback proves inert `INGEST_TOKEN` is absent. Planned `beta.wtfhq.in` remains absent from live routes and requires its dedicated production-Beta pair.
- [x] **BETA-REL-04E:** Exact-host tests prove both Beta roots map to `/beta`, `wtfhq.in` and `workers.dev` do not receive the Beta-host redirect, spoofed suffixes fail safely, and the app config does not enable Clerk satellite/domain sharing. IAB/CDP confirms authentication session and refresh cookies are host-only to `beta-staging.wtfhq.in`; parent-domain Clerk client metadata is not accepted as Beta session authority.
- [x] **BETA-REL-04F:** Exact Git ancestry distinguishes the historical beta tags, `origin/release/beta`, PR #77's `beta_0.1`, PR #48, local merge `d3b4590`, and current runtime candidate `409a8ae` (with `2c04bc2`, `748c0e4`, `5229f99`, `25019a3`, `f6bbf58`, `abb5413`, and `32ec582` retained as historical lineage rows); the surface authority is recorded in the Beta 0.3 lineage audit.
- [x] **BETA-REL-04G:** `/beta/chat` uses the accepted Alpha-derived presentation for both principal kinds through typed owner-scoped adapters. Member `mcnv_*` and operator `cnv_*` stores, lifecycle capabilities, RBAC/admin surfaces, and the canonical Alpha answer/source/moment contract remain distinct and intact; Beta does not fork inference or evidence.
- [x] **BETA-REL-04H:** Settings has one canonical account, memory, sessions, and appearance lane for every admitted principal, with capability-gated release, users, and audit additions for operators. Real Clerk logout is staging-verified. Memory exposes the immutable create/archive preference lifecycle; Sessions reads active plus archived owner history. The Beta 0.1 owner decision keeps operator history Archive-only; permanent Operator Delete remains unavailable until a separately reviewed detach/tombstone, audit, and anti-resurrection contract exists.
- [x] **BETA-REL-04I:** Beta exposes no ingest capability, navigation destination, protected policy, or callable `/beta/workspace/ingest` UI; the direct URL fails closed. Alpha `/ops/ingest`, its role grants, retrieval, inference, citations, timestamps, and rich moments remain unchanged.

### B. Staging acceptance for the exact candidate

- [x] **BETA-REL-05:** Before the current candidate's D1 change, Time Travel returned recovery bookmark `0000006b-00000000-000050e3-0bb7072294902e18dbbd945231ef35dc`; the migration inventory identified only reviewed migration `0016_rich_chat_source_metadata.sql` for this change.
- [x] **BETA-REL-06:** Wrangler applied only `0016_rich_chat_source_metadata.sql` to `wtfmedia-ops-staging`. This extends Beta's owner-scoped message persistence for Alpha's validated public citation/moment projection; it does not create or seed a corpus, ingest queue, Vectorize index, R2 bucket, or KV namespace.
- [x] **BETA-REL-07:** *(Historical provenance preserved)* Candidate `abb5413` was deployed to `wtfmedia-edge-staging` as version `3b4aa27f-6c62-4077-a432-897f10002039`. Wrangler readback showed staging-only bindings and the canonical Clerk authorized party; `/v1/health` returned HTTP 200 with `environment=staging`, `service=wtfmedia-edge-staging`, and `index=wtfmedia-catalogue-staging-v1`. This is historical predecessor evidence, not the current `748c0e4` identity or binding contract.
- [x] **BETA-REL-08:** *(Historical provenance preserved)* Candidate `32ec582` was deployed to `wtfmedia-web-staging` as version `b67a0833-3eda-4c53-b6ed-056969200350`. Wrangler retained `beta-staging.wtfhq.in`, `ASSETS`, self-reference, and only the `wtfmedia-edge-staging` service binding. Staging edge remained version `3b4aa27f-6c62-4077-a432-897f10002039`; production web/edge versions remained unchanged. This is historical predecessor evidence, not the current `748c0e4` identity.
- [x] **BETA-REL-08B:** Authenticated super-admin IAB proves the repaired staging shell: shared chat presentation, persisted operator history, zero document-width overflow, no chat/settings bottom dock, no ingest destination, reduced settings navigation, and a real D1-backed operator roster. This closes the named regression only, not BETA-REL-09 through BETA-REL-11.
- [x] **BETA-REL-08C:** Candidate `f6bbf58` is deployed as edge version `427b1ea7-4e2a-4a8e-86c1-1920c0741e6c` and web version `3668390f-1dc8-438e-9dab-19c331b732ce`. Authenticated super-admin IAB confirms chat history loads through the operator route, Account shows logout, duplicate workspace memory/session links are absent, Preferences loads its real empty owner ledger and create form, and Sessions loads both current conversations through `includeArchived=1`. No archive, delete, memory, roster, release, Clerk, DNS, or production mutation was performed during acceptance.
- [x] **BETA-REL-08D:** Runtime candidate `25019a3` is deployed as Edge version `a68b4793-ed57-4c1c-8436-2e574ccdb192` and web version `cbc25bba-c2a1-436f-8612-11813dbd3946`. Named-profile readback proves the staging-only bindings and healthy staging identity; pre/post readback leaves production Edge `ccd1d952-5be7-41c9-9275-f9d6b3b470a7` and web `3d5a5965-14f3-486a-a608-330d539dec81` unchanged. Authenticated IAB proves a new owner-scoped empty-corpus conversation persists across reload at all three required viewports with zero horizontal overflow. This is partial runtime acceptance, not grounded rich-sheet acceptance.
- [x] **BETA-REL-08A:** **DROPPED / SUPERSEDED by owner decision 2026-09-12.** Beta must not populate or operate a separate staging evidence corpus. Alpha already owns corpus/ingest/retrieval/inference/grounding/citations/moments, and Beta consumes only its validated public chat projection through the reviewed service binding.
- [x] **BETA-REL-08E:** *(Historical QA-failed receipt preserved.)* Candidate `5229f99` was deployed only to staging Edge version `91b2fc7a-f657-4dd1-a66d-4f3c50ad7b21`; staging Web remained `cbc25bba-c2a1-436f-8612-11813dbd3946`. Independent QA found that grouped Alpha citation markers were misclassified as ungrounded, so this receipt is not final grounded acceptance despite the otherwise correct identity/persistence boundary.
- [x] **BETA-REL-08F:** Candidate `748c0e4` is deployed only to staging Edge version `67e93676-8519-4e33-8b7b-83b45fa975a8`; staging Web remains `cbc25bba-c2a1-436f-8612-11813dbd3946`. The final fix reuses Alpha's canonical grouped-citation parser. A fresh authenticated super-admin `cnv_*` conversation renders a grounded answer and rich source sheet with no abstention/fallback label and remains correct after reload. Per-episode `N cited` counts cited excerpts, not editor moment rows. Production Edge `ccd1d952-5be7-41c9-9275-f9d6b3b470a7` and Web `3d5a5965-14f3-486a-a608-330d539dec81` remained unchanged.
- [x] **BETA-REL-08G:** Final staging runs Edge `71f8c529-1040-4248-8e36-351741fc4edb` and Web `b4e4ef19-2990-4f1b-bdf1-c9d8d8556792` for candidate `409a8ae` (Edge runtime-equivalent from `2c04bc2`). Health names staging and `alpha_public_api`; the secret list omits `INGEST_TOKEN`; `/v1/admin/enqueue` and `/beta/preview` are absent; production Edge `ccd1d952-5be7-41c9-9275-f9d6b3b470a7` and Web `3d5a5965-14f3-486a-a608-330d539dec81` are unchanged.
- [x] **BETA-REL-08H:** The final Alpha-derived chat surface has grounded citations, rich moments, playback/export, owner history, archive actions, reachable composer, long-title clamping, drawer focus return, and zero horizontal overflow at 1382x887, 1382x1180, and 320x710.
- [x] **BETA-REL-08I:** Live acceptance found and repaired an inert logout fallback. On final Web, the control invokes the browser Clerk session, redirects to `/sign-in`, and a direct visit to the prior protected chat exposes no conversation, evidence, history, archive action, or chat composer.
- [ ] **BETA-REL-09:** Signed-out denial/recovery and real logout now pass. Still use the Codex in-app browser to prove Member A/Member B isolation across private chat, history, archive/delete, settings, and fresh reauthentication.
- [ ] **BETA-REL-10:** In the Codex in-app browser, prove editor, admin, super-admin, and suspended/revoked route, screen, action, direct-URL, and server-policy behavior, including the shared `/beta/chat` role-safe history view; super-admin landing must not create a member row.
- [x] **BETA-REL-11:** Record the real authenticated `https://beta-staging.wtfhq.in` flow at 1382x887, 1382x1180, and 320x710, including environment identity, drawer focus, composer/source reachability, long titles, session actions, and no protected-content flash. The final Web change is isolated to logout; the accepted chat-shell artifact is runtime-equivalent across the receipt.
- [x] **BETA-REL-12:** Staging rollback is rehearsed with exact versions in both services. Edge moved from accepted `67e93676-8519-4e33-8b7b-83b45fa975a8` to predecessor `91b2fc7a-f657-4dd1-a66d-4f3c50ad7b21` and back before the no-ingest/secret candidate; Web then moved from final `b4e4ef19-2990-4f1b-bdf1-c9d8d8556792` to predecessor `d098c2ce-fd70-4540-bad3-cc547a8f276d` and back. Staging health, signed-out ingress, and public Alpha stayed healthy; D1/corpus state was not rolled back or mutated.

IAB note: the canonical hostname resolves in Codex IAB and the existing
super-admin authenticated before the final verified logout. Candidate `409a8ae` proves the Alpha-overlay
path: the grounded rich source sheet and owner-scoped persistence work without
a Beta corpus, including grouped Alpha citation markers after reload. Operator
permanent Delete remains unavailable because that server store
has no reviewed delete/tombstone contract. Do not close BETA-REL-09 or
BETA-REL-10 from this one-persona observation. The live roster currently has no
suspended/revoked principal, so that lifecycle requires an audited staging test
identity rather than a fabricated browser state.

### C. Production promotion after staging acceptance

- [ ] **BETA-REL-13:** Obtain a fresh owner authorization naming the accepted staging evidence, BETA-REL-02 SHA, production target, release window, and rollback owner.
- [ ] **BETA-REL-14:** Capture a production Clerk configuration receipt authorizing exactly `https://beta.wtfhq.in`, including issuer/JWKS/authorized-party and redirect/domain policy presence; record no secrets or session material and do not reuse the staging Clerk project.
- [ ] **BETA-REL-15:** Capture a production D1 backup/recovery and migration inventory receipt before any production migration.
- [ ] **BETA-REL-16:** Apply the reviewed production migrations for BETA-REL-02 and record the post-apply migration/readiness receipt separately from staging.
- [ ] **BETA-REL-17:** Deploy and verify the exact BETA-REL-02 Edge revision to dedicated `wtfmedia-edge-beta` before web traffic changes; record version, health, explicit identity/persistence bindings, the reviewed Alpha web service binding, and absence of AI/Vectorize/R2/KV/queue/ingest bindings.
- [ ] **BETA-REL-18:** Deploy and verify the exact BETA-REL-02 web revision to dedicated `wtfmedia-web-beta`, attach the `beta.wtfhq.in` Custom Domain, and record version, certificate/domain activation, `ASSETS`, and binding only to `wtfmedia-edge-beta`; the unchanged Alpha inference contract remains behind the Beta Edge boundary.
- [ ] **BETA-REL-19:** Record `beta.wtfhq.in` custom-domain and traffic attribution to the exact production-Beta web revision, then perform the approved read-only production smoke without assuming reachability proves revision identity; `wtfhq.in` must remain attributed to public Alpha.
- [ ] **BETA-REL-20:** Rehearse or execute the approved rollback path with exact prior web/edge versions, D1 recovery decision, domain/traffic recovery, and post-rollback read-only smoke.

All production promotion rows (`BETA-REL-13` through `BETA-REL-20`) therefore require explicit receipts tied to the then-current full SHA in `BETA-REL-02` (currently `409a8ae`) and may not use `078f582` or any other SHA unless an equivalence receipt is attached.

## Explicitly deferred from the Beta 0.1 exit decision

These remain work items, not hidden promises or evidence gaps that may be
silently inferred from the Beta release:

- **BETA-DEFER-01 — provider and YouTube persistence:** AI Route and YouTube
  Analytics remain non-persisted local previews, synthetic, and non-authoritative.
  No fake production corpus shortcuts are authorized until a separately approved
  provider integration, data contract, and lifecycle exists.
- **BETA-DEFER-02 — scheduled jobs:** YouTube/provider refresh, reporting, and
  other scheduled jobs require separately approved source access, schedule,
  failure handling, and operational receipts.
- **BETA-DEFER-03 — long-context compaction:** no claim of automatic
  compaction, durable full-session continuity, or context-checkpoint recovery
  is allowed until paging, checkpoint, failure, and evaluation criteria pass.
- **BETA-DEFER-04 — full Alpha alignment:** the release may verify the
  explicitly listed authenticated Beta routes and viewports, but it does not
  certify a wholesale merge, complete visual/interaction parity, or all future
  Alpha capability adoption.
- **BETA-DEFER-05 — inert staging secret cleanup:** version readback still lists
  a legacy `INGEST_TOKEN` secret on the staging Edge. It provides no ingest
  capability because the enqueue route is disabled and no queue/resource is
  bound. Removing it requires a separate owner-approved credential mutation.

## Handoff rule

Future agents must append evidence to the applicable checklist row rather than
upgrading a `VERIFIED-SOURCE` or `REPORTED` fact into `STAGING-ACCEPTED` or
`PRODUCTION-ACCEPTED`. A failed, unknown, missing, or mismatched exact-revision
receipt blocks the corresponding release decision.
