# Lessons

- When the user asks for a new video after an existing render, do not assume the old composition should be replaced. Create a separate composition/output unless they explicitly ask to overwrite or shorten the previous one.
