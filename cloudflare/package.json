{
  "name": "wtfmedia-edge",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "target:config": "node scripts/generate-target-config.mjs",
    "target:dry-run": "node ../scripts/wrangler-profile.mjs wtfmedia deploy --dry-run --config wrangler.target.generated.jsonc",
    "target:deploy": "node scripts/deploy-target.mjs",
    "types": "wrangler types",
    "test": "node scripts/test.mjs"
  },
  "devDependencies": {
    "jose": "6.2.10",
    "wrangler": "4.95.0"
  }
}
