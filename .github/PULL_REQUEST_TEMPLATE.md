<!--
Session-PR template. Delete every section that doesn't apply.
Every claim in this PR should resolve to a source, an owner, or an
explicit unknown — the "evidence precedes automation" rule from AGENTS.md.
-->

## Summary

<!-- One paragraph. What changed and why. Link the driving requirement,
     spec section, decision ID, or client-question doc. -->

## What ships

<!-- Bulleted list of concrete deliverables. File paths welcome.
     Group by concern; keep this PR single-concern. -->

## Not in this PR

<!-- What a reader might expect but won't find here. Prevents scope creep
     comments during review. -->

## Verification checklist

<!-- Uncheck what doesn't apply; add rows as needed. Every ticked box
     should be backed by a command you actually ran or a file you actually
     inspected. -->

- [ ] Only the paths listed under "What ships" are staged
- [ ] No credentials, tokens, absolute machine paths, or private URLs land
- [ ] `git diff --check` reports no whitespace issues in this diff
- [ ] Repository lint / typecheck / build pass locally where applicable
- [ ] `.planning/` docs added here open with `Status: draft-held` if they
      propose a decision
- [ ] Rollback path is either trivial (`git revert`) or documented below

## Next after this merges

<!-- The named next branch, PR, or client reply this unblocks. Keeps the
     roadmap legible. -->

## Trigger multi-agent cloud review

<!-- Optional. Uncomment the line below and comment it on the PR (or run
     it from a Claude Code terminal) to launch the ultrareview. -->

<!-- /code-review ultra -->
